{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades da janela principal (criada em runtime na URL do backend).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
