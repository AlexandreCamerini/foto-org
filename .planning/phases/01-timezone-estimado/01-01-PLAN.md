---
phase: 01-timezone-estimado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fotoorganizer/geolocation/timezones.py
  - tests/test_timezones.py
  - fotoorganizer/classification/engine.py
  - tests/test_suggestion_engine.py
  - fotoorganizer/server/app.py
  - tests/test_server_api.py
autonomous: true
requirements: [TZ-01]

must_haves:
  truths:
    - "Uma foto cujo país foi resolvido (GPS próprio, herança temporal D-025, ou nome de pasta) ganha tz_estimado depois de gerar()"
    - "Uma foto sem país conhecido (ou país fora da tabela) tem tz_estimado=None — nunca inventa, nunca lança erro"
    - "Regenerar sugestões (segunda chamada a gerar()) atualiza tz_estimado — não deixa valor obsoleto de uma rodada anterior"
    - "GET /api/midia/{id} devolve o campo tz_estimado"
    - "Toda entrada de TZ_POR_PAIS é um identificador IANA válido, e a tabela cobre os 98 países de PAISES_PT por completo"
  artifacts:
    - path: "fotoorganizer/geolocation/timezones.py"
      provides: "TZ_POR_PAIS: dict[str, str] — nome PT-BR do país (mesma chave que PAISES_PT.values() produz) → fuso IANA"
      contains: "TZ_POR_PAIS: dict[str, str]"
    - path: "tests/test_timezones.py"
      provides: "Cobertura completa dos 98 países + validação IANA via zoneinfo"
      contains: "zoneinfo.available_timezones()"
    - path: "fotoorganizer/classification/engine.py"
      provides: "Escrita direta de media.tz_estimado dentro de _persistir_sugestao, sem Evidence/Suggestion"
      contains: "media.tz_estimado"
    - path: "fotoorganizer/server/app.py"
      provides: "tz_estimado no JSON de _media_json (grade e detalhe)"
      contains: "\"tz_estimado\": m.tz_estimado"
  key_links:
    - from: "fotoorganizer/classification/engine.py"
      to: "fotoorganizer/geolocation/timezones.py"
      via: "TZ_POR_PAIS.get(evidencias[\"pais\"].valor)"
      pattern: "TZ_POR_PAIS\\.get"
    - from: "fotoorganizer/server/app.py"
      to: "fotoorganizer/models/catalog.py (MediaFile.tz_estimado)"
      via: "passthrough de atributo em _media_json"
      pattern: "m\\.tz_estimado"
---

<objective>
Fechar o modelo de dois instantes de D-038: fotos ganham `tz_estimado`, um
fuso IANA estimado a partir do país já atribuído (GPS próprio, herança
temporal de D-025, ou nome de pasta), gravado direto em `MediaFile` no
mesmo padrão não revisado de `gps_lat_estimado` — sem `Evidence`, sem
`Suggestion`, sem entrada em `docs/CONFIANCA.md`.

Purpose: `tz_estimado IS NOT NULL` passa a ser o sinal de "fuso conhecido"
do catálogo (D-038) — hoje esse sinal não existe, e a diferença entre
`data_capturada`/`data_capturada_utc` é ambígua para fusos reais de +00:00.

Output: tabela estática `TZ_POR_PAIS` (98 países), escrita em
`_persistir_sugestao`, campo exposto em `GET /api/midia/{id}`, testes
cobrindo os quatro cenários do spec (GPS próprio, herança, desconhecido,
regeneração).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-timezone-estimado/01-CONTEXT.md
@.planning/phases/01-timezone-estimado/01-PATTERNS.md
@docs/prompts/fase-11-timezone-estimado.md
</context>

<interfaces>
<!-- Contract Task 1 creates; Task 2 consumes directly, no exploration needed. -->

From fotoorganizer/geolocation/paises.py (existing, model for the new module):
```
PAISES_PT: dict[str, str] = {"AD": "Andorra", ..., "ZW": "Zimbábue"}  # 98 entries, ISO alfa-2 -> PT-BR name
```

New public contract Task 1 must produce in fotoorganizer/geolocation/timezones.py:
```
TZ_POR_PAIS: dict[str, str]  # key: PT-BR country name (PAISES_PT.values()), value: IANA zone id
```
No helper functions — consumer does a plain `TZ_POR_PAIS.get(nome)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tabela TZ_POR_PAIS + validação IANA</name>
  <files>fotoorganizer/geolocation/timezones.py, tests/test_timezones.py</files>
  <read_first>
    fotoorganizer/geolocation/paises.py (module shape, docstring voice, PAISES_PT — the exact 98 keys/values to cover)
    tests/test_paises.py (test file shape/imports to mirror)
    docs/prompts/fase-11-timezone-estimado.md §1 e §4 (regra de cobertura e de teste)
    .planning/phases/01-timezone-estimado/01-PATTERNS.md "Flags for Planner" #2 (nota sobre zoneinfo/tzdata — plataforma macOS-only deste projeto, sem dependência tzdata nova)
  </read_first>
  <behavior>
    - test_cobre_todos_os_paises_de_paises_pt: `set(TZ_POR_PAIS) == set(PAISES_PT.values())` — nenhum dos 98 países fica de fora, nenhuma chave extra.
    - test_todo_valor_e_identificador_iana_valido: para cada `pais, fuso in TZ_POR_PAIS.items()`, `fuso in zoneinfo.available_timezones()`.
  </behavior>
  <action>
    Criar `fotoorganizer/geolocation/timezones.py` no mesmo espírito de
    `paises.py`: `from __future__ import annotations`, docstring de módulo
    explicando o porquê (granularidade grosseira de propósito, zero rede —
    invariante 4, nenhuma dependência nova tipo `timezonefinder`/`pytz`/
    `geo-tz` — D-07), e um único `TZ_POR_PAIS: dict[str, str]` literal. Não
    reexportar em `fotoorganizer/geolocation/__init__.py` (mesmo padrão de
    `paises.py`, que também não está lá).

    Chave: o nome PT-BR exatamente como `PAISES_PT` produz (não o código
    ISO — D-05). Cobrir os 98 países de `PAISES_PT` por completo — nenhum
    de fora por acidente.

    Regra de preenchimento (D-08), documentada uma vez em comentário no
    topo do arquivo: para país com um único fuso oficial, usar o
    identificador IANA canônico da capital (resposta determinística, sem
    julgamento por país — ex.: França → "Europe/Paris", Japão →
    "Asia/Tokyo"). Para país com mais de um fuso oficial, usar o fuso da
    capital ou o de maior população. Os cinco exemplos citados
    explicitamente pelo spec (fase-11 §1, D-08), com os valores já
    verificados nesta sessão contra `zoneinfo.available_timezones()`:
    Brasil → "America/Sao_Paulo" (capital Brasília, mesmo fuso de São
    Paulo/Rio); Estados Unidos → "America/New_York" (capital Washington
    DC, fuso Eastern, também o mais populoso); Rússia → "Europe/Moscow";
    Canadá → "America/Toronto" (capital Ottawa, fuso Eastern — IANA não
    tem "America/Ottawa"); Austrália → "Australia/Sydney" (Sydney é o
    identificador IANA canônico da região da capital Canberra, também o
    mais populoso). Para qualquer outro país multi-fuso encontrado nos 98
    (ex.: México, Indonésia, Portugal, Espanha, Chile, Equador —
    identificação não é exaustiva, cabe ao executor detectar ao preencher
    a tabela), aplicar a MESMA regra (capital/maior população) e validar o
    valor escolhido contra `zoneinfo.available_timezones()` antes de
    considerar a entrada pronta — o teste `test_todo_valor_e_identificador_iana_valido`
    é a rede de segurança final, mas não substitui checar durante o
    preenchimento.

    Criar `tests/test_timezones.py` espelhando a forma de `test_paises.py`
    (docstring curto, imports diretos, funções de teste simples — sem
    parametrize necessário para estas duas asserções). Importar
    `zoneinfo` (stdlib), `PAISES_PT` de `fotoorganizer.geolocation.paises`,
    `TZ_POR_PAIS` de `fotoorganizer.geolocation.timezones`.
  </action>
  <acceptance_criteria>
    - `python3 -c "from fotoorganizer.geolocation.timezones import TZ_POR_PAIS; from fotoorganizer.geolocation.paises import PAISES_PT; assert set(TZ_POR_PAIS) == set(PAISES_PT.values())"` não lança AssertionError.
    - `pytest tests/test_timezones.py -x` passa, incluindo as duas funções de teste do bloco `<behavior>`.
    - `grep -c "TZ_POR_PAIS" fotoorganizer/geolocation/__init__.py` retorna 0 (não reexportado).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_timezones.py -x</automated>
  </verify>
  <done>TZ_POR_PAIS existe com 98 entradas cobrindo PAISES_PT.values(), todo valor validado como IANA por teste, arquivo não reexportado em __init__.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistência direta em _persistir_sugestao</name>
  <files>fotoorganizer/classification/engine.py, tests/test_suggestion_engine.py</files>
  <read_first>
    fotoorganizer/classification/engine.py linhas 1036-1066 (`_persistir_sugestao`, ponto de inserção exato) e linhas 328-351 (`_persistir_herancas`, precedente de escrita direta sem Evidence)
    tests/test_suggestion_engine.py linhas 1-91 (imports, fixture `_media`, `ambiente`, `FakeGeocoder`, helper `_sugestao_de`) e linhas 1026-1057 (`test_estimativa_some_quando_a_foto_ganha_gps_proprio` — molde exato para o teste de regeneração)
    .planning/phases/01-timezone-estimado/01-PATTERNS.md "Flags for Planner" #1 (por que NÃO usar o snippet literal do spec sem `else`)
    docs/prompts/fase-11-timezone-estimado.md §2 e §4 (snippet original e lista de cenários de teste exigidos)
  </read_first>
  <behavior>
    - País vindo de GPS próprio: `media.tz_estimado` é gravado com o valor de `TZ_POR_PAIS[<país resolvido>]` depois de `engine.gerar()`.
    - País só por herança temporal (sem GPS próprio, D-025): mesmo resultado — `tz_estimado` gravado.
    - Sem nenhum país conhecido: `tz_estimado` permanece `None` depois de `gerar()` — nunca lança exceção.
    - Regenerar sugestões (`gerar()` chamado uma segunda vez depois que a mídia perde a evidência de país que tinha antes) atualiza `tz_estimado` para `None` — não deixa valor obsoleto de uma rodada anterior (mesmo padrão de `test_estimativa_some_quando_a_foto_ganha_gps_proprio` para `gps_lat_estimado`).
  </behavior>
  <action>
    Em `fotoorganizer/classification/engine.py`, adicionar
    `from fotoorganizer.geolocation.timezones import TZ_POR_PAIS` junto aos
    demais imports diretos de submódulo de `geolocation` (mesmo padrão de
    `from fotoorganizer.geolocation.folder_names import _normalizar`, linha
    44).

    Dentro de `_persistir_sugestao`, inserir logo após o loop que popula
    `evidencias[draft.campo] = evidencia` (linha 1061) e antes de
    `campos = {campo: ev.valor for campo, ev in evidencias.items()}`
    (linha 1063):

    `media.tz_estimado = TZ_POR_PAIS.get(evidencias["pais"].valor) if "pais" in evidencias else None`

    Esta forma DEVIA da linha literal do spec
    (`docs/prompts/fase-11-timezone-estimado.md:117-119`, que não tem
    `else` e deixaria um `tz_estimado` obsoleto sobreviver quando uma
    rodada futura de `gerar()` não resolve mais `"pais"` para aquela
    mídia). A forma com `else None` é necessária para o próprio cenário de
    teste exigido pelo spec ("regenerar sugestões atualiza tz_estimado",
    fase-11 §4) e seque o mesmo padrão de `_persistir_herancas`
    (`engine.py:340-347`, que zera `gps_lat_estimado` quando a herança some).
    Ambas as formas satisfazem D-04 ("nunca inventa, nunca lança erro") —
    a diferença só aparece numa segunda chamada a `gerar()`.

    Nenhum try/except necessário — `dict.get()` já degrada para `None` em
    país desconhecido ou fora da tabela, sem exceção.

    Adicionar em `tests/test_suggestion_engine.py` os quatro cenários do
    bloco `<behavior>`, reaproveitando a fixture `_media`/`ambiente` e
    `FakeGeocoder` já existentes no arquivo (o geocoder falso já resolve
    coordenadas próximas de `(43.9, 4.8)` para `GeoResult("França", ...)`
    — `TZ_POR_PAIS["França"]` existirá depois da Task 1). Ler
    `tz_estimado` direto do `MediaFile` via `session.scalar(select(MediaFile).where(...))`
    (não é `Evidence`, D-03) — mesmo idioma usado no resto do arquivo.
  </action>
  <acceptance_criteria>
    - `grep -n 'from fotoorganizer.geolocation.timezones import TZ_POR_PAIS' fotoorganizer/classification/engine.py` retorna uma linha.
    - `grep -n 'media.tz_estimado = TZ_POR_PAIS.get' fotoorganizer/classification/engine.py` retorna uma linha, dentro de `_persistir_sugestao`.
    - `pytest tests/test_suggestion_engine.py -k tz_estimado -x` passa e cobre os 4 cenários do `<behavior>`.
    - `pytest tests/test_suggestion_engine.py -x` (arquivo inteiro) continua verde — nenhuma regressão nos testes existentes de `gps_*_estimado`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_suggestion_engine.py -k tz_estimado -x</automated>
  </verify>
  <done>media.tz_estimado gravado direto em _persistir_sugestao (com else None), 4 cenários de teste passando (GPS próprio, herança, desconhecido, regeneração), suíte completa de test_suggestion_engine.py verde.</done>
</task>

<task type="auto">
  <name>Task 3: Serialização em GET /api/midia/{id}</name>
  <files>fotoorganizer/server/app.py, tests/test_server_api.py</files>
  <read_first>
    fotoorganizer/server/app.py linhas 275-308 (`_media_json`, ponto de inserção exato — dict literal compartilhado pela grade e pelo detalhe)
    tests/test_server_api.py linhas 862-887 (`test_detalhe_traz_a_foto_que_doou_a_coordenada` — molde exato: fixture MediaFile via session, client.get, asserção de campo no JSON)
  </read_first>
  <action>
    Em `fotoorganizer/server/app.py`, dentro de `_media_json` (função que
    monta o JSON de `MediaFile`, usada tanto pela grade —
    `app.py:602`, `"itens": [_media_json(m, fora) for m in itens]` —
    quanto pelo detalhe — `app.py:686`,
    `detalhe = _media_json(media, _fontes_fora_de_alcance())`), adicionar
    ao dict literal a chave `"tz_estimado": m.tz_estimado` — passthrough
    direto de atributo, mesmo estilo de `"source_id": m.source_id` /
    `"trip_id": m.trip_id` (não é campo computado como `gps_lat_efetivo`,
    não precisa de query extra).

    Adicionar em `tests/test_server_api.py` um teste
    `test_detalhe_traz_o_fuso_estimado` que grava `media.tz_estimado`
    diretamente via `session` (fixture `MediaFile` já existente no banco
    de teste, sem precisar rodar `gerar()` — testa o serializador
    isoladamente) e confirma `client.get(f"/api/midia/{media_id}").json()["tz_estimado"]`
    igual ao valor gravado. Usar as fixtures `client`/`migrated_engine` e o
    import local `from fotoorganizer.models import MediaFile` dentro da
    função de teste — convenção já estabelecida no arquivo
    (`test_server_api.py:865, 893`).
  </action>
  <acceptance_criteria>
    - `grep -n '"tz_estimado": m.tz_estimado' fotoorganizer/server/app.py` retorna uma linha, dentro de `_media_json`.
    - `pytest tests/test_server_api.py -k tz_estimado -x` passa.
    - `pytest tests/test_server_api.py -x` (arquivo inteiro) continua verde.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_server_api.py -k tz_estimado -x</automated>
  </verify>
  <done>_media_json inclui tz_estimado (grade e detalhe), teste novo confirma o passthrough via GET /api/midia/{id}, suíte completa de test_server_api.py verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| server (FastAPI, 127.0.0.1) → webapp client | Já existente; esta fase apenas acrescenta um campo (`tz_estimado`) a uma resposta JSON já servida (`_media_json`), sem novo endpoint, sem novo input de usuário, sem rede externa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `_media_json` (`app.py`) — novo campo `tz_estimado` na resposta | accept | Mesma classe de exposição que `gps_estimado`/`gps_lat_efetivo`, já servidos pelo mesmo endpoint local-only (127.0.0.1); `tz_estimado` é fuso IANA por país (granularidade grosseira, sem coordenada precisa), não PII sensível, sem escrita nova de dado do usuário. |

Sem superfície de ataque nova: nenhum input de usuário, nenhum
subprocesso, nenhum pacote novo instalado (D-07 descarta dependência
externa de timezone). `TZ_POR_PAIS` é tabela estática de código, não dado
de usuário — sem vetor de tampering via input externo.
</threat_model>

<verification>
`pytest` verde no repositório inteiro, incluindo:
- `pytest tests/test_timezones.py -x` (cobertura + validação IANA)
- `pytest tests/test_suggestion_engine.py -x` (4 cenários novos + regressão)
- `pytest tests/test_server_api.py -x` (serialização + regressão)

Checagem manual adicional (não bloqueante, fora do critério de pronto
formal desta fase — ver D-13 em 01-CONTEXT.md): medir contra o catálogo
real, via cópia (`sqlite3 .backup`), quantas fotos ganhariam
`tz_estimado` e quantas só por herança — número que confirma ou não o
"~2.235 fotos" citado no ROADMAP. Roda depois que o catálogo for
repovoado (zerado em 2026-08-16).
</verification>

<success_criteria>
- `TZ_POR_PAIS` existe, cobre os 98 países de `PAISES_PT`, todo valor é
  IANA válido (testado).
- Foto com país resolvido por GPS próprio, herança ou pasta ganha
  `tz_estimado` depois de `gerar()`; sem país conhecido, `tz_estimado`
  fica `None`, nunca erro.
- Regenerar sugestões atualiza `tz_estimado` (não deixa valor obsoleto).
- `GET /api/midia/{id}` devolve `tz_estimado`.
- `tz_estimado` não passa por `Evidence`/`Suggestion`/revisão humana, não
  entra em `docs/CONFIANCA.md` (D-03).
- `data_capturada`/`data_capturada_utc` não são reescritos por esta fase
  (D-09).
- `pytest` inteiro verde.
</success_criteria>

<output>
Create `.planning/phases/01-timezone-estimado/01-01-SUMMARY.md` when done
</output>
