---
phase: 03-revis-o-acess-vel-e-consistente
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/components/Review.tsx
  - webapp/src/components/Inspector.tsx
  - webapp/src/components/Operations.tsx
autonomous: false
requirements: [REV-02]

must_haves:
  truths:
    - "O total da fila de Revisão (\"N em M grupos\") é lido em texto-2 (≈6,1:1), não em texto-3 (≈3,46:1)"
    - "O nome do arquivo no subtítulo da miniatura de comparação é lido em texto-2"
    - "As mensagens de estado vazio de Revisão e do Inspetor são lidas em texto-2"
    - "Rótulo do botão \"desfazer\", nomes de namespace e chaves de metadado do Inspetor são lidos em texto-2"
    - "O progresso \"N/M copiados\" e o veredito do dry-run em Operações são lidos em texto-2"
    - "Os 10 usos decorativos/transientes de texto-3 (caret, seta aria-hidden, ícone ✎, contadores em badge, \"carregando…\", \"lendo…\", placeholder, fallback de CORES_STATUS) continuam texto-3"
  artifacts:
    - path: "webapp/src/components/Review.tsx"
      provides: "3 promoções texto-3 → texto-2 (linhas 145, 253, 447)"
      contains: "text-texto-2"
    - path: "webapp/src/components/Inspector.tsx"
      provides: "4 promoções texto-3 → texto-2 (linhas 202, 239, 246, 250)"
      contains: "text-texto-2"
    - path: "webapp/src/components/Operations.tsx"
      provides: "2 promoções texto-3 → texto-2 (linhas 152, 223)"
      contains: "text-texto-2"
  key_links:
    - from: "webapp/src/components/*.tsx"
      to: "webapp/src/index.css @theme"
      via: "classe Tailwind text-texto-2 resolvida pelo token --color-texto-2 já existente"
      pattern: "text-texto-2"
---

<objective>
REV-02 (parcial): promover a `texto-2` as 9 linhas de `texto-3` que carregam
conteúdo real em `Review.tsx`, `Inspector.tsx` e `Operations.tsx`, e deixar
intactas as 10 que são decoração, estado transiente ou convenção.

Purpose: `texto-3` tem contraste ≈3,46:1 — abaixo de WCAG AA para texto de
corpo. O commit `ae60319` já corrigiu as 4 instâncias citadas no achado
original de `docs/AVALIACAO_UX.md` B.1; sobraram 19 usos, auditados linha a
linha em CONTEXT.md D-02 pelo critério travado em D-01 (**vira texto-2 quando
o usuário precisa LER aquele texto para decidir algo; fica texto-3 quando é
auxílio secundário ao lado de um valor já legível, estado transiente ou
convenção universal**). Aplicar `texto-2` a tudo seria competir com a foto,
contra `docs/DIRECAO_DE_ARTE.md` — por isso a lista é fechada, não heurística.

Output: 9 trocas de classe Tailwind (`text-texto-3` → `text-texto-2`), sem
nenhuma mudança de JSX, prop, lógica ou token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-revis-o-acess-vel-e-consistente/03-CONTEXT.md
@.planning/phases/03-revis-o-acess-vel-e-consistente/03-PATTERNS.md
@.planning/phases/03-revis-o-acess-vel-e-consistente/03-UI-SPEC.md
@CLAUDE.md

Fase 100% frontend. Nenhum endpoint, nenhum backend, nenhuma dependência,
nenhum token novo — `texto-2` e `texto-3` já existem no `@theme` de
`webapp/src/index.css` (`--color-texto-2 #9499a2`, `--color-texto-3 #686c73`).

**Precedência a respeitar:** onde `03-UI-SPEC.md` (escrito antes do
`/gsd:discuss-phase`) e `03-CONTEXT.md` D-02 divergirem, **D-02 vence** —
é a decisão travada do dono, posterior e linha a linha. Divergência concreta
conhecida: a UI-SPEC sugere promover também o fallback
`CORES_STATUS[...] ?? "text-texto-3"` (Operations.tsx:291); D-02 mantém essa
linha como `texto-3` (mesma família de REV-07, cor por estado, não é problema
de contraste de conteúdo). Não tocar a 291.
</context>

<interfaces>
<!-- Lista fechada. O executor não deve auditar, re-derivar nem procurar outras ocorrências. -->

**Estado atual medido (grep, 2026-08-16):**
`Review.tsx` 9 ocorrências de `text-texto-3` / 12 de `text-texto-2`;
`Inspector.tsx` 7 / 8; `Operations.tsx` 4 / 11.

**Promover a `text-texto-2` (9 linhas):**
- `Review.tsx:145` — `<span className="text-texto-3">` do total da fila
  (`{totalNaFila…} em {lista.length} grupos`)
- `Review.tsx:253` — `<div className="mb-1 truncate text-[11px] text-texto-3">`
  do nome do arquivo (`s.nome`) no subtítulo da miniatura
- `Review.tsx:447` — `<div className="px-3 pb-2 pl-[68px] text-texto-3">` de
  "Sem evidência registrada para esta sugestão."
- `Inspector.tsx:202` — `className="px-1 text-texto-3"` no rótulo do botão
  "desfazer" (`Botao variante="fantasma"`)
- `Inspector.tsx:239` — `<div className="mt-1 text-texto-3">` de "Este
  arquivo não trouxe metadado nenhum."
- `Inspector.tsx:246` — `<div className="mb-1 text-[11px] text-texto-3">`
  do rótulo de namespace (`ns.rotulo`)
- `Inspector.tsx:250` — `<dt className="w-24 shrink-0 break-all text-texto-3">`
  da chave de metadado (`item.chave`)
- `Operations.tsx:152` — `<div className="text-texto-3">` que envolve a linha
  de status do plano (`N/M copiados`)
- `Operations.tsx:223` — o literal `"text-texto-3"` no ramo `else` do
  ternário do veredito do dry-run

**Manter `text-texto-3` (10 linhas — não tocar):**
`Review.tsx` 141 (contador em badge), 190 (caret ▾/▸), 198 (seta →
`aria-hidden`), 316 (ícone ✎ com `title`), 403 e 443 ("carregando…");
`Inspector.tsx` 196 ("classificado por você"), 232 (contador entre
parênteses), 236 ("lendo…"); `Operations.tsx` 122
(`placeholder:text-texto-3`), 291 (fallback de `CORES_STATUS`).

**Molde do diff — commit `ae60319`, já aplicado 6× nestes mesmos arquivos:**
troca literal da substring `text-texto-3` por `text-texto-2` dentro de um
`className` existente. Zero mudança de estrutura JSX, zero prop nova, zero
import novo, zero mudança de lógica.

**Fora de escopo explícito:** qualquer outro arquivo (D-02 restringe REV-02 a
estes três); peso de fonte, espaçamento, radius (UI-SPEC: nada de
`font-semibold`→`font-medium` como drive-by, nada de mexer em `px-3`, nada de
`leading-*` novo); `Funil.tsx`/`ArvoreDePastas.tsx` e demais componentes que
também usam `text-texto-3`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Promover as 9 linhas de conteúdo a texto-2</name>

  <files>webapp/src/components/Review.tsx, webapp/src/components/Inspector.tsx, webapp/src/components/Operations.tsx</files>

  <read_first>
    - `webapp/src/components/Review.tsx` — ler o arquivo e localizar as 9
      ocorrências de `text-texto-3` antes de editar; confirmar que as linhas
      145, 253 e 447 são as descritas em `<interfaces>` (o conteúdo, não só o
      número da linha, é o critério).
    - `webapp/src/components/Inspector.tsx` — idem, linhas 202, 239, 246, 250.
    - `webapp/src/components/Operations.tsx` — idem, linhas 152 e 223;
      confirmar que 223 é o ramo `else` do ternário cujo ramo `then` é
      `"text-erro"`.
    - `.planning/phases/03-revis-o-acess-vel-e-consistente/03-CONTEXT.md`
      D-01/D-02 — o critério e a lista fechada que autorizam exatamente estas
      9 linhas e proíbem as outras 10.
    - `webapp/src/index.css` — confirmar que `--color-texto-2` e
      `--color-texto-3` já existem no bloco `@theme` (nenhum token novo é
      necessário nesta fase).
  </read_first>

  <action>
    Substituir a substring `text-texto-3` por `text-texto-2` nas 9 linhas
    listadas em `<interfaces>`, e só nelas.

    Regras de precisão:
    - Em `Operations.tsx:223`, trocar apenas o literal `"text-texto-3"` do
      ramo `else`; o ramo `"text-erro"` (linha 222) fica idêntico.
    - Em `Operations.tsx:152`, muda só a cor-base da `<div>` que envolve a
      linha; o `<span>` interno do status continua recebendo sua cor de
      `CORES_STATUS[p.status]` — não mexer nesse `<span>` nem no dicionário.
    - `Operations.tsx:122` (`placeholder:text-texto-3`) e
      `Operations.tsx:291` (`CORES_STATUS[item.status] ?? "text-texto-3"`)
      **não** mudam, mesmo que `03-UI-SPEC.md` sugira o contrário — D-02
      (decisão do dono, posterior) prevalece.
    - `Inspector.tsx:202` muda, e `Review.tsx:316` não, apesar de ambos serem
      `className="px-1 text-texto-3"` dentro de botão: o do Inspetor tem
      texto visível ("desfazer") que o usuário precisa ler; o do Review é o
      ícone `✎` cuja ação já está no `title`. Conferir o conteúdo antes de
      editar para não trocar um pelo outro.
    - Nada mais muda em nenhum dos três arquivos: sem reordenar classes, sem
      renomear variável, sem reformatar JSX, sem alterar peso de fonte ou
      espaçamento, sem tocar em `Botao`/`Confianca`/`Miniatura`.
    - Não escrever a string literal `text-texto-3` em comentário novo nesses
      arquivos — as verificações são por contagem de linhas e um comentário
      falsearia o resultado.
  </action>

  <verify>
    <automated>cd webapp && for f in Review Inspector Operations; do echo "$f t3=$(grep -c 'text-texto-3' src/components/$f.tsx) t2=$(grep -c 'text-texto-2' src/components/$f.tsx)"; done && npx vitest run 2>&1 | tail -8 && npx tsc -b 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Contagens exatas depois da edição (eram `9/12`, `7/8`, `4/11`):
      `Review.tsx` → `text-texto-3` = **6**, `text-texto-2` = **15**;
      `Inspector.tsx` → **3** e **12**; `Operations.tsx` → **2** e **13**.
    - `grep -n 'text-texto-3' webapp/src/components/Review.tsx` devolve
      exatamente as linhas do contador em badge, do caret `▾`/`▸`, da seta
      `aria-hidden`, do ícone `✎` e das duas de "carregando…" — nenhuma outra.
    - `grep -n 'text-texto-3' webapp/src/components/Inspector.tsx` devolve
      exatamente "classificado por você", o contador entre parênteses e
      "lendo…".
    - `grep -n 'text-texto-3' webapp/src/components/Operations.tsx` devolve
      exatamente `placeholder:text-texto-3` e o fallback
      `CORES_STATUS[item.status] ?? "text-texto-3"`.
    - `grep -n 'text-erro' webapp/src/components/Operations.tsx` continua
      casando o ramo `then` do ternário do veredito (ramo intacto).
    - `git diff --stat` mostra apenas os 3 arquivos e **9 linhas alteradas no
      total** (9 inserções + 9 remoções), nenhum arquivo novo.
    - `git diff -U0 | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -vc 'texto-[23]'`
      retorna 0 — ou seja, nenhuma linha do diff muda algo que não seja o
      token de cor.
    - `cd webapp && npx vitest run` termina com 0 falhas (suíte inteira; os
      testes atuais não asseram className, então a promoção não pode quebrar
      nenhum deles — se algum quebrar, é sinal de edição fora do escopo).
    - `cd webapp && npx tsc -b` sai com código 0.
    - `git diff webapp/package.json webapp/package-lock.json` é vazio.
  </acceptance_criteria>

  <done>
    As 9 linhas de conteúdo renderizam com `--color-texto-2`; as 10 linhas
    decorativas/transientes seguem em `--color-texto-3`; suíte verde, tipos
    limpos, diff restrito a trocas de token de cor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — verificação visual de contraste nas 3 telas</name>

  <files>nenhum (task de verificação; não altera arquivo)</files>

  <read_first>
    - `docs/DIRECAO_DE_ARTE.md` — a régua "a foto é a cor da interface", que
      é o critério de julgamento do dono nesta verificação.
    - `.planning/phases/03-revis-o-acess-vel-e-consistente/03-CONTEXT.md`
      D-01/D-02 — o que deveria ter mudado e o que deveria ter ficado igual.
    - O diff da Task 1 (`git diff`) — para saber exatamente o que olhar.
  </read_first>

  <action>
    Apresentar ao dono o roteiro de `<how-to-verify>` abaixo, tela por tela
    (Revisão, Inspetor, Operações), e aguardar o sinal de retomada. Não
    prosseguir nem fechar o plano sem resposta explícita. Se o dono apontar
    uma linha como gritante demais ou ainda apagada demais, registrar qual e
    tratar como ajuste dentro do critério de D-01 — não reabrir a
    classificação inteira nem estender REV-02 a outros arquivos.
  </action>

  <verify>
    <automated>cd webapp &amp;&amp; npx vitest run 2>&amp;1 | tail -5</automated>
    <human-check>Dono confirma a legibilidade das 9 linhas promovidas e que as 10 mantidas em texto-3 seguem discretas, nas telas Revisão, Inspetor e Operações</human-check>
  </verify>

  <acceptance_criteria>
    - O dono respondeu "aprovado" ou nomeou telas/linhas com problema — não
      basta ausência de resposta.
    - Se houve ajuste, ele ficou restrito aos 3 arquivos da lista fechada e as
      contagens de `text-texto-3`/`text-texto-2` foram reconferidas depois
      (alvos: 6/15, 3/12, 2/13, ou o novo alvo explicitado no SUMMARY).
    - `git diff --name-only` continua listando no máximo os 3 componentes da
      Task 1.
  </acceptance_criteria>

  <done>
    Aprovação visual registrada (ou ajustes aprovados aplicados e
    reverificados), com o veredito do dono citado no SUMMARY.
  </done>

  <what-built>
    9 promoções de `texto-3` (≈3,46:1) para `texto-2` (≈6,1:1) nas três telas
    da lista fechada de D-02, deixando 10 usos legítimos de `texto-3`
    intactos. Não existe teste automatizado de contraste neste projeto (a
    suíte vitest testa texto/comportamento, não classe Tailwind), então o
    olho é a verificação — a Task 1 já provou por grep e diff que só as
    linhas certas mudaram.
  </what-built>

  <how-to-verify>
    1. Suba o app: `.venv/bin/python -m fotoorganizer` (ou o entrypoint local
       de sempre) + `cd webapp && npm run dev`; abra a URL do Vite em
       127.0.0.1.
    2. **Revisão** — confira que ficaram mais legíveis (cinza claro, não
       cinza apagado): o total da fila no topo ("N em M grupos") e, ao abrir
       a edição inline de uma sugestão, o nome do arquivo sob a miniatura.
       Expanda uma sugestão sem evidência: "Sem evidência registrada para
       esta sugestão." deve estar legível.
       Confira que continuam apagados de propósito: o contador dentro do
       badge ao lado do filtro, o caret `▾`/`▸`, a seta `→` entre origem e
       destino, o ícone `✎`, e o "carregando…".
    3. **Inspetor** — selecione uma foto: o rótulo "desfazer", o rótulo de
       namespace ("EXIF"/"IPTC"…) e as chaves de metadado ("ISO",
       "FNumber"…) devem estar legíveis. Em um arquivo sem metadado, "Este
       arquivo não trouxe metadado nenhum." também. Continuam apagados:
       "classificado por você", o contador entre parênteses no cabeçalho
       "Metadados do arquivo" e "lendo…".
    4. **Operações** — em um plano com dry-run: a linha de progresso
       "N/M copiados" e o veredito do dry-run devem estar legíveis. O
       placeholder do campo de busca continua apagado (convenção), e a
       palavra do status mantém a cor própria de `CORES_STATUS` (não virou
       cinza).
    5. Régua de julgamento (`docs/DIRECAO_DE_ARTE.md`): o texto promovido
       deve ficar confortável de ler sem passar a competir com a foto. Se
       alguma linha ficou gritante demais ou ainda apagada demais, diga qual.
  </how-to-verify>

  <resume-signal>Responda "aprovado" ou nomeie as telas/linhas com problema</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhum novo | Mudança exclusivamente de classe CSS em componentes React já existentes: nenhum input novo, nenhuma chamada de rede nova, nenhum dado novo renderizado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | `Inspector.tsx` chaves/namespaces de metadado, `Review.tsx` nome de arquivo | accept | O dado já era renderizado no DOM e já era visível; só o contraste muda. Nenhum campo novo é exposto e nada sai da máquina (app local-first, 127.0.0.1). |
| T-03-05 | Tampering | `Operations.tsx` linha de status e veredito do dry-run | mitigate | O veredito é o que autoriza executar um plano (invariante 2 do CLAUDE.md: dry-run antes de qualquer operação física). Torná-lo legível reduz risco de aprovação por leitura errada. Gate: a lógica do ternário e `CORES_STATUS` ficam intactas — provado pelo critério de diff restrito a `texto-[23]` na Task 1. |
| T-03-06 | Repudiation | `Review.tsx` mensagem "Sem evidência registrada" | accept | Melhora a leitura da trilha de evidências (core value do projeto); não altera o que é gravado nem o audit log. |
| T-03-SC | Tampering | instalação de pacotes npm/pip/cargo | mitigate | Zero pacotes instalados nesta fase. Gate: `git diff webapp/package.json webapp/package-lock.json` vazio (já em `<acceptance_criteria>`). Nenhum Package Legitimacy Audit necessário porque não há install. |

Perfil de risco: baixo. Troca de classe Tailwind sobre tokens já existentes;
nenhuma superfície de rede, nenhum fluxo de dado, backend intocado.
</threat_model>

<verification>
1. Contagens de `text-texto-3`/`text-texto-2` batendo com os alvos
   (6/15, 3/12, 2/13).
2. `git diff -U0` só contém linhas cujo delta é `texto-3` → `texto-2`.
3. `cd webapp && npx vitest run` verde e `npx tsc -b` sem erro.
4. Checkpoint humano aprovado nas 3 telas.
5. Nenhum arquivo fora dos 3 da lista fechada foi tocado.
</verification>

<success_criteria>
- REV-02 fechado no escopo travado por D-02: 9/9 linhas de conteúdo em
  `texto-2`, 10/10 linhas legítimas preservadas em `texto-3`.
- Nenhum arquivo além de `Review.tsx`, `Inspector.tsx` e `Operations.tsx`
  alterado; nenhuma mudança de estrutura, peso, espaçamento ou lógica.
- Aprovação visual do dono registrada no SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/03-revis-o-acess-vel-e-consistente/03-02-SUMMARY.md` when done
</output>
