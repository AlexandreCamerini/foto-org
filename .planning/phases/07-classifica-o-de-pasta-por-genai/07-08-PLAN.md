---
phase: 07-classifica-o-de-pasta-por-genai
plan: 08
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - webapp/src/components/Review.tsx
  - webapp/src/components/Review.test.tsx
autonomous: true
requirements: [GENAI-03]

must_haves:
  truths:
    - "O dono abre a classificação de pasta a partir da Revisão, sem aba nova"
    - "Uma evidência de origem `llm_pasta` é distinguível na Revisão de uma evidência de qualquer outra origem"
    - "Nada muda na Revisão para quem nunca ligou o recurso"
  artifacts:
    - path: "webapp/src/components/Review.tsx"
      provides: "Botão de disparo, montagem do modal e pastilha de origem no PorQue"
      contains: "ClassificacaoPasta"
    - path: "webapp/src/components/Review.test.tsx"
      provides: "Testes do disparo e da pastilha de origem"
  key_links:
    - from: "webapp/src/components/Review.tsx"
      to: "webapp/src/components/ClassificacaoPasta.tsx"
      via: "montagem condicional por estado booleano"
      pattern: "classificacaoAberta"
    - from: "webapp/src/components/Review.tsx"
      to: "ev.origem === 'llm_pasta'"
      via: "pastilha condicional no PorQue"
      pattern: "llm_pasta"
---

<objective>
Ligar o assistente à Revisão e fazer a origem `llm_pasta` sobreviver visualmente
até a tela compartilhada onde o dono compara sugestões.

Purpose: o ROADMAP fixa o escopo de UI desta fase como estreito — um ponto de
disparo dentro da Revisão, não uma tela nova. E o critério 3 da fase exige que a
sugestão gerada seja distinguível na Revisão de uma vinda do Advisor de cluster:
isso só fecha quando a pastilha aparece no `PorQue`, não só dentro do modal.

Output: botão de disparo, montagem do modal e a pastilha condicional no
`PorQue`, com testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-PATTERNS.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-06-SUMMARY.md
@docs/DIRECAO_DE_ARTE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar a árvore de render de Review.tsx e montar o modal no ponto certo</name>
  <files>webapp/src/components/Review.tsx</files>
  <read_first>
    - `webapp/src/components/Review.tsx` INTEIRO (500 linhas). Este arquivo NÃO pode ser modificado por suposição: o `07-PATTERNS.md` § "Open Items" item 5 registra explicitamente que o ponto de montagem do modal não foi verificado (a sessão de mapeamento leu o cabeçalho, as linhas da lista e o `PorQue`, mas não o `return` externo). Leitura obrigatória antes de qualquer edição, para confirmar: onde o componente principal começa e termina, onde ficam os `useState` locais existentes (`status`, `abertos`, `porque`, `editando`, `valorEdicao`, `erroEdicao`), e qual elemento é o invólucro de raiz.
    - `webapp/src/components/ModalCaminho.tsx` — como um modal deste projeto é montado por um componente-pai (contrato de props e posição na árvore).
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md` § Entry point — o botão vai à ESQUERDA de "Gerar/atualizar sugestões", com `variante="contorno" tamanho="md"` e a cópia literal "Classificar pastas por IA…".
  </read_first>
  <action>
    Primeiro, confirmar por leitura a estrutura real de `Review.tsx`: identificar
    a linha do `return (` do componente principal, o elemento de raiz e a linha
    do seu fechamento. Registrar no SUMMARY deste plano os números de linha
    confirmados — se divergirem do que este plano assume, vale o arquivo, não o
    plano.

    Acrescentar `const [classificacaoAberta, setClassificacaoAberta] =
    useState(false)` junto dos demais `useState` locais já existentes no topo do
    componente.

    Inserir o botão de disparo imediatamente ANTES do `<Botao>` de
    "Gerar/atualizar sugestões" na barra de cabeçalho, com a cópia e as props
    literais do UI-SPEC: `<Botao variante="contorno" tamanho="md" onClick={() =>
    setClassificacaoAberta(true)}>Classificar pastas por IA…</Botao>` (reticências
    em caractere único `…`, como o contrato escreve).

    Montar o modal condicionalmente dentro do elemento de raiz do componente,
    como último filho, antes do fechamento: `{classificacaoAberta &&
    <ClassificacaoPasta onFechar={() => setClassificacaoAberta(false)} />}`.
    Importar o componente no topo, junto dos demais imports de `./`.

    Não acrescentar entrada em `ABAS` no `App.tsx` — o ROADMAP e o UI-SPEC
    proíbem aba nova nesta fase. Nenhum arquivo além de `Review.tsx` é tocado
    nesta task.
  </action>
  <acceptance_criteria>
    - `grep -n "Classificar pastas por IA…" webapp/src/components/Review.tsx` retorna exatamente 1 linha, e ela vem ANTES da linha de `"Gerar/atualizar sugestões"` no arquivo.
    - `grep -n "classificacaoAberta" webapp/src/components/Review.tsx` retorna 3 linhas (declaração, `onClick`, montagem condicional).
    - `grep -c "ClassificacaoPasta" webapp/src/components/App.tsx webapp/src/App.tsx 2>/dev/null` retorna `0` — o modal não foi montado como aba.
    - O SUMMARY do plano registra os números de linha reais do `return` e do fechamento do elemento de raiz de `Review.tsx`, confirmados por leitura.
    - `cd webapp && npx tsc -b --noEmit` limpo.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit && npx vitest run Review</automated>
  </verify>
  <done>Botão no lugar exato do contrato, modal montado no ponto verificado da árvore real, sem aba nova.</done>
</task>

<task type="auto">
  <name>Task 2: Pastilha de origem `llm_pasta` no PorQue</name>
  <files>webapp/src/components/Review.tsx</files>
  <read_first>
    - `webapp/src/components/Review.tsx` linhas 463-495 — o componente `PorQue`, que hoje renderiza `ev.campo`, `ev.valor` e `ev.justificativa` e NÃO renderiza `ev.origem` em lugar nenhum.
    - `webapp/src/api.ts` — o tipo da evidência devolvida em `Suggestion.evidencias`; confirmar se `origem` já vem no payload da API. Se não vier, o backend já a persiste (`Evidence.origem`) e falta apenas expô-la na serialização de `fotoorganizer/server/app.py`; nesse caso, registrar o achado no SUMMARY e acrescentar `origem` ao tipo do frontend E ao dicionário serializado, que é a mudança mínima — sem inventar campo novo.
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md` § Extension to PorQue — o trecho JSX exato e as classes exatas.
  </read_first>
  <action>
    Confirmar se `ev.origem` chega ao frontend. Se não chegar, expor `origem` na
    serialização de evidência do backend e no tipo correspondente de `api.ts`
    — mudança aditiva e mínima; registrar no SUMMARY que ela foi necessária.

    Em `PorQue`, imediatamente depois de `<Confianca nivel={ev.nivel}
    rotulo={false} />`, acrescentar a pastilha condicional exatamente como o
    UI-SPEC escreve: renderizada só quando `ev.origem === "llm_pasta"`, com as
    classes `rounded-full border border-herdado/40 bg-herdado/10 px-1.5 py-0.5
    text-[11px] text-herdado` e o texto `IA · pasta`.

    Não alterar o layout das evidências de outras origens: uma evidência de
    origem `exif`/`gps`/`pasta`/`llm` continua renderizando exatamente como
    antes. Comentário curto explicando por que a paleta é `herdado` e não a
    neutra: a pastilha neutra já significa "colisão de fonte" nesta tela, e usar
    a mesma faria uma proposta de IA parecer outra coisa.
  </action>
  <acceptance_criteria>
    - `grep -n "llm_pasta" webapp/src/components/Review.tsx` retorna 1 linha, dentro de `PorQue`.
    - `grep -n "border-herdado/40 bg-herdado/10" webapp/src/components/Review.tsx` encontra as classes literais do contrato.
    - `git diff webapp/src/components/Review.tsx` no bloco do `PorQue` mostra apenas linhas adicionadas — nenhuma linha existente de renderização de evidência foi alterada.
    - `cd webapp && npx tsc -b --noEmit` limpo.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run Review</automated>
  </verify>
  <done>Evidência de LLM de pasta é distinguível na Revisão; as demais origens renderizam idênticas ao que renderizavam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes do disparo e da pastilha</name>
  <files>webapp/src/components/Review.test.tsx</files>
  <read_first>
    - `webapp/src/components/Review.test.tsx` INTEIRO — montagem existente, mocks de `api` e convenções de consulta de texto; os testes novos entram nesse mesmo formato e nenhum teste existente pode ser alterado.
    - `webapp/src/components/Review.tsx` (Tasks 1 e 2).
  </read_first>
  <behavior>
    - `test: botão de classificação abre o modal` — clicar em "Classificar pastas por IA…" renderiza o modal (assertar por um texto que só existe nele).
    - `test: modal fecha e a Revisão continua` — acionar `onFechar` remove o modal e a lista da Revisão continua na tela.
    - `test: evidência llm_pasta ganha a pastilha` — abrir o `PorQue` de uma sugestão cuja evidência tem `origem: "llm_pasta"` mostra "IA · pasta".
    - `test: evidência de outra origem não ganha pastilha` — a mesma tela com `origem: "gps"` não mostra "IA · pasta".
  </behavior>
  <action>
    Acrescentar os quatro casos do bloco `<behavior>` a
    `webapp/src/components/Review.test.tsx`, sem tocar nos testes existentes. O
    mock de `ClassificacaoPasta` pode ser um componente trivial para isolar este
    arquivo do assistente inteiro — mas os dois testes de pastilha devem exercitar
    o `PorQue` REAL, porque é ele que este plano alterou.
  </action>
  <acceptance_criteria>
    - `cd webapp && npx vitest run Review` passa, com 4 testes a mais do que antes deste plano.
    - Nenhum teste pré-existente de `Review.test.tsx` foi editado: `git diff webapp/src/components/Review.test.tsx` mostra apenas adições.
    - `cd webapp && npm run build` verde.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run Review && npm run build</automated>
  </verify>
  <done>Quatro testes novos verdes, nenhum teste existente alterado, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidência derivada de LLM → leitura do dono na Revisão | É a fronteira de confiança perceptual: se a origem não aparece, o dono não consegue calibrar quanto acreditar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Spoofing | Evidência de LLM indistinguível de determinística na tela compartilhada | mitigate | Pastilha condicional por `ev.origem === "llm_pasta"` no `PorQue`, em paleta própria; testes positivo e negativo. |
| T-07-08-02 | Tampering | Regressão em `Review.tsx`, tela central do app | mitigate | Mudança estritamente aditiva (verificada por `git diff`), ponto de montagem confirmado por leitura do arquivo inteiro antes da edição, nenhum teste existente alterado. |
| T-07-08-03 | Information Disclosure | Botão disparando chamada externa por engano | mitigate | O botão só abre o modal; toda saída de dado continua atrás do gate de dois flags do 07-04. O passo 0 do modal é o consentimento. |
| T-07-08-SC | Tampering | instalação de pacote npm | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `cd webapp && npx vitest run Review` verde, com 4 testes a mais.
- `cd webapp && npx tsc -b --noEmit` limpo, `npm run build` verde.
- `git diff webapp/src/components/Review.tsx` só com adições.
</verification>

<success_criteria>
- Ponto de disparo dentro da Revisão, sem aba nova, na posição exata do contrato.
- Ponto de montagem do modal verificado contra o arquivo real, não assumido.
- Origem `llm_pasta` visível na Revisão; demais origens inalteradas.
</success_criteria>

<output>
Create `.planning/phases/07-classifica-o-de-pasta-por-genai/07-08-SUMMARY.md` when done
</output>
