---
phase: 07-classifica-o-de-pasta-por-genai
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fotoorganizer/classification/location_advisor.py
  - tests/test_classification_pasta_genai.py
autonomous: true
requirements: [GENAI-02]

must_haves:
  truths:
    - "O corpo enviado ao Claude contém somente nome de pasta e metadado já catalogado — nenhum caminho de imagem, nenhum byte de imagem, nenhuma miniatura"
    - "Uma sessão inteira sai numa única chamada, não uma chamada por pasta"
    - "Rede caída, recusa do modelo, JSON inválido ou 429 devolvem resultado vazio e nunca levantam exceção"
    - "Uma pasta que o modelo devolveu mas que não foi pedida é descartada"
    - "Pasta sem resposta confiável não vira proposta nenhuma"
  artifacts:
    - path: "fotoorganizer/classification/location_advisor.py"
      provides: "ClassificadorDePasta (Protocol), ClassificacaoDePastaNula, ClassificacaoDePastaClaude, PastaPayload"
      contains: "class ClassificacaoDePastaClaude"
      min_lines: 120
    - path: "tests/test_classification_pasta_genai.py"
      provides: "Prova de payload, de modelo/thinking, de never-crash e do filtro anti-alucinação"
  key_links:
    - from: "fotoorganizer/classification/location_advisor.py"
      to: "anthropic client.messages.create"
      via: "chamada única em lote com output_config json_schema"
      pattern: "output_config"
    - from: "fotoorganizer/classification/location_advisor.py"
      to: "filtro de resposta"
      via: "só aceita item cuja pasta estava no pedido"
      pattern: "in pedidos"
---

<objective>
Criar o cliente de classificação de pasta por Claude Sonnet 5: uma chamada em
lote por sessão, saída estruturada, contrato never-crash e payload que
comprovadamente nunca contém imagem.

Purpose: GENAI-02 exige que só nome de pasta e metadado já catalogado saiam da
máquina, com Sonnet 5 (precedente D-059/D-060, que vale a fortiori aqui porque
nome de pasta sozinho é evidência mais esparsa que o cluster medido). D-03 exige
uma chamada por sessão, não uma por pasta.

Output: `fotoorganizer/classification/location_advisor.py` e a suíte
`tests/test_classification_pasta_genai.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-CONTEXT.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-RESEARCH.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-PATTERNS.md
@CLAUDE.md

<interfaces>
Contratos que este plano CRIA. Os planos 07-03 (custo) e 07-04 (endpoints)
consomem estes nomes exatos.

```python
# fotoorganizer/classification/location_advisor.py
MODELO_PADRAO = "claude-sonnet-5"
MAX_TOKENS = 16000

@dataclass(frozen=True, slots=True)
class PastaPayload:
    """O que sai da máquina por pasta. Nada além disto."""
    pasta: str                     # caminho relativo/nome da pasta
    n_fotos: int
    periodo: str | None            # "2024-03-12 a 2024-03-19", já catalogado
    camposA_preencher: tuple[str, ...]   # ("categoria",) | ("cidade","pais") | ambos
    ja_conhecido: dict[str, str]   # campos que JÁ têm valor — o modelo não deve propô-los

@dataclass(frozen=True, slots=True)
class PropostaDoModelo:
    pasta: str
    cidade: str | None
    pais: str | None
    categoria: str | None
    evento: str | None
    justificativa: str

class ClassificadorDePasta(Protocol):
    @property
    def local(self) -> bool: ...
    def classificar(self, pastas: list[PastaPayload]) -> list[PropostaDoModelo]: ...
    def corpo_da_chamada(self, pastas: list[PastaPayload]) -> dict: ...

class ClassificacaoDePastaNula:      # padrão; local=True; devolve []
class ClassificacaoDePastaClaude:    # local=False; __init__(model=MODELO_PADRAO, client=None)
```

`corpo_da_chamada()` devolve o dict de kwargs exato que vai para
`messages.create` (model, max_tokens, thinking, system, output_config,
messages). Existe para que 07-03 possa estimar custo e para que o teste de
privacidade possa inspecionar o payload sem chamar a API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contratos — PastaPayload, PropostaDoModelo, Protocol e implementação nula</name>
  <files>fotoorganizer/classification/location_advisor.py</files>
  <read_first>
    - `fotoorganizer/classification/lexico.py` (196 linhas, inteiro) — docstring de contrato de privacidade no topo, par Protocol + objeto nulo (linhas 110-127), propriedade `local`.
    - `fotoorganizer/classification/advisor.py` linhas 1-60 — o comentário de escolha de modelo (D-047..D-060) que este módulo precisa citar, e o formato de `ClusterInfo`.
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-CONTEXT.md` § D-02, D-03, D-06.
  </read_first>
  <action>
    Criar `fotoorganizer/classification/location_advisor.py` começando pela
    docstring de módulo no molde de `lexico.py`: declarar explicitamente que sai
    da máquina apenas o NOME da pasta e metadados já catalogados (contagem de
    fotos, período), NUNCA a imagem, nunca bytes, nunca miniatura, nunca o
    caminho absoluto do arquivo; declarar que o recurso exige `[privacidade]
    servicos_externos` E o opt-in próprio `classificacao_pasta_genai` (o gate
    fica em 07-04, a docstring já o nomeia); declarar que o padrão é a
    implementação nula.

    Definir `MODELO_PADRAO = "claude-sonnet-5"` com o comentário de precedente:
    citar D-059/D-060 (Haiku afirmava categoria onde Opus recusava em ≥19/31
    discordâncias; Sonnet cai nesse padrão 7 vezes em 104 clusters) e registrar
    que aqui o argumento vale a fortiori — nome de pasta sozinho é entrada mais
    esparsa que o cluster que foi medido, então o risco de afirmar sem base é
    maior, não menor. Definir `MAX_TOKENS = 16000` (mesmo teto de `lexico.py`).

    Definir os dataclasses `PastaPayload` e `PropostaDoModelo` exatamente como
    no bloco `<interfaces>`. `PastaPayload.ja_conhecido` existe por causa de
    D-02: o prompt precisa dizer ao modelo quais campos já têm dono para ele não
    gastar esforço propondo o que nunca seria gravado.

    Definir o `Protocol` `ClassificadorDePasta` com `local`, `classificar` e
    `corpo_da_chamada`, e a classe `ClassificacaoDePastaNula` como o padrão:
    `local` devolve `True`, `classificar` devolve `[]`, `corpo_da_chamada`
    devolve `{}`. Docstring da nula: "Padrão: nenhuma pasta sai da máquina,
    nenhuma opinião."
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "from fotoorganizer.classification.location_advisor import ClassificacaoDePastaNula as N; n=N(); print(n.local, n.classificar([]), n.corpo_da_chamada([]))"` imprime `True [] {}`.
    - `grep -vn '^#' fotoorganizer/classification/location_advisor.py | grep -c "claude-sonnet-5"` ≥ 1 e `grep -c "haiku" fotoorganizer/classification/location_advisor.py` conta apenas a menção no comentário de precedente, nunca em `MODELO_PADRAO`.
    - `grep -n "D-059\|D-060" fotoorganizer/classification/location_advisor.py` retorna ao menos uma linha.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from fotoorganizer.classification.location_advisor import ClassificacaoDePastaNula, ClassificadorDePasta, PastaPayload, PropostaDoModelo, MODELO_PADRAO; assert MODELO_PADRAO=='claude-sonnet-5'; print('ok')"</automated>
  </verify>
  <done>Módulo importável com contratos definidos, implementação nula como padrão e o precedente de modelo citado no código.</done>
</task>

<task type="auto">
  <name>Task 2: ClassificacaoDePastaClaude — chamada única em lote, schema e never-crash</name>
  <files>fotoorganizer/classification/location_advisor.py</files>
  <read_first>
    - `fotoorganizer/classification/lexico.py` linhas 30-196 — `_SYSTEM`, `_SCHEMA`, `LexicoClaude._lote()`: forma exata da chamada em lote, tratamento de `stop_reason == "refusal"`, `JSONDecodeError`, e o filtro `item.get("nome") in pedidos`.
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-RESEARCH.md` § Pattern 1 — o `_SCHEMA` alvo (array de objetos com `pasta`, `cidade`, `pais`, `categoria`, `justificativa`) e o `additionalProperties: false`.
    - `fotoorganizer/classification/location_advisor.py` (Task 1).
  </read_first>
  <action>
    Adicionar `_SYSTEM` e `_SCHEMA` ao módulo. O `_SCHEMA` é um objeto com a
    propriedade `pastas` do tipo array; cada item tem `pasta` (string),
    `cidade`, `pais`, `categoria`, `evento` (cada um `anyOf` string ou null) e
    `justificativa` (string); `required` lista todos os seis; `additionalProperties:
    false` no item e no objeto raiz. `categoria` é restrita por `enum` ao mesmo
    vocabulário que a cascata já reconhece — ler `_CATEGORIAS_PASTA` em
    `fotoorganizer/classification/engine.py` e usar os valores canônicos de lá,
    não inventar uma lista nova.

    O `_SYSTEM` deve instruir, em português, no tom do `_SYSTEM` de
    `advisor.py`/`lexico.py`: (a) o modelo recebe nome de pasta e metadado, e
    nada mais; (b) NUNCA inventar — se o nome não permitir concluir com
    segurança, devolver `null` no campo, é resposta válida e preferida (D-06);
    (c) só propor os campos listados em `campos_a_preencher` de cada item, nunca
    os que aparecem em `ja_conhecido` (D-02); (d) `justificativa` explica em uma
    frase de onde veio a conclusão, porque toda sugestão do app precisa
    responder "por quê?".

    Implementar `ClassificacaoDePastaClaude` com
    `__init__(self, model=MODELO_PADRAO, client=None)` — quando `client` é
    `None`, instanciar `anthropic.Anthropic()` sem argumento, para que a
    credencial venha do ambiente/`ant auth` e nunca do código (mesmo padrão de
    `advisor.py`/`lexico.py`). `local` devolve `False`.

    `corpo_da_chamada(pastas)` monta e devolve o dict de kwargs:
    `model`, `max_tokens=MAX_TOKENS`, `thinking={"type": "disabled"}`,
    `system=_SYSTEM`, `output_config={"format": {"type": "json_schema",
    "schema": _SCHEMA}}` e `messages` com um único item de papel `user` cujo
    `content` é `json.dumps({"pastas": [...]}, ensure_ascii=False)` — cada item
    serializado a partir de `PastaPayload` campo a campo, por allowlist
    explícita, nunca por `asdict()` genérico nem por serialização de um objeto
    de domínio: a allowlist é o controle que garante que um campo novo em
    `MediaFile` jamais vaze por acidente.

    `classificar(pastas)` faz UMA chamada com `self._client.messages.create(
    **self.corpo_da_chamada(pastas))` para a lista inteira — sem laço de lote,
    sem fatiar, sem chamada por pasta (D-03). Todo caminho de falha devolve
    lista vazia e registra log, nunca levanta: `except Exception` na chamada
    (log `warning`), `stop_reason == "refusal"` (log `info`), bloco de texto
    ausente, `json.JSONDecodeError` (log `warning`). Ao interpretar a resposta,
    montar `pedidos = {p.pasta for p in pastas}` e aceitar apenas itens cujo
    `pasta` está em `pedidos` — uma pasta que o modelo inventou é descartada em
    silêncio. Descartar também o item cujos quatro campos de valor
    (`cidade`, `pais`, `categoria`, `evento`) vierem todos `null`: é a "resposta
    ambígua" de D-06 e ela não vira proposta nenhuma. Descartar o campo cuja
    chave está em `ja_conhecido` daquela pasta, mesmo que o modelo tenha
    respondido — D-02 não depende da obediência do modelo.
  </action>
  <acceptance_criteria>
    - `grep -c "for .* in range(0, len" fotoorganizer/classification/location_advisor.py` retorna `0` — não há laço de lote; é uma chamada só (D-03).
    - `grep -n "messages.create" fotoorganizer/classification/location_advisor.py` retorna exatamente 1 linha.
    - `grep -n "asdict\|__dict__" fotoorganizer/classification/location_advisor.py` retorna 0 linhas — serialização por allowlist explícita.
    - `grep -vn '^ *#' fotoorganizer/classification/location_advisor.py | grep -c "raise"` retorna `0` dentro de `classificar` (nenhuma exceção escapa do módulo).
    - Comportamento provado pelos testes do Task 3.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_classification_pasta_genai.py -q --no-header</automated>
  </verify>
  <done>Cliente faz uma única chamada por sessão, com schema estruturado, filtro anti-alucinação e nenhum caminho que levante exceção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes — payload sem imagem, modelo/thinking, never-crash, filtro e D-06</name>
  <files>tests/test_classification_pasta_genai.py</files>
  <read_first>
    - `tests/test_lexico.py` (210 linhas, inteiro) — `_RespostaFalsa`/`_ClienteFalso` (linhas 126-145), o teste de payload `test_manda_so_as_palavras` (148-159) e o `@pytest.mark.parametrize` de never-crash (176-183). Copiar essas formas, não inventar mocks.
    - `fotoorganizer/classification/location_advisor.py` (Tasks 1 e 2).
  </read_first>
  <behavior>
    - `test_payload_nunca_envia_imagem`: o `content` da mensagem enviada, desserializado, é um dict com a única chave `pastas`, e cada item tem exatamente as chaves da allowlist. Assert negativo explícito: nenhuma chave nem valor do corpo serializado contém `caminho`, `thumb`, `miniatura`, `base64`, `image`, `.jpg`, `.cr2` ou `.heic`.
    - `test_modelo_e_thinking`: `corpo_da_chamada` traz `model == "claude-sonnet-5"` e `thinking == {"type": "disabled"}`.
    - `test_uma_chamada_para_muitas_pastas`: 12 `PastaPayload` produzem exatamente 1 entrada em `cliente.chamadas` (D-03).
    - `test_falha_nunca_derruba` (parametrizado): `RuntimeError("sem rede")`, resposta com `stop_reason="refusal"`, texto não-JSON e um erro simulando 429 → todos devolvem `[]` sem levantar.
    - `test_ignora_pasta_que_nao_foi_pedida`: resposta com uma pasta inventada é descartada; a pedida é aceita.
    - `test_resposta_incerta_nao_vira_proposta`: item com cidade/país/categoria/evento todos `null` não gera `PropostaDoModelo` (D-06).
    - `test_nao_propoe_campo_ja_conhecido`: item cuja pasta declarou `ja_conhecido={"categoria": "Viagens"}` tem a `categoria` da resposta descartada mesmo que o modelo a tenha devolvido (D-02).
    - `test_nula_e_o_padrao`: `ClassificacaoDePastaNula().classificar([...])` devolve `[]` e `local is True`.
  </behavior>
  <action>
    Criar `tests/test_classification_pasta_genai.py` com os oito casos do bloco
    `<behavior>`, reusando as classes `_RespostaFalsa`/`_ClienteFalso` no formato
    de `tests/test_lexico.py` (redefinidas localmente, como o precedente faz —
    não extrair helper compartilhado nesta fase). Nenhum teste toca a rede;
    `ClassificacaoDePastaClaude(client=_ClienteFalso(...))` é o único caminho de
    instanciação usado. Nomes e docstrings em português, cada docstring dizendo
    qual regra o teste protege (GENAI-02, D-02, D-03, D-06 ou o contrato
    never-crash).

    O teste de payload é o teste de invariante 4 desta fase: escrever a asserção
    de forma que qualquer campo novo adicionado a `PastaPayload` no futuro quebre
    o teste se não for adicionado à allowlist esperada — comparar o conjunto de
    chaves com um literal, não com um subconjunto.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_classification_pasta_genai.py -q` passa; `grep -c "def test_" tests/test_classification_pasta_genai.py` retorna `8`.
    - `grep -c "anthropic.Anthropic()" tests/test_classification_pasta_genai.py` retorna `0` — nenhum teste instancia cliente real.
    - O teste de payload compara conjunto de chaves com igualdade (`==`), não com `<=` nem `in`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_classification_pasta_genai.py -q --no-header</automated>
  </verify>
  <done>Oito testes verdes provando GENAI-02 (payload e modelo), D-03 (chamada única), D-06 e o contrato never-crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Máquina local → api.anthropic.com | Única fronteira de saída de dado desta fase. Atravessam nome de pasta, contagem de fotos, período e os campos já conhecidos. Nunca imagem, nunca caminho absoluto, nunca miniatura. |
| Resposta do modelo → domínio | Texto gerado por modelo entra como proposta. Não é confiável por construção. |
| Credencial → processo | `ANTHROPIC_API_KEY` / perfil `ant auth` lidos pelo SDK. Nunca por este código. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | `corpo_da_chamada()` vazando campo não previsto (invariante 4, GENAI-02) | mitigate | Serialização por allowlist literal campo a campo, proibido `asdict()`/`__dict__`; `test_payload_nunca_envia_imagem` compara o conjunto de chaves com igualdade, então campo novo quebra o teste. |
| T-07-02-02 | Spoofing | Modelo devolve pasta que não existe no catálogo | mitigate | Filtro `item["pasta"] in pedidos` antes de aceitar qualquer item (ASVS V5, mesmo controle de `lexico.py:189-196`); `test_ignora_pasta_que_nao_foi_pedida`. |
| T-07-02-03 | Tampering | Prompt injection por pasta com nome hostil | mitigate | `output_config` com `additionalProperties: false` limita o que pode voltar; o filtro acima limita a quem pode voltar; D-02 é reaplicado no código sobre a resposta, então obediência do modelo não é pré-requisito de segurança. Risco real baixo: os nomes são do disco do próprio dono. |
| T-07-02-04 | Denial of Service | 429/timeout derrubando a geração de sugestões | mitigate | Todo caminho de falha loga e devolve `[]`; nenhum `raise` escapa de `classificar`. `test_falha_nunca_derruba` parametrizado. |
| T-07-02-05 | Information Disclosure | Credencial em código, log ou repositório | mitigate | `anthropic.Anthropic()` sem argumento; a chave nunca é lida, impressa nem logada por este módulo. Nenhum teste usa credencial real. |
| T-07-02-SC | Tampering | instalação de pacote | accept | Nenhum pacote novo — `anthropic>=0.116` já é dependência instalada e em uso em `advisor.py`/`lexico.py`. `07-RESEARCH.md` § Package Legitimacy Audit registra que o gate não se aplica. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_classification_pasta_genai.py -q` verde (8 testes).
- `.venv/bin/python -m pytest -q --no-header` sem regressão.
- `grep -n "messages.create" fotoorganizer/classification/location_advisor.py` = 1 ocorrência.
</verification>

<success_criteria>
- Uma sessão de N pastas produz exatamente uma chamada à API.
- O payload é provado por teste como contendo só nome de pasta e metadado catalogado.
- Sonnet 5 é o modelo, com `thinking` desabilitado.
- Nenhuma falha de API escapa do módulo.
</success_criteria>

<output>
Create `.planning/phases/07-classifica-o-de-pasta-por-genai/07-02-SUMMARY.md` when done
</output>
