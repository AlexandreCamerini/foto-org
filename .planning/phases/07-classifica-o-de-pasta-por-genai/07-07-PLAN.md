---
phase: 07-classifica-o-de-pasta-por-genai
plan: 07
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - webapp/src/components/ClassificacaoPasta.tsx
  - webapp/src/components/ClassificacaoPasta.test.tsx
autonomous: true
requirements: [GENAI-03]

must_haves:
  truths:
    - "O dono vê, por pasta, o valor de antes e o proposto, e escolhe quais aceitar"
    - "Uma proposta de LLM é visualmente distinguível de uma sugestão de outra fonte, pela etiqueta de origem"
    - "Pasta sem resposta confiável aparece como uma linha de resumo explicada, nunca como linha em branco"
    - "Fechar sem aprovar não grava nada como evidência"
    - "O dono consegue responder 'por quê?' de cada proposta sem sair da tela"
  artifacts:
    - path: "webapp/src/components/ClassificacaoPasta.tsx"
      provides: "Passos 4 (revisão antes/depois) e 5 (concluído)"
      contains: "IA · pasta"
    - path: "webapp/src/components/ClassificacaoPasta.test.tsx"
      provides: "Testes de revisão, resumo de D-06 e aprovação"
  key_links:
    - from: "webapp/src/components/ClassificacaoPasta.tsx"
      to: "api.aprovarGenaiPasta"
      via: "botão Aprovar N selecionadas"
      pattern: "aprovarGenaiPasta"
    - from: "webapp/src/components/ClassificacaoPasta.tsx"
      to: "webapp/src/components/Confianca.tsx"
      via: "badge de confiança reusado sem alteração"
      pattern: "Confianca"
---

<objective>
Fechar o assistente com a tela de revisão antes/depois por pasta e o resumo de
conclusão.

Purpose: GENAI-03 exige que o resultado seja distinguível de uma sugestão do
Advisor de cluster, e D-06 exige que a pasta sem resposta confiável apareça como
ausência nomeada e explicada, nunca como campo em branco que pareça bug. As duas
coisas acontecem nesta tela.

Output: passos 4 e 5 de `ClassificacaoPasta.tsx` e os testes correspondentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-PATTERNS.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-06-SUMMARY.md
@docs/DIRECAO_DE_ARTE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Passo 4 — revisão antes/depois, etiqueta de origem e resumo de D-06</name>
  <files>webapp/src/components/ClassificacaoPasta.tsx</files>
  <read_first>
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md` § Step 4 INTEIRO — as duas linhas por linha de proposta, as duas pastilhas, o resumo recolhido de D-06, o botão `ⓘ` com `title`/`aria-expanded`, e a cópia literal de cada elemento.
    - `webapp/src/components/ClassificacaoPasta.tsx` (estado do assistente e casca, do plano 07-06).
    - `webapp/src/components/Review.tsx` linhas 300-345 — a pastilha `rotuloDeFonte` (mesma casca `rounded-full ... px-1.5 py-0.5 text-[11px]`, paleta neutra) que a nova pastilha precisa NÃO parecer, e o padrão de botão "por quê?" com `title` e `aria-expanded`.
    - `webapp/src/components/Confianca.tsx` (88 linhas, inteiro) — reusar verbatim, sem fork.
    - `webapp/src/components/EscritaExif.tsx` linhas 470-500 — linha com caixa de marcação e conteúdo de duas alturas.
  </read_first>
  <behavior>
    - Cada proposta vira uma linha com caixa marcada por padrão, a linha "antes: {campo} — (vazio)" e a linha "depois: {campo} — {valor}".
    - A linha "depois" carrega a pastilha de origem com o texto "IA · pasta" na paleta `herdado` (`border-herdado/40 bg-herdado/10 text-herdado`), seguida do componente `Confianca`.
    - O botão `ⓘ` expande a justificativa embaixo da linha, com `title="Por que esta proposta para {pasta}?"` e `aria-expanded` correto.
    - Havendo `pastas_sem_resposta`, aparece UMA linha de resumo com o glifo `⚠` em `text-atencao` e a cópia literal do contrato; "Ver quais »" expande a lista de nomes em `text-texto-3`, sem caixa de marcação e sem linha "depois".
    - Não havendo `pastas_sem_resposta`, a linha de resumo não é renderizada.
    - O rodapé mostra "{marcadas} de {total} propostas selecionadas", com "Aprovar {N} selecionadas" (`solido`) e "Fechar sem aprovar" (`fantasma`).
    - "Aprovar" chama `api.aprovarGenaiPasta` com apenas as pastas marcadas e avança ao passo 5.
    - "Fechar sem aprovar" fecha o modal sem chamar `aprovarGenaiPasta`.
  </behavior>
  <action>
    Implementar o passo `"revisao"` dentro de `ClassificacaoPasta.tsx`, com um
    `Set<string>` de pastas marcadas semeado com TODAS as propostas ao entrar no
    passo (aprovar-por-padrão, mesma fraseologia opt-out do passo 1). Agrupar as
    propostas recebidas por `pasta`, para que uma pasta com proposta de categoria
    E de cidade apareça como UMA linha com duas linhas "depois", não duas linhas
    de pasta repetida — o dono decide por pasta, que é a unidade de D-01/D-03.

    A pastilha de origem usa a casca da pastilha existente de `Review.tsx` com a
    paleta `herdado`, deliberadamente diferente da neutra: o UI-SPEC é explícito
    em que o objetivo é uma proposta de IA nunca ser confundida com a pastilha de
    colisão de fonte nem com uma origem determinística. Registrar isso em
    comentário curto no código.

    Estado local de expansão da justificativa e da lista de "sem resposta" por
    `useState`, no molde do `porque === s.media_id` de `Review.tsx`.

    Toda string visível vem do § Copywriting Contract literalmente.
  </action>
  <acceptance_criteria>
    - `grep -n "IA · pasta" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal (com o meio-ponto, não hífen).
    - `grep -n "border-herdado/40 bg-herdado/10 text-herdado" webapp/src/components/ClassificacaoPasta.tsx` encontra as classes exatas do contrato.
    - `grep -n "sem resposta confiável — Claude não teve segurança pra classificar" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal.
    - `grep -c "aria-expanded" webapp/src/components/ClassificacaoPasta.tsx` ≥ 1.
    - `cd webapp && npx tsc -b --noEmit` limpo.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit && npx vitest run ClassificacaoPasta</automated>
  </verify>
  <done>Revisão antes/depois por pasta, com pastilha de origem distinguível, justificativa expansível e resumo nomeado das pastas sem resposta.</done>
</task>

<task type="auto">
  <name>Task 2: Passo 5 — concluído, e o fim do ciclo do modal</name>
  <files>webapp/src/components/ClassificacaoPasta.tsx</files>
  <read_first>
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md` § Step 5 — o `✓` em `text-ok`, a cópia que evita implicar aparecimento instantâneo na grade atrás do modal.
    - `07-03-SUMMARY.md` — se a decisão da Task 1 daquele plano foi `c-hibrida`, este passo também mostra o custo REAL da chamada (`custo_real` da resposta de `rodar`); caso contrário, não mostra.
    - `webapp/src/components/ClassificacaoPasta.tsx` (Task 1).
  </read_first>
  <behavior>
    - Passo 5 mostra "✓ {N} pastas classificadas" com o glifo em `text-ok` e a frase de expectativa ("As sugestões aparecem em Revisão na próxima geração de sugestões.").
    - Só o botão "Fechar", que chama `onFechar`.
    - Se a decisão de 07-03 foi `c-hibrida`, uma linha adicional mostra o custo real da entrada, rotulada como real e não como estimativa.
    - Reabrir o modal depois de concluído começa no passo 1 com consulta fresca de candidatas (ou no passo 0 se o dono desligou pelo link "Desligar").
    - Se houver propostas pendentes de uma sessão anterior não aprovada (`api.propostasGenaiPasta` devolve linhas), o modal abre direto no passo 4 com elas, em vez de perder uma chamada já paga.
  </behavior>
  <action>
    Implementar o passo `"concluido"` e a lógica de entrada do modal. Na
    montagem, depois de resolver a configuração: se o gate está desligado, passo
    0; senão, consultar `api.propostasGenaiPasta()` — havendo propostas
    pendentes, entrar no passo 4 com elas (o dono já pagou por aquela resposta e
    o UI-SPEC não previu esse caminho porque a persistência de proposta foi
    decidida depois; registrar em comentário que a recuperação existe para não
    cobrar duas vezes pelo mesmo resultado, D-07); não havendo, passo 1.

    Remover o marcador provisório dos passos `revisao`/`concluido` deixado pelo
    plano 07-06.
  </action>
  <acceptance_criteria>
    - `grep -n "As sugestões aparecem em Revisão na próxima geração de sugestões" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal.
    - `grep -n "propostasGenaiPasta" webapp/src/components/ClassificacaoPasta.tsx` mostra a consulta de recuperação na entrada do modal.
    - Nenhum marcador provisório restante: `grep -ci "TODO\|placeholder\|em breve" webapp/src/components/ClassificacaoPasta.tsx` retorna `0`.
    - `cd webapp && npm run build` verde.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Ciclo do modal fechado, com recuperação de sessão paga não aprovada e nenhum trecho provisório.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes vitest dos passos 4 e 5</name>
  <files>webapp/src/components/ClassificacaoPasta.test.tsx</files>
  <read_first>
    - `webapp/src/components/ClassificacaoPasta.test.tsx` (os 9 testes do plano 07-06 — mesmo arquivo, mesma montagem de `QueryClientProvider` e mesmo mock de `api`).
    - `webapp/src/components/ClassificacaoPasta.tsx` (Tasks 1 e 2).
  </read_first>
  <behavior>
    - `test: propostas nascem todas marcadas` — rodapé diz "N de N propostas selecionadas".
    - `test: pastilha de origem aparece na linha depois` — o texto "IA · pasta" está presente em cada proposta.
    - `test: duas propostas da mesma pasta viram uma linha` — uma pasta com categoria e cidade aparece uma vez, com duas linhas "depois".
    - `test: resumo de sem-resposta é uma linha só` — duas pastas sem resposta produzem UMA linha de resumo, e nenhuma linha de proposta em branco.
    - `test: sem pastas sem resposta, sem linha de resumo`.
    - `test: Ver quais expande os nomes` — clicar mostra os nomes das pastas sem resposta.
    - `test: aprovar envia só as marcadas` — desmarcar uma e aprovar chama `aprovarGenaiPasta` com o restante.
    - `test: fechar sem aprovar não chama aprovar` — o mock de `aprovarGenaiPasta` não é chamado e `onFechar` é.
    - `test: abre no passo 4 quando há proposta pendente` — com `propostasGenaiPasta` devolvendo linhas, o modal monta já na revisão.
    - `test: passo concluído mostra a frase de expectativa`.
  </behavior>
  <action>
    Acrescentar ao arquivo de teste os dez casos do bloco `<behavior>`,
    mantendo o estilo do plano 07-06 e comparando com as strings literais do §
    Copywriting Contract onde o contrato define cópia.
  </action>
  <acceptance_criteria>
    - `cd webapp && npx vitest run ClassificacaoPasta` passa com 19 testes no total (9 do 07-06 + 10 destes).
    - `grep -c "it(\|test(" webapp/src/components/ClassificacaoPasta.test.tsx` retorna `19`.
    - O teste de resumo assevera contagem de elementos (uma linha para N pastas), não só presença de texto.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run ClassificacaoPasta</automated>
  </verify>
  <done>Dezenove testes verdes cobrindo o assistente inteiro, do portão de opt-in à conclusão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proposta do modelo → decisão do dono | Esta tela é o ponto onde o palpite da máquina precisa parecer palpite. A pastilha de origem e o badge de confiança são o controle. |
| Aprovação → evidência | Só o que o dono marcou atravessa; o resto é descartado no servidor sem virar evidência. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Spoofing | Proposta de IA passando por sugestão determinística na revisão | mitigate | Pastilha "IA · pasta" em paleta `herdado`, deliberadamente distinta da pastilha neutra de colisão de fonte; badge `Confianca` com o nível derivado do score próprio. Teste `pastilha de origem aparece`. |
| T-07-07-02 | Repudiation | Pasta sem resposta sumindo em silêncio ou aparecendo como bug | mitigate | Linha de resumo obrigatória com contagem, motivo e lista expansível (D-06). Testes de presença e de ausência. |
| T-07-07-03 | Denial of Service (perda de dado) | Chamada paga perdida por refresh do navegador | mitigate | Modal recupera propostas pendentes do servidor na montagem; o servidor nunca apaga a linha (07-01). |
| T-07-07-04 | Tampering | Aprovação enviando pasta que o dono desmarcou | mitigate | O corpo enviado é derivado do `Set` de marcados, nunca da lista completa; teste `aprovar envia só as marcadas`. |
| T-07-07-SC | Tampering | instalação de pacote npm | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `cd webapp && npx vitest run ClassificacaoPasta` verde (19 testes).
- `cd webapp && npx tsc -b --noEmit` limpo e `npm run build` verde.
</verification>

<success_criteria>
- Revisão antes/depois por pasta, opt-out, com origem e confiança visíveis.
- Ausência de resposta aparece nomeada e explicada, uma vez.
- Fechar sem aprovar não grava nada; sessão paga não é perdida por refresh.
</success_criteria>

<output>
Create `.planning/phases/07-classifica-o-de-pasta-por-genai/07-07-SUMMARY.md` when done
</output>
