---
phase: 07-classifica-o-de-pasta-por-genai
plan: 09
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-05"]
files_modified:
  - scripts/medir_score_llm_pasta.py
  - fotoorganizer/classification/confidence.py
  - docs/DECISOES.md
  - docs/CONFIANCA.md
autonomous: false
requirements: [GENAI-03]

must_haves:
  truths:
    - "O score de `llm_pasta` deixa de ser provisório e passa a ter uma medição contra o acervo real por trás"
    - "A medição roda no terminal do dono, com a chave dele — esta sessão nunca manuseia a credencial"
    - "A decisão do número fica registrada em docs/DECISOES.md com o resultado que a fundamenta"
    - "docs/CONFIANCA.md descreve a origem nova e por que ela fica onde ficou"
  artifacts:
    - path: "scripts/medir_score_llm_pasta.py"
      provides: "Medição de acerto/abstenção/erro do classificador de pasta contra verdade determinística do catálogo"
      contains: "def main"
    - path: "fotoorganizer/classification/confidence.py"
      provides: "SCORES_REFERENCIA['llm_pasta'] medido, sem a marca PROVISÓRIO"
    - path: "docs/DECISOES.md"
      provides: "Decisão numerada com números da medição, alternativas e como reverter"
    - path: "docs/CONFIANCA.md"
      provides: "Origem llm_pasta documentada na tabela de referência"
  key_links:
    - from: "scripts/medir_score_llm_pasta.py"
      to: "fotoorganizer/classification/location_advisor.py"
      via: "usa o MESMO payload e o MESMO schema da produção"
      pattern: "location_advisor"
    - from: "docs/DECISOES.md"
      to: "SCORES_REFERENCIA['llm_pasta']"
      via: "decisão registra o número que o código carrega"
      pattern: "llm_pasta"
---

<objective>
Substituir o valor provisório de `SCORES_REFERENCIA["llm_pasta"]` por um número
medido contra o acervo real, no mesmo método de D-074 e D-059/D-060.

Purpose: `07-RESEARCH.md` (Pitfall 2, Open Question 1), `07-PATTERNS.md` (Open
Items 1) e o `07-UI-SPEC.md` (Open Items 1) convergem no mesmo ponto: este
projeto mede scores antes de travá-los, e nenhuma medição existe para esta
origem. Um número inventado aqui vira verdade de base para o elo mais fraco e
para o índice de saúde da Fase 10 — que é exatamente a classe de bug que já
vazou uma vez (D-071).

Output: script de medição, decisão registrada e a constante travada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-RESEARCH.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-02-SUMMARY.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-05-SUMMARY.md
@docs/CONFIANCA.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de medição contra verdade determinística do catálogo</name>
  <files>scripts/medir_score_llm_pasta.py</files>
  <read_first>
    - `scripts/medir_qualidade_advisor.py` INTEIRO — o precedente do método (D-047/D-048/D-049/D-059/D-060): como monta a amostra, como compara dois lados, o formato do relatório e a separação entre "modelo se compromete" e "modelo recusa".
    - `scripts/calibrar_raio_incerteza.py` — o outro precedente de calibração contra acervo real (D-074), para o formato de saída e o cuidado com cobertura.
    - `fotoorganizer/classification/location_advisor.py` (07-02) — `PastaPayload`, `corpo_da_chamada`, `classificar`. A medição precisa usar o MESMO payload e o MESMO schema da produção; um schema paralelo mediria outra coisa.
    - `fotoorganizer/classification/candidatas_de_pasta.py` (07-03) — como a pasta e seu metadado são agregados.
  </read_first>
  <action>
    Criar `scripts/medir_score_llm_pasta.py`, no estilo dos demais `scripts/medir_*`
    (executável direto pelo `.venv/bin/python`, argparse, sem dependência nova).

    Desenho da medição — a verdade de referência é o próprio catálogo: montar a
    amostra a partir das pastas onde a cascata DETERMINÍSTICA já resolveu
    categoria e/ou cidade/país (origem `pasta`, `gps`, `geocoding_offline` ou
    `exif`), porque essas são as pastas cuja resposta certa se conhece sem
    perguntar a ninguém. Enviar ao modelo o mesmo `PastaPayload` que a produção
    enviaria, com o campo em medição marcado como ausente — o modelo NÃO pode
    ver a resposta, nem no `ja_conhecido`, nem no metadado. Comparar a proposta
    com a verdade e classificar cada item em três baldes, com os mesmos nomes do
    precedente de advisor: acertou, recusou (devolveu `null` — comportamento
    desejado quando não dá para saber) e errou (afirmou valor diferente da
    verdade).

    Normalizar antes de comparar cidade/país com acento e caixa (D-066/D-067 já
    definiram a normalização; reusar a função existente, não escrever outra).

    Relatório final impresso: total da amostra, contagem e percentual dos três
    baldes por campo (categoria e cidade/país separados — são afirmações de
    natureza diferente e misturá-las repetiria o erro que `docs/CONFIANCA.md`
    proíbe), e uma amostra literal de até 10 erros com pasta, verdade e proposta,
    para o dono conseguir julgar se o erro é do modelo ou da verdade de
    referência.

    O script NÃO grava nada no banco, NÃO altera `confidence.py` e NÃO lê
    credencial de lugar nenhum — o cliente vem de `anthropic.Anthropic()`, que
    resolve a chave do ambiente do dono. Aceitar `--limite` para controlar
    quantas pastas entram (e portanto quanto custa) e `--dry-run` que monta a
    amostra e imprime o custo estimado sem chamar a API. Imprimir o custo
    estimado antes de chamar, sempre.

    Cabeçalho do script com o aviso de classe C: quem roda é o dono, no terminal
    dele, com a própria chave; a sessão de desenvolvimento não manuseia
    credencial (mesmo protocolo de D-048/D-049/D-059).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python scripts/medir_score_llm_pasta.py --help` imprime a ajuda sem erro.
    - `.venv/bin/python scripts/medir_score_llm_pasta.py --dry-run --limite 5` monta a amostra, imprime custo estimado e NÃO chama a API (verificável porque roda sem `ANTHROPIC_API_KEY` definida).
    - `grep -c "ANTHROPIC_API_KEY\|api_key" scripts/medir_score_llm_pasta.py` retorna `0`.
    - `grep -n "location_advisor" scripts/medir_score_llm_pasta.py` confirma que o payload e o schema vêm da produção.
    - `grep -n "categoria" scripts/medir_score_llm_pasta.py` e a saída do `--dry-run` mostram os dois campos relatados separadamente.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/medir_score_llm_pasta.py --help && .venv/bin/python scripts/medir_score_llm_pasta.py --dry-run --limite 5</automated>
  </verify>
  <done>Script roda em seco, monta amostra com verdade determinística, imprime custo antes de gastar e não toca credencial.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rodar a medição e decidir o score</name>
  <what-built>
    `scripts/medir_score_llm_pasta.py` mede o classificador de pasta contra a
    parte do catálogo cuja resposta já se conhece por caminho determinístico,
    usando o payload e o schema idênticos aos de produção. Ele reporta, por campo
    (categoria e cidade/país, separados), quantas vezes o modelo acertou, recusou
    e errou — e imprime o custo estimado antes de gastar qualquer coisa.

    A constante `SCORES_REFERENCIA["llm_pasta"]` está hoje em `0.55` marcada como
    `PROVISÓRIO` pelo plano 07-05, com ponteiro para este plano. Ela não é um
    número calibrado até esta medição acontecer.
  </what-built>
  <how-to-verify>
    Passo a passo, no seu terminal, com a sua chave (esta sessão não manuseia
    credencial — mesmo protocolo de D-048/D-049/D-059):

    1. Rodar em seco primeiro, para ver o tamanho e o custo da amostra:
       `.venv/bin/python scripts/medir_score_llm_pasta.py --dry-run --limite 60`
       Conferir se a amostra faz sentido: as pastas listadas devem ter resposta
       determinística conhecida, e nenhuma delas deve ser miniatura/referência.

    2. Rodar de verdade, com a sua chave:
       `ANTHROPIC_API_KEY=... .venv/bin/python scripts/medir_score_llm_pasta.py --limite 60`

    3. Ler o relatório e responder três perguntas:
       - Qual a taxa de ACERTO por campo (categoria, cidade/país)?
       - Qual a taxa de ERRO (afirmou algo diferente da verdade) por campo? Esta
         é a que importa mais: é o padrão "afirma sem base" que D-049 mediu e que
         motivou trocar de modelo.
       - Olhando a amostra literal de erros: eles são erro do modelo, ou casos em
         que a "verdade" do catálogo é que estava errada?

    4. Decidir o número. Balizas do que já está travado na tabela hoje:
       `pasta` = 0,60 (parse determinístico de segmento explícito),
       `lexico` = 0,58, `llm` = 0,55 (advisor de cluster),
       `vizinhanca`/`curadoria`/`album_externo` = 0,55, `fs` = 0,40.
       Os limiares de nível são 0,8 (alta) e 0,5 (média) — abaixo de 0,5 a
       sugestão nasce BAIXA. Um número abaixo de 0,50 significa que toda
       sugestão desta origem entra na revisão marcada como baixa confiança; um
       número igual a 0,55 a iguala ao advisor de cluster mesmo tendo entrada
       mais esparsa.

    5. Cenário aceitável de resposta: um número, ou "a taxa de erro é alta demais,
       o recurso não deve ficar acima de X". Se a medição indicar que o modelo
       erra muito mais do que recusa, isso é um achado que vale registrar mesmo
       que atrase a fase — foi assim que D-049 pegou o Haiku.
  </how-to-verify>
  <read_first>
    - `docs/CONFIANCA.md` — a regra do elo mais fraco e por que scores não se somam.
    - `docs/DECISOES.md` D-059/D-060 (Sonnet sobre Haiku, medido) e D-074 (calibração de GPS contra 40.678 fotos) — o padrão de rigor que esta decisão precisa igualar.
    - `.planning/STATE.md` § Blockers/Concerns — o aviso de que a base de medição desta fase é pequena (~1.400 arquivos, duas fontes) por decisão explícita do dono; o número medido aqui é preliminar nessa escala e a decisão precisa dizer isso.
  </read_first>
  <acceptance_criteria>
    - O relatório da medição real (números dos três baldes, por campo) está colado na resposta ou salvo no repositório.
    - Um valor numérico para `SCORES_REFERENCIA["llm_pasta"]` foi escolhido pelo dono — ou uma decisão explícita de bloquear a fase por taxa de erro alta.
    - A limitação de tamanho de amostra (~1.400 arquivos, duas fontes, ARCH-01 deferido) está declarada junto do número.
  </acceptance_criteria>
  <files>(nenhum — o dono roda a medição no terminal dele; o resultado entra no Task 3)</files>
  <action>Apresentar o procedimento de cinco passos ao dono, aguardar o relatório real da medição e o número escolhido, e não tocar em `confidence.py` antes disso. Se a resposta for "bloquear", registrar o motivo como blocker em `.planning/STATE.md` e parar o plano — a fase não fecha com score não medido.</action>
  <verify>
    <automated>.venv/bin/python scripts/medir_score_llm_pasta.py --dry-run --limite 5</automated>
  </verify>
  <done>Relatório real entregue pelo dono e valor decidido (ou bloqueio registrado com motivo).</done>
  <resume-signal>Responda com o número escolhido e o relatório da medição, ou com "bloquear: {motivo}"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Travar o score medido e registrar a decisão</name>
  <files>fotoorganizer/classification/confidence.py, docs/DECISOES.md, docs/CONFIANCA.md</files>
  <read_first>
    - A resposta da Task 2 (números da medição e valor escolhido).
    - `docs/DECISOES.md` — as duas últimas entradas, para o formato (Fase, Classe, Data, Contexto, Por quê, Resultado, Como reverter, Status) e para pegar o próximo número livre.
    - `docs/CONFIANCA.md` — a tabela de origens e o tom das justificativas existentes.
    - `fotoorganizer/classification/confidence.py` — o comentário `PROVISÓRIO` posto pelo plano 07-05.
  </read_first>
  <action>
    Atualizar `SCORES_REFERENCIA["llm_pasta"]` para o valor decidido na Task 2 e
    substituir o comentário `PROVISÓRIO` por um que cite a decisão nova, os
    números medidos (tamanho da amostra e taxas por campo) e a limitação de
    escala da base. Se o valor decidido for igual ao provisório `0.55`, ainda
    assim trocar o comentário — o que muda é o status epistêmico do número, e
    deixar a marca `PROVISÓRIO` no código depois da medição é dívida silenciosa.

    Acrescentar a entrada nova a `docs/DECISOES.md` (próximo número livre) no
    formato das existentes, registrando: contexto (por que o score não podia ser
    escolhido por analogia), o método (script, amostra, verdade determinística,
    payload idêntico ao de produção), o resultado numérico, as alternativas
    consideradas com o motivo de cada uma ter sido descartada, a limitação de
    escala da base, e como reverter.

    Acrescentar a origem `llm_pasta` à tabela de referência de
    `docs/CONFIANCA.md`, com a mesma prosa de justificativa que as vizinhas têm
    — o que a origem significa, por que fica onde fica, e o que ela NÃO é (não é
    o advisor de cluster, não é parse determinístico).
  </action>
  <acceptance_criteria>
    - `grep -c "PROVISÓRIO" fotoorganizer/classification/confidence.py` retorna `0`.
    - `grep -n "llm_pasta" docs/CONFIANCA.md` retorna ao menos uma linha na tabela de origens.
    - A nova entrada de `docs/DECISOES.md` cita o número escolhido E o tamanho da amostra medida.
    - `.venv/bin/python -c "from fotoorganizer.classification.confidence import SCORES_REFERENCIA as S; print(S['llm_pasta'])"` imprime o valor decidido.
    - `.venv/bin/python -m pytest -q --no-header` verde — nenhum teste dependia do valor `0.55` como literal (se algum dependia, ele é corrigido para ler da tabela, não para fixar o número novo).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --no-header 2>&1 | tail -3 && grep -c "PROVISÓRIO" fotoorganizer/classification/confidence.py</automated>
  </verify>
  <done>Score medido no código, decisão registrada com os números, `docs/CONFIANCA.md` documentando a origem nova, suíte verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal do dono → api.anthropic.com | A medição gasta dinheiro real e usa a chave do dono. Fronteira operada por ele, classe C. |
| Verdade do catálogo → modelo | O campo em medição precisa estar oculto do payload, senão a medição mede a própria resposta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Repudiation | Score travado sem medição, virando verdade de base para a Fase 10 | mitigate | Este plano inteiro existe para isso: medição obrigatória, decisão registrada, comentário `PROVISÓRIO` removido só depois. Gate de fase (07-10) confere. |
| T-07-09-02 | Tampering | Medição enviesada por vazamento da resposta no payload | mitigate | O campo em medição é removido do payload e do `ja_conhecido`; o script usa `corpo_da_chamada` da produção para não divergir. Amostra de erros impressa para o dono julgar a verdade de referência. |
| T-07-09-03 | Information Disclosure | Credencial manuseada pela sessão de desenvolvimento | mitigate | Protocolo classe C: `grep` de `api_key` = 0 no script; a chave é digitada pelo dono no terminal dele; `--dry-run` permite validar a amostra sem chave. |
| T-07-09-04 | Denial of Service (financeiro) | Medição gastando mais do que o esperado | mitigate | `--limite` obrigatório na prática, custo estimado impresso antes de qualquer chamada, `--dry-run` como passo 1 do procedimento. |
| T-07-09-SC | Tampering | instalação de pacote | accept | Nenhum pacote novo. |
</threat_model>

<verification>
- `.venv/bin/python scripts/medir_score_llm_pasta.py --dry-run --limite 5` roda sem chave.
- Relatório da medição real entregue pelo dono e colado na decisão.
- `grep -c "PROVISÓRIO" fotoorganizer/classification/confidence.py` = 0.
- `.venv/bin/python -m pytest -q --no-header` verde.
</verification>

<success_criteria>
- O número de `llm_pasta` tem medição real por trás, não analogia.
- A decisão está registrada com método, resultado, alternativas e limitação de escala.
- `docs/CONFIANCA.md` documenta a origem nova.
</success_criteria>

<output>
Create `.planning/phases/07-classifica-o-de-pasta-por-genai/07-09-SUMMARY.md` when done
</output>
