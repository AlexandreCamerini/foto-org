---
phase: 07-classifica-o-de-pasta-por-genai
plan: 06
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - webapp/src/api.ts
  - webapp/src/components/ClassificacaoPasta.tsx
  - webapp/src/components/ClassificacaoPasta.test.tsx
autonomous: true
requirements: [GENAI-01]

must_haves:
  truths:
    - "Com o recurso desligado, o dono vê o que exatamente sairia da máquina e precisa marcar a caixa antes de conseguir habilitar"
    - "Com serviços externos desligados, a tela diz isso e não oferece caixa nenhuma — não é possível ligar o mestre por aqui"
    - "O dono vê a lista pré-filtrada de pastas candidatas, todas marcadas, e desmarca o que não quer"
    - "O dono vê o custo estimado com a incerteza da saída declarada, e nada é enviado antes de ele clicar em confirmar"
    - "Durante a chamada não há como fechar o modal, e a tela diz por quê"
  artifacts:
    - path: "webapp/src/api.ts"
      provides: "Tipos e funções cliente de /api/genai-pasta/*"
      contains: "genai-pasta"
    - path: "webapp/src/components/ClassificacaoPasta.tsx"
      provides: "Modal-assistente, passos 0 a 3"
      contains: "ClassificacaoPasta"
    - path: "webapp/src/components/ClassificacaoPasta.test.tsx"
      provides: "Testes de gate, seleção, custo e estado em voo"
  key_links:
    - from: "webapp/src/components/ClassificacaoPasta.tsx"
      to: "webapp/src/api.ts"
      via: "api.candidatasGenaiPasta / api.estimarCustoGenaiPasta / api.rodarGenaiPasta"
      pattern: "api\\.\\w*GenaiPasta"
    - from: "webapp/src/api.ts"
      to: "/api/genai-pasta/*"
      via: "json<T> e post<T> já existentes"
      pattern: "post<|json<"
---

<objective>
Entregar o cliente tipado dos endpoints e os quatro primeiros passos do
assistente: portão de opt-in, lista de candidatas, custo e chamada em voo.

Purpose: GENAI-01 é uma exigência de INTERFACE — "o dono habilita por sessão,
com custo estimado visível antes de confirmar". Sem esta tela o recurso existe
no backend e não existe para o dono.

Output: `api.ts` estendido e `ClassificacaoPasta.tsx` com os passos 0-3, com
testes vitest presos à cópia do contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-PATTERNS.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-03-SUMMARY.md
@.planning/phases/07-classifica-o-de-pasta-por-genai/07-04-SUMMARY.md
@docs/DIRECAO_DE_ARTE.md
@CLAUDE.md

<interfaces>
Contratos que este plano CRIA. O plano 07-07 continua o MESMO componente
(passos 4 e 5) e o 07-08 monta o componente dentro de `Review.tsx`.

```typescript
// webapp/src/api.ts (aditivo, dentro do objeto `api` já existente)
export interface ConfigGenaiPasta {
  servicos_externos: boolean;
  classificacao_pasta_genai: boolean;
}
export interface CandidataGenaiPasta {
  pasta: string;
  n_fotos: number;
  campos_ausentes: ("categoria" | "cidade_pais")[];
  periodo: string | null;
}
export interface CustoGenaiPasta {
  tokens_entrada: number;
  entrada_exata: boolean;
  custo_entrada_usd: number;
  teto_tokens_saida: number;
  teto_custo_saida_usd: number;
  teto_custo_total_usd: number;
  teto_custo_total_brl: number;
  cambio_usd_brl: number;
  cambio_fonte: string;
}
export interface PropostaGenaiPasta {
  pasta: string;
  campo: "categoria" | "cidade" | "pais" | "evento";
  valor_antes: string | null;
  valor_proposto: string;
  justificativa: string;
}
export interface RodadaGenaiPasta {
  propostas: PropostaGenaiPasta[];
  pastas_sem_resposta: string[];
  custo_real: CustoGenaiPasta | null;
}

// funções adicionadas ao objeto `api`
configGenaiPasta, habilitarGenaiPasta, candidatasGenaiPasta,
estimarCustoGenaiPasta, rodarGenaiPasta, propostasGenaiPasta,
aprovarGenaiPasta

// webapp/src/components/ClassificacaoPasta.tsx
export function ClassificacaoPasta({ onFechar }: { onFechar: () => void }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos e funções cliente em api.ts</name>
  <files>webapp/src/api.ts</files>
  <read_first>
    - `webapp/src/api.ts` linhas 340-420 (tipos `PlanoExif`/`ItemPlanoExif`, com o comentário de doc explicando por que união literal e não `string` solto), 460-508 (os wrappers `json<T>` e `post<T>`, que preservam o `detail` do servidor como `Error.message`) e 600-672 (as entradas de `/api/exif/*` dentro do objeto `api`).
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-04-SUMMARY.md` — os formatos de resposta REAIS entregues pelos endpoints; se divergirem do bloco `<interfaces>` acima, os tipos seguem o backend, não este plano.
  </read_first>
  <action>
    Acrescentar a `webapp/src/api.ts` os cinco tipos exportados do bloco
    `<interfaces>`, com o mesmo comentário-de-doc que os tipos de EXIF usam —
    em especial explicando por que `campo` e `campos_ausentes` são uniões
    literais e não `string`: a tela mapeia campo para rótulo/cor num `switch`, e
    `string` solto tiraria a exaustividade que o TypeScript garante.

    Acrescentar as sete funções ao objeto `api` já existente (nunca um segundo
    objeto exportado, nunca `fetch` cru dentro de componente), reusando `json<T>`
    e `post<T>`:
    `configGenaiPasta()` → `GET /api/genai-pasta/config`;
    `habilitarGenaiPasta(habilitado)` → `PUT /api/genai-pasta/config` — se
    `post<T>` só faz POST, acrescentar um wrapper `put<T>` no mesmo formato de
    `post<T>` (preservando `detail`), ao lado dele, em vez de embutir `method`
    em chamador;
    `candidatasGenaiPasta()`, `estimarCustoGenaiPasta(pastas)`,
    `rodarGenaiPasta(pastas)`, `propostasGenaiPasta()`,
    `aprovarGenaiPasta(pastas)`.
  </action>
  <acceptance_criteria>
    - `cd webapp && npx tsc -b --noEmit` sem erro.
    - `grep -c "genai-pasta" webapp/src/api.ts` retorna 7.
    - `grep -c "export const api\|export default" webapp/src/api.ts` continua igual ao de antes (nenhum segundo objeto cliente).
    - Nenhuma das funções novas usa `fetch(` direto: `grep -n "fetch(" webapp/src/api.ts` só aparece dentro de `json`/`post`/`put`.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit</automated>
  </verify>
  <done>Sete funções tipadas e cinco tipos exportados, compilando, sem cliente HTTP paralelo.</done>
</task>

<task type="auto">
  <name>Task 2: ClassificacaoPasta.tsx — passos 0 (opt-in), 1 (candidatas), 2 (custo) e 3 (rodando)</name>
  <files>webapp/src/components/ClassificacaoPasta.tsx</files>
  <read_first>
    - `.planning/phases/07-classifica-o-de-pasta-por-genai/07-UI-SPEC.md` INTEIRO — este arquivo é o contrato; toda string visível vem do § Copywriting Contract literalmente, e as classes Tailwind do § Layout & Component Inventory. Atenção: se o plano 07-03 Task 1 tiver decidido `b-local` ou `c-hibrida`, as linhas de cópia do passo 2 já foram atualizadas lá — usar a versão vigente do arquivo, não a citada de memória.
    - `webapp/src/components/ModalCaminho.tsx` (55 linhas, inteiro) — casca do modal (`fixed inset-0 z-50 ... bg-black/95`) e o contrato de props `onConfirmar`/`onCancelar`.
    - `webapp/src/components/EscritaExif.tsx` linhas 255-410 — `useMutation` com `onError: (e: Error) => setErro(e.message)`, o `Set` de marcados semeado por `useEffect`, o botão desabilitado com `title` explicando o motivo, e `{erro && <span className="text-erro">{erro}</span>}`.
    - `webapp/src/ui/Botao.tsx` — variantes `solido`/`contorno`/`fantasma` e tamanhos; todo botão desta tela usa este primitivo, nunca `<button>` cru.
    - `webapp/src/components/Review.tsx` linha ~497 — o helper `pastaCurta`, a ser reusado para truncar caminho (não reimplementar).
  </read_first>
  <behavior>
    - Passo 0 aparece quando `classificacao_pasta_genai` é falso. Com `servicos_externos` falso, mostra a mensagem de mestre desligado e NENHUMA caixa de marcação.
    - Com `servicos_externos` verdadeiro, mostra a caixa "Habilitar classificação de pasta por IA"; "Habilitar e continuar" fica desabilitado enquanto a caixa não estiver marcada.
    - Habilitar chama `api.habilitarGenaiPasta(true)` e avança para o passo 1 na mesma sessão do modal, sem recarregar.
    - Passo 1 lista as candidatas com TODAS marcadas por padrão; o rodapé mostra "{marcadas} de {total} pastas selecionadas"; "Avançar" fica desabilitado com `title="Selecione ao menos uma pasta"` quando zero.
    - Cada linha do passo 1 mostra a etiqueta de campo ausente correta: "categoria", "cidade/país" ou "categoria · cidade/país".
    - Sem candidata alguma, o passo 1 mostra o estado vazio do contrato e só o botão "Fechar".
    - O link "Desligar" existe no cabeçalho do passo 1, chama `api.habilitarGenaiPasta(false)` e volta ao passo 0.
    - Passo 2 mostra as linhas de entrada, saída e total; o valor total sempre com o prefixo "até"; a linha de saída em `text-texto-3`; a nota de honestidade presente.
    - "Voltar" no passo 2 preserva a seleção do passo 1 sem refazer a consulta.
    - Passo 3 não oferece fechar (Esc, clique no fundo e botão de fechar inertes) e mostra o contador de segundos e a frase de não-cancelável.
    - Falha na chamada leva a um estado de erro com a cópia do contrato, não silenciosamente de volta ao passo 2.
  </behavior>
  <action>
    Criar `webapp/src/components/ClassificacaoPasta.tsx` exportando
    `ClassificacaoPasta({ onFechar })`. Estado do assistente por `useState` com
    união literal de passo (`"gate" | "candidatas" | "custo" | "rodando" |
    "revisao" | "concluido" | "erro"`) — os passos `revisao`/`concluido` são
    declarados aqui e implementados no plano 07-07; até lá, renderizar um
    marcador mínimo para não quebrar a máquina de estados.

    Casca do modal copiada de `ModalCaminho.tsx`, com o painel na largura do
    contrato: `w-[720px] max-w-[92vw] max-h-[85vh] overflow-y-auto rounded-md
    border border-borda bg-painel p-4`.

    Consultas com `useQuery` (`configGenaiPasta`, `candidatasGenaiPasta`) e
    mutações com `useMutation` (`habilitarGenaiPasta`,
    `estimarCustoGenaiPasta`, `rodarGenaiPasta`), cada mutação com
    `onError: (e: Error) => setErro(e.message)` — o `detail` do 409/502 do
    servidor é a cópia que o dono lê, não um "erro 409".

    Seleção do passo 1 num `Set<string>` de caminhos de pasta (não `Set<number>`:
    a chave aqui é a pasta), semeado com TODAS as candidatas no `useEffect` de
    sucesso da consulta — a fraseologia do contrato é opt-out ("Desmarque o que
    não quer"), então nascer vazio contradiz a decisão D-01.

    O passo 3 desabilita todo caminho de fechamento: nenhum `onKeyDown` de Esc
    ativo, nenhum `onClick` no fundo, e o `onFechar` não é oferecido. Contador
    de segundos por `useEffect` com `setInterval`, limpo no desmonte.

    Todas as strings visíveis saem do § Copywriting Contract do UI-SPEC, na
    forma literal. Nenhum ícone de biblioteca — só os glifos de texto que o
    contrato lista.
  </action>
  <acceptance_criteria>
    - `cd webapp && npx tsc -b --noEmit` sem erro e `npm run build` verde.
    - `grep -c "<button" webapp/src/components/ClassificacaoPasta.tsx` retorna `0` fora de `<input type="checkbox">` — todo botão é `<Botao>`.
    - `grep -n "Desmarque o que não quer incluir nesta sessão" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal do contrato.
    - `grep -n "Serviços externos estão desligados" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal.
    - `grep -n "A chamada já foi enviada e não pode ser cancelada" webapp/src/components/ClassificacaoPasta.tsx` encontra a cópia literal.
    - Nenhuma classe de cor inventada: `grep -oE "text-\[#|bg-\[#" webapp/src/components/ClassificacaoPasta.tsx` retorna vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit && npx vitest run ClassificacaoPasta</automated>
  </verify>
  <done>Passos 0 a 3 renderizam com a cópia literal do contrato, seleção opt-out e nenhum caminho de fechar durante a chamada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes vitest dos passos 0 a 3</name>
  <files>webapp/src/components/ClassificacaoPasta.test.tsx</files>
  <read_first>
    - `webapp/src/components/EscritaExif.test.tsx` (INTEIRO) — este é o analog e o `07-PATTERNS.md` avisa que a estrutura dele foi inferida, não lida: confirmar aqui como o `QueryClientProvider` é montado, como `api` é mockado (`vi.mock`) e como os textos são consultados, e seguir esse jeito em vez de inventar outro.
    - `webapp/src/components/ClassificacaoPasta.tsx` (Task 2).
  </read_first>
  <behavior>
    - `test: mestre desligado não oferece caixa` — com `servicos_externos: false`, a mensagem de mestre desligado aparece e não há `checkbox` na tela.
    - `test: habilitar exige a caixa marcada` — botão "Habilitar e continuar" começa desabilitado; marcar a caixa habilita.
    - `test: candidatas nascem todas marcadas` — três candidatas, rodapé diz "3 de 3 pastas selecionadas".
    - `test: desmarcar atualiza a contagem e o botão` — desmarcar as três desabilita "Avançar" com o `title` de motivo.
    - `test: etiqueta de campo ausente` — as três variantes de etiqueta aparecem conforme os dados.
    - `test: estado vazio` — zero candidatas mostra a cópia literal do estado vazio e só o botão "Fechar".
    - `test: passo de custo mostra "até" no total` — o total renderizado contém "até" e a linha de saída não é apresentada como exata.
    - `test: durante a rodada não há como fechar` — no passo 3, Esc não chama `onFechar` e nenhum botão de fechar está presente.
    - `test: erro do servidor vira a cópia do contrato` — mutação rejeitada com `Error("...")` renderiza a mensagem no lugar de erro.
  </behavior>
  <action>
    Criar `webapp/src/components/ClassificacaoPasta.test.tsx` com os nove casos do
    bloco `<behavior>`, no molde confirmado em `EscritaExif.test.tsx`. Cada
    asserção de texto usa a string literal do § Copywriting Contract — o teste é
    o que prende a implementação ao contrato de cópia, mesma disciplina que os
    12 testes de `EscritaExif.test.tsx` seguiram na Fase 6.
  </action>
  <acceptance_criteria>
    - `cd webapp && npx vitest run ClassificacaoPasta` passa com 9 testes.
    - `grep -c "it(\|test(" webapp/src/components/ClassificacaoPasta.test.tsx` retorna `9`.
    - Ao menos quatro asserções comparam com strings literais do Copywriting Contract.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run ClassificacaoPasta</automated>
  </verify>
  <done>Nove testes verdes prendendo os passos 0 a 3 ao contrato de cópia e ao comportamento de gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dono → consentimento | O passo 0 é a superfície de consentimento informado do invariante 4: precisa dizer O QUE sai, PARA QUE, PARA ONDE e como revogar, antes de a caixa poder ser marcada. |
| Navegador → servidor local | Somente 127.0.0.1. A lista de pastas enviada é reconciliada no servidor (07-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Elevation of Privilege | Tela ligando o recurso sem consentimento explícito | mitigate | "Habilitar e continuar" desabilitado enquanto a caixa não é marcada; o corpo do passo 0 declara literalmente o que sai (nome de pasta e metadado, nunca imagens). Testes `mestre desligado` e `habilitar exige a caixa`. |
| T-07-06-02 | Elevation of Privilege | Tela ligando a chave mestra `servicos_externos` | mitigate | Com o mestre desligado a tela não renderiza caixa nenhuma e só instrui a editar o TOML. Nenhuma chamada a endpoint que escreva o mestre — ele não existe. |
| T-07-06-03 | Repudiation | Custo apresentado com falsa precisão | mitigate | Total sempre prefixado "até"; linha de saída em token secundário; nota de honestidade obrigatória; teste assevera o "até". |
| T-07-06-04 | Information Disclosure | Cópia de erro vazando segredo do servidor | accept | O `detail` do servidor é escrito pelo próprio app (07-04) e não contém credencial; o app roda só em 127.0.0.1 para um único usuário. Revisar a mensagem de 502 durante a verificação humana (07-09/07-10). |
| T-07-06-SC | Tampering | instalação de pacote npm | accept | Nenhuma dependência nova — React, React Query, Tailwind e vitest já instalados. |
</threat_model>

<verification>
- `cd webapp && npx tsc -b --noEmit` limpo.
- `cd webapp && npx vitest run ClassificacaoPasta` verde (9 testes).
- `cd webapp && npm run build` verde.
</verification>

<success_criteria>
- Passo 0 é consentimento informado com revogação visível.
- Passo 1 pré-filtra e nasce opt-out.
- Passo 2 mostra custo com a incerteza declarada.
- Passo 3 é honestamente não-cancelável.
</success_criteria>

<output>
Create `.planning/phases/07-classifica-o-de-pasta-por-genai/07-06-SUMMARY.md` when done
</output>
