---
phase: 06-escrita-exif-de-localiza-o
plan: 07
type: execute
wave: 5
depends_on: ["06-06"]
files_modified:
  - webapp/src/api.ts
  - webapp/src/hooks/useJob.ts
  - webapp/src/components/EscritaExif.tsx
  - webapp/src/App.tsx
autonomous: true
requirements: [EXIF-01, EXIF-02]

must_haves:
  truths:
    - "Existe uma aba Localização que lista os planos de escrita e o detalhe do plano selecionado"
    - "O dono vê, por arquivo, os três campos de localização e o valor que entraria em cada um"
    - "Campo já preenchido aparece como pulado, com o valor existente no title, e não como erro"
    - "Gravar fica desabilitado enquanto não houver dry-run"
  artifacts:
    - path: "webapp/src/components/EscritaExif.tsx"
      provides: "tela plano→dry-run→gravar, sidebar, veredito, linhas tipo A"
      min_lines: 180
    - path: "webapp/src/api.ts"
      provides: "PlanoExif, ItemPlanoExif, PlanoExifDetalhe, RelatorioDryRunExif e as chamadas /api/exif"
      contains: "planosExif"
  key_links:
    - from: "webapp/src/App.tsx"
      to: "webapp/src/components/EscritaExif.tsx"
      via: "aba Localização"
      pattern: "EscritaExif"
    - from: "webapp/src/components/EscritaExif.tsx"
      to: "/api/exif"
      via: "useQuery/useMutation sobre o cliente de api.ts"
      pattern: "planosExif|dryRunExif|criarPlanoExif"
---

<objective>
A base da tela `Localização`: tipos e cliente HTTP, o método de job para
disparar a escrita, e o componente `EscritaExif.tsx` na sua forma esqueleto —
sidebar de planos, barra de ações, linha de veredito, estado vazio e linha
tipo A (escrita direta, com os três chips de campo).

Purpose: EXIF-01 exige que o dono veja o plano antes de qualquer escrita. Este
plano entrega a superfície onde ele vê; o plano 06-08 acrescenta os checkboxes,
as linhas B/C e o detalhamento pós-execução.

Output: `api.ts`, `useJob.ts`, `EscritaExif.tsx` e a aba registrada em
`App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-CONTEXT.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-UI-SPEC.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-PATTERNS.md
@docs/DIRECAO_DE_ARTE.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrato HTTP produzido pelo plano 06-06 (não re-derivar) -->

`GET /api/exif` → `PlanoExif[]`
`POST /api/exif/plano` → `PlanoExif` (409 com `detail`)
`GET /api/exif/{id}` → `PlanoExifDetalhe`
`POST /api/exif/{id}/dry-run` → `RelatorioDryRunExif`
`POST /api/exif/{id}/executar` corpo `{itens: number[] | null}` → estado do job
`GET /api/exif/{id}/auditoria` → `LinhaAuditoria[]`

<!-- Tipos a declarar em webapp/src/api.ts -->

```
export type StatusCampoExif =
  | "pendente" | "pronto" | "pulado" | "sem_valor" | "gravado" | "falha";

export interface CampoExif {
  valor: string | [number, number] | null;
  status: StatusCampoExif;
  motivo: string | null;
}

export interface ItemPlanoExif {
  id: number;
  media_id: number;
  origem: string;
  nome: string;
  incluido: boolean;
  formato_suportado: boolean;
  motivo_nao_suportado: string | null;
  sidecar_destino: string | null;
  pasta_sincronizada: string | null;
  erro: string | null;
  backup_original: string | null;
  campos: { gps: CampoExif; cidade: CampoExif; pais: CampoExif };
}

export interface PlanoExif {
  id: number; nome: string; status: string;
  dry_run_em: string | null; criado_em: string;
  total_itens: number;
  prontos: number | null; problemas: number | null;
  campos_a_gravar: number | null; sidecars: number | null;
  nao_suportados: number; sincronizados: number;
  gravados: number; com_erro: number;
  executavel: boolean;
}

export type PlanoExifDetalhe = PlanoExif & { itens: ItemPlanoExif[] };

export interface RelatorioDryRunExif {
  prontos: number; problemas: string[];
  campos_a_gravar: number; sidecars: number;
  nao_suportados: number; sincronizados: number;
}
```

<!-- Produzido para o plano 06-08 -->
`EscritaExif.tsx` default-exporta um componente com a prop `{ job: Job }`,
igual a `Operations.tsx`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tipos, cliente /api/exif e executarEscritaExif no useJob</name>
  <files>webapp/src/api.ts, webapp/src/hooks/useJob.ts</files>
  <read_first>
    - `webapp/src/api.ts` (linhas 300-360 e 390-420 e 515-540) — os tipos
      `Plano`/`ItemPlano`/`PlanoDetalhe`/`RelatorioDryRun`/`LinhaAuditoria`, os
      helpers `json<T>`/`post<T>` (com a extração de `dados.detail` que preserva
      a mensagem do servidor) e o literal do objeto `api` onde as funções vivem.
    - `webapp/src/hooks/useJob.ts` (arquivo inteiro) — `disparar`, o objeto de
      retorno com `executarPlano`, e `export type Job = ReturnType<typeof useJob>`.
  </read_first>
  <action>
    Em `webapp/src/api.ts`, declarar os tipos do bloco `<interfaces>` acima,
    posicionados logo depois do bloco de tipos de `Plano`/`ItemPlano`/
    `RelatorioDryRun`, com um comentário curto explicando por que `ItemPlanoExif`
    tem `campos` aninhado e não nove chaves planas (os três campos são irmãos e
    a UI os renderiza como três chips; achatar espalharia a mesma estrutura por
    três lugares) e por que existe `StatusCampoExif` como união literal em vez de
    `string` (a UI mapeia status → cor/copy e um `string` solto deixaria o
    `switch` sem exaustividade).

    Reusar `LinhaAuditoria` como está — a forma da linha de auditoria não mudou.

    Acrescentar ao objeto `api`, junto das funções de operações:
    `planosExif`, `planoExif`, `criarPlanoExif` (sem parâmetro — escopo global,
    escrita in-place, não há raiz de destino), `dryRunExif`, `auditoriaExif`,
    apontando para as URLs do bloco `<interfaces>` e reusando os helpers
    `json`/`post` existentes (não criar wrapper de fetch novo).

    Em `webapp/src/hooks/useJob.ts`, acrescentar ao objeto de retorno
    `executarEscritaExif: (planId: number, itens: number[] | null) =>
    disparar(\`/api/exif/${planId}/executar\`, { itens })`, ao lado de
    `executarPlano`. Comentar em uma linha que este é o único disparo de job que
    carrega seleção do dono no corpo — os outros só recebem o alvo.
  </action>
  <verify>
    <automated>cd webapp && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd webapp && npx tsc -b` sai com código 0.
    - `grep -c "planosExif\|planoExif\|criarPlanoExif\|dryRunExif\|auditoriaExif" webapp/src/api.ts` >= 5.
    - `grep -c "executarEscritaExif" webapp/src/hooks/useJob.ts` >= 1.
    - `grep -c "StatusCampoExif" webapp/src/api.ts` >= 2 (declaração + uso em `CampoExif`).
    - `cd webapp && npm test` continua verde (nenhum teste existente quebrou com a mudança do tipo `Job`).
  </acceptance_criteria>
  <done>Os tipos compilam, as cinco chamadas existem no objeto `api` e `useJob` expõe `executarEscritaExif`.</done>
</task>

<task type="auto">
  <name>Task 2: EscritaExif.tsx (esqueleto + linhas tipo A) e a aba Localização</name>
  <files>webapp/src/components/EscritaExif.tsx, webapp/src/App.tsx</files>
  <read_first>
    - `webapp/src/components/Operations.tsx` (arquivo inteiro, 331 linhas) — o
      analog estrutural completo: imports, `veredito()`, o layout de duas
      colunas, o `<aside className="w-72 ...">` da lista de planos, a barra de
      ações, o wiring de `useQuery`/`useMutation`/`useQueryClient`, o bloco de
      linhas e o `<details>` de auditoria. Prestar atenção ao que **não** copiar:
      o input de texto de `destino` e o mapa `CORES_STATUS`.
    - `.planning/phases/06-escrita-exif-de-localiza-o/06-UI-SPEC.md` (arquivo
      inteiro) — é contrato, não sugestão: escala de espaçamento, tokens de cor
      com significado por fase, tipografia, anatomia da linha tipo A, ponto focal
      e a tabela de Copywriting Contract com as strings exatas.
    - `webapp/src/App.tsx` (linhas 20-70 e o bloco de render de abas por volta
      da linha 309) — `ABAS`, `DICAS`, `ABAS_COM_FONTE` e o padrão
      `{aba === "X" && <Componente job={job} />}`.
    - `webapp/src/ui/Botao.tsx` — variantes disponíveis (`solido` para a CTA).
    - `webapp/src/index.css` — os tokens `--color-acento`, `--color-atencao`,
      `--color-ok`, `--color-erro`, `--color-herdado`, `--color-texto-2/3`,
      `--text-micro`, `font-titulo`.
  </read_first>
  <action>
    Criar `webapp/src/components/EscritaExif.tsx` copiando a **estrutura** de
    `Operations.tsx` e substituindo o conteúdo:

    - Imports no mesmo formato do analog, trocando `Plano`/`RelatorioDryRun` por
      `PlanoExif`/`PlanoExifDetalhe`/`RelatorioDryRunExif`/`ItemPlanoExif`.
    - `veredito(p: PlanoExif): string` na mesma forma do analog, com o verbo
      "gravar": sem dry-run → `"sem dry-run — nada pode ser gravado ainda"`;
      `prontos === 0` → `` `dry-run ${quando}: nenhum arquivo a gravar (${p.problemas} problemas)` ``;
      com problemas → `` `dry-run ${quando}: ${p.prontos} prontos, ${p.problemas} com problema` ``;
      limpo → `` `dry-run ${quando}: ${p.prontos} prontos, sem problemas` ``.
    - Sidebar `<aside className="w-72 ...">` com a lista de planos, seleção
      destacada com `--color-acento`, cópia estrutural do analog.
    - Barra de ações com três botões: `"Criar plano de escrita"` (sem campo de
      texto — não existe destino a escolher), `"Rodar dry-run"` e a CTA
      `` `Gravar ${n} arquivos` `` com `Botao variante="solido"`, desabilitada
      enquanto `!plano.executavel`, com `title` explicando o porquê no estilo do
      analog. Neste plano a CTA ainda chama
      `job.executarEscritaExif(plano.id, null)` — a seleção por checkbox entra no
      plano 06-08.
    - Estado vazio com o heading `"Nada para gravar"` e o corpo exato da tabela
      de copy da UI-SPEC.
    - Lista de linhas: uma linha por `ItemPlanoExif`, shell
      `flex items-center gap-3 px-3 py-1.5 border-b border-borda`, nome do
      arquivo em `font-titulo` com truncate, e à direita os três chips de campo
      (GPS, Cidade, País) como pills `rounded-full text-[11px] px-2 py-0.5
      border border-borda`.
      - Campo com `status === "pronto"`: o chip mostra o rótulo e o valor —
        GPS formatado com 4 casas (`GPS -23.5505, -46.6333`), cidade e país com
        o texto. `text-texto-2`.
      - Campo com `status === "pulado"`: chip com `Cidade já preenchida` em
        `text-texto-2` (**não** `text-erro` — é comportamento esperado) e o
        `title` recebendo o `motivo` que o servidor mandou (que já vem no formato
        `"já preenchido: {valor} — não sobrescrito"`).
      - Campo com `status === "sem_valor"`: chip em `text-texto-3` com o rótulo e
        um travessão; `title` com o motivo.
      - Os demais status (`gravado`, `falha`) ficam para o plano 06-08 — neste
        plano cair no default neutro é aceitável, mas o `switch` deve ser
        exaustivo sobre `StatusCampoExif` para o TypeScript acusar quando o
        próximo plano esquecer um caso.
    - Bloco `<details>` de auditoria, reuso verbatim da forma do analog, apontando
      para `api.auditoriaExif`.

    Não introduzir nenhum token de cor novo, nenhum tamanho de fonte fora dos
    três da UI-SPEC, e nenhuma biblioteca de ícone — só glifos de texto, como o
    resto do código. O acento `--color-acento` é reservado à CTA, ao plano
    selecionado na sidebar e ao anel de foco; badge e chip usam os tokens
    semânticos.

    Em `webapp/src/App.tsx`: acrescentar `"Localização"` ao array `ABAS`,
    **depois** de `"Operações"`; acrescentar a entrada correspondente em `DICAS`
    com o texto exato da UI-SPEC
    (`"revise o plano linha a linha; desmarque o que não quer gravar — o original só muda depois de aprovar"`);
    **não** acrescentar a `ABAS_COM_FONTE` (escopo global, decisão explícita da
    UI-SPEC); e acrescentar
    `{aba === "Localização" && <EscritaExif job={job} />}` no bloco de render,
    ao lado da linha de `Operações`.
  </action>
  <verify>
    <automated>cd webapp && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd webapp && npx tsc -b` e `npm run build` saem com código 0.
    - `grep -c '"Localização"' webapp/src/App.tsx` >= 2 (ABAS + render) e `grep -c "EscritaExif" webapp/src/App.tsx` >= 2 (import + uso).
    - `grep -c "ABAS_COM_FONTE" webapp/src/App.tsx` inalterado e a linha de `ABAS_COM_FONTE` não contém `Localização`: `grep "ABAS_COM_FONTE" webapp/src/App.tsx | grep -c "Localização"` retorna 0.
    - `grep -c "Nada para gravar" webapp/src/components/EscritaExif.tsx` >= 1.
    - `grep -c "Criar plano de escrita" webapp/src/components/EscritaExif.tsx` >= 1 e `grep -c "Rodar dry-run" webapp/src/components/EscritaExif.tsx` >= 1.
    - `grep -c "destino" webapp/src/components/EscritaExif.tsx` retorna 0 — nenhum campo de destino foi copiado do analog.
    - `grep -c "text-erro" webapp/src/components/EscritaExif.tsx` conta apenas usos ligados a `falha`/`erro`, nunca ao chip de `pulado`.
    - `cd webapp && npm test` continua verde.
  </acceptance_criteria>
  <done>A aba Localização aparece, lista planos, cria plano, roda dry-run e mostra uma linha por arquivo com os três chips de campo; o build e o typecheck passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta do servidor local → render | JSON do backend vira texto e `title` na tela |
| clique do dono → disparo de escrita física | A CTA é o gatilho da primeira mutação de arquivo original do produto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-37 | Tampering | Gravar disparado antes do dry-run pela UI | mitigate | CTA desabilitada enquanto `!plano.executavel`; o servidor também recusa com 409 e o executor tem a própria porta — três camadas |
| T-06-38 | Information Disclosure | Caminho completo do arquivo exposto na tela | accept | App local-first, tela do próprio dono; o caminho é informação que ele precisa para decidir (invariante 4 trata de dado saindo da máquina, não de tela local) |
| T-06-39 | Spoofing | Mensagem de erro do servidor engolida e substituída por "erro 409" | mitigate | Reuso do helper `post` existente, que já extrai `dados.detail`; a UI mostra a frase literal |
| T-06-40 | Repudiation | Campo pulado parecendo erro e levando o dono a concluir que o app falhou | mitigate | UI-SPEC fixa `text-texto-2` para `pulado` e reserva `text-erro` para falha; gate de `grep` no critério de aceite |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova no `webapp/package.json` — checkbox e chips são HTML nativo com Tailwind (RESEARCH.md Pitfall 6); gate: `git diff webapp/package.json` vazio |
</threat_model>

<verification>
- `cd webapp && npx tsc -b && npm test && npm run build` — os três verdes.
- `git diff webapp/package.json webapp/package-lock.json` vazio — nenhuma dependência nova.
- `scripts/verificar.sh` verde.
</verification>

<success_criteria>
1. A aba `Localização` existe, é global (não filtrada por fonte) e renderiza a tela de duas colunas no molde de `Operações`.
2. O dono cria um plano e roda o dry-run pela UI, e vê a linha de veredito com o verbo "gravar".
3. Cada arquivo aparece com os três chips de campo, mostrando o valor que entraria ou o motivo do pulo.
4. `Gravar N arquivos` fica desabilitado até haver dry-run aprovado.
5. Nenhum token de cor, tamanho de fonte ou dependência nova foi introduzido.
</success_criteria>

<output>
Create `.planning/phases/06-escrita-exif-de-localiza-o/06-07-SUMMARY.md` when done
</output>
