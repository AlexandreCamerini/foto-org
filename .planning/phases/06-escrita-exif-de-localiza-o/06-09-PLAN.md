---
phase: 06-escrita-exif-de-localiza-o
plan: 09
type: execute
wave: 7
depends_on: ["06-04", "06-05", "06-08"]
files_modified:
  - docs/ARQUITETURA.md
autonomous: false
requirements: [EXIF-01, EXIF-02, EXIF-03, EXIF-04, EXIF-05]

must_haves:
  truths:
    - "A arquitetura documentada descreve o módulo de escrita EXIF e por que ele não estende operations/"
    - "A fatia inteira está verde: pytest, benchmark de agrupamento, vitest e build do webapp"
    - "O dono percorreu o fluxo real na UI e confirmou que o plano, o opt-out e o resultado batem com o que a tela promete"
    - "A localização gravada é lida de volta pela ferramenta que o dono usa no dia a dia"
  artifacts:
    - path: "docs/ARQUITETURA.md"
      provides: "seção do módulo exif_write e do fluxo plano→dry-run→escrita verificada"
      contains: "exif_write"
  key_links:
    - from: "docs/ARQUITETURA.md"
      to: "docs/DECISOES.md D-075 e a entrada de allowlist medida"
      via: "referência cruzada"
      pattern: "D-075"
---

<objective>
Fechar a fase: documentar o módulo novo na arquitetura, rodar o gate completo do
projeto, e submeter ao dono as duas verificações que só ele pode fazer — o fluxo
real na UI e a leitura de volta da localização gravada na ferramenta que ele usa
(a pergunta em aberto que a pesquisa deixou explicitamente para antes do
rollout).

Purpose: `scripts/verificar.sh` verde é o critério do projeto para "fatia
pronta". E a premissa de D-075 — gravar no EXIF torna o dado utilizável em
qualquer ferramenta — nunca foi confirmada empiricamente contra Lightroom,
Photos.app ou Finder; se ela não valer, o argumento inteiro da fase enfraquece e
o dono precisa saber disso antes de rodar o plano no acervo completo.

Output: `docs/ARQUITETURA.md` atualizado e dois checkpoints de verificação
humana respondidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-CONTEXT.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-RESEARCH.md
@.planning/phases/06-escrita-exif-de-localiza-o/06-UI-SPEC.md
@docs/ARQUITETURA.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentar o módulo exif_write e rodar o gate completo</name>
  <files>docs/ARQUITETURA.md</files>
  <read_first>
    - `docs/ARQUITETURA.md` (arquivo inteiro) — as seções `## Fluxo de dados`,
      `## Schema inicial`, `## Decisões registradas` e `## Riscos principais`, e
      o nível de detalhe/tom já usado (é doc de arquitetura curto, não manual).
    - `.planning/phases/06-escrita-exif-de-localiza-o/06-RESEARCH.md` §
      "Architecture Patterns / System Architecture Diagram" — o desenho já
      aprovado do fluxo; transcrever a essência, não reinventar.
    - `docs/DECISOES.md` — a entrada D-075 e a entrada de allowlist medida criada
      no plano 06-04, para referenciar pelos números certos.
    - `CLAUDE.md` invariante 7 (texto vigente) — confirmar que já cobre a
      permissão desta fase e **não** precisa de alteração; se precisar, dizer no
      SUMMARY em vez de editar sem sinalizar.
  </read_first>
  <action>
    Acrescentar a `docs/ARQUITETURA.md` uma seção sobre `fotoorganizer/exif_write/`,
    no tom curto do documento, cobrindo:
    - o que o módulo é: escrita de localização (GPS lat/long, cidade, país) no
      arquivo original ou num sidecar `.xmp`, só em campo vazio, autorizada por
      D-075 e pelo invariante 7 vigente;
    - por que é módulo próprio e não extensão de `operations/`: a cópia se
      protege criando um caminho novo que ainda não existe (`open('xb')`), e
      mutação in-place não tem equivalente disso; o modelo de segurança é outro
      (backup `_original` + diff de tags), e o modelo de status também (por
      campo, não por item);
    - o fluxo: planner (só banco) → dry-run (lê o disco ao vivo, em lote) →
      seleção do dono → executor (reconfere ao vivo, escreve, verifica por diff,
      só então apaga o backup) → `AuditLog`;
    - a nota de que `AuditLog` é reusado com `plan_id=NULL` e o id do plano
      dentro do `detalhe` JSON, porque a coluna tem FK real para
      `operation_plans.id`;
    - o que **não** é escrito: qualquer campo fora de localização, e nunca sobre
      valor existente;
    - referência à entrada de allowlist medida em `docs/DECISOES.md` (a criada
      no plano 06-04) como fonte de verdade de quais formatos entram.

    Acrescentar às tabelas/listas existentes de `## Decisões registradas` a
    referência a D-075 e à entrada nova, e a `## Riscos principais` o risco
    concreto de escrita em pasta sincronizada (D-07) e o de formato sem amostra
    testável (D-09), com a mitigação de cada um em uma linha.

    Rodar o gate completo do projeto: `scripts/verificar.sh`. Se algo estiver
    vermelho, corrigir antes de seguir — não avançar de fatia com teste quebrado
    é regra do `METODO_DE_TRABALHO.md`. Transcrever a saída resumida no SUMMARY.
  </action>
  <verify>
    <automated>scripts/verificar.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verificar.sh` sai com código 0 e imprime `✅ Fatia verde`.
    - `grep -c "exif_write" docs/ARQUITETURA.md` >= 1 e `grep -c "D-075" docs/ARQUITETURA.md` >= 1.
    - `grep -c "operation_plans" docs/ARQUITETURA.md` >= 1 — a armadilha da FK está documentada onde alguém vai procurar.
    - `git diff --name-only` não lista `CLAUDE.md` (o invariante 7 já cobre a fase; qualquer necessidade de mudança é reportada no SUMMARY, não aplicada em silêncio).
  </acceptance_criteria>
  <done>A arquitetura documenta o módulo novo e a fatia inteira está verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dono percorre o fluxo real na UI</name>
  <files>(nenhum — verificação humana, nada é modificado)</files>
  <read_first>
    - `.planning/phases/06-escrita-exif-de-localiza-o/06-UI-SPEC.md` §
      "Copywriting Contract" e § "Row anatomy" — o que a tela prometeu entregar.
  </read_first>
  <action>
    Preparar o ambiente e entregar o roteiro ao dono, sem executar o roteiro por
    ele. Antes de abrir o checkpoint: confirmar que a Task 1 deixou
    `scripts/verificar.sh` verde, que `exiftool -ver` responde, e que
    `scripts/executar.sh` sobe o servidor local. **Não** cadastrar fonte, não
    criar plano e não gravar nada em nome do dono — o passo de duplicar a pasta
    antes de testar é justamente a salvaguarda que este checkpoint existe para
    exercitar, e fazê-lo por ele destruiria o teste. Apresentar o roteiro de
    `<how-to-verify>` na íntegra e aguardar a resposta.
  </action>
  <what-built>
    A aba `Localização` completa: criar plano de escrita, rodar dry-run, ver uma
    linha por arquivo com os três campos de localização, desmarcar linhas
    pontuais, gravar em lote, e ver o resultado campo a campo. O backend grava só
    em campo vazio, verifica cada escrita por diff completo de tags e preserva a
    cópia `_original` até o diff aprovar.
  </what-built>
  <how-to-verify>
    Antes de tudo: **rodar contra uma cópia descartável, não contra o acervo
    real.** Duplique uma pasta pequena de fotos (`cp -R` para
    `~/Desktop/teste-exif`), cadastre-a como fonte no app, escaneie, e gere
    sugestões. Só depois siga.

    1. Suba o app (`scripts/executar.sh`) e abra a aba `Localização`.
    2. Se a tela mostrar `"Nada para gravar"`, confirme que é verdade: em
       `Revisão`, veja se há sugestões de localização geradas para a pasta de
       teste. Se houver e a tela ainda disser "nada para gravar", isso é um bug —
       reporte.
    3. Clique em `Criar plano de escrita`. Confirme que a lista traz uma linha
       por arquivo elegível e que cada linha mostra os três chips (GPS, Cidade,
       País) com o valor que entraria ou o motivo do pulo.
    4. Clique em `Rodar dry-run`. Confirme que: (a) a linha de veredito aparece
       com a contagem; (b) **nenhum arquivo mudou** — rode
       `find ~/Desktop/teste-exif -name '*_original' -o -name '*.xmp'` e confirme
       que não veio nada.
    5. Desmarque uma linha. Confirme que o rótulo do botão cai de
       `Gravar N arquivos` para `Gravar N-1 arquivos`.
    6. Desmarque todas. Confirme que o botão fica desabilitado.
    7. Remarque duas linhas e clique em `Gravar 2 arquivos`. Acompanhe o
       progresso.
    8. Ao terminar: confirme que as duas linhas mostram `✓` nos campos gravados;
       que a linha desmarcada **não** mudou; e que
       `find ~/Desktop/teste-exif -name '*_original'` não retorna nada (o backup
       foi apagado depois do diff aprovar).
    9. Rode o dry-run de novo no mesmo plano. Confirme que os campos já gravados
       aparecem como `pulado — já preenchido` e que gravar de novo não muda nada
       (idempotência).
    10. Abra o `<details>` de `Auditoria` e confirme que há linhas para plano
        criado, dry-run, escrita verificada e limpeza de backup.
    11. Se houver algum arquivo `.cr2`/`.dng`/`.tif` ou de formato não suportado
        na pasta, confirme que ele aparece com o motivo visível e, no caso não
        suportado, com a oferta `Gravar sidecar .xmp para este arquivo`
        **desmarcada** por padrão.
  </how-to-verify>
  <verify>
    <human-check>Os 11 passos do roteiro foram percorridos numa pasta descartável e o resultado de cada um foi reportado.</human-check>
  </verify>
  <acceptance_criteria>
    - Os passos 4 e 8 confirmam ausência de resíduo (`_original`/`.xmp` órfão).
    - O passo 5/6 confirma que o opt-out por linha funciona e desabilita a CTA em zero.
    - O passo 8 confirma que a linha desmarcada não foi tocada.
    - O passo 9 confirma idempotência com a cópia `"já preenchido"` visível.
    - Qualquer divergência entre a tela e o Copywriting Contract da UI-SPEC é reportada como issue, não aceita em silêncio.
  </acceptance_criteria>
  <done>O dono percorreu os 11 passos numa cópia descartável e aprovou, ou listou as divergências.</done>
  <resume-signal>Escreva "aprovado" ou descreva o que divergiu, passo a passo.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar leitura de volta na ferramenta que o dono usa</name>
  <files>(nenhum — verificação humana; um eventual registro de decisão sai como follow-up)</files>
  <read_first>
    - `.planning/phases/06-escrita-exif-de-localiza-o/06-RESEARCH.md` §
      "Open Questions" pergunta 1 e § "Assumptions Log" A1 — o que exatamente
      não foi verificado e por quê.
  </read_first>
  <action>
    Escolher um dos arquivos gravados no checkpoint anterior (da pasta
    descartável, nunca do acervo real), rodar `exiftool -G1 -a` nele e mostrar ao
    dono, no próprio checkpoint, quais grupos e tags de localização estão
    presentes — para ele conseguir comparar com o que a ferramenta de terceiro
    exibe. Então apresentar o roteiro de `<how-to-verify>` e aguardar. Não abrir
    Finder/Fotos/Lightroom por conta própria: quem sabe qual ferramenta importa
    no fluxo real é o dono.
  </action>
  <what-built>
    A escrita grava cidade e país em **dois grupos** de propósito
    (`IPTC:City` + `XMP-photoshop:City`; `IPTC:Country-PrimaryLocationName` +
    `XMP-photoshop:Country`) e GPS em `GPS:GPSLatitude/Ref` +
    `GPS:GPSLongitude/Ref`. Esses são os namespaces documentados que Adobe e
    IPTC-Core usam, e o `exiftool -validate` aprova — mas nenhuma ferramenta de
    terceiro leu isso de volta ainda nesta máquina.
  </what-built>
  <how-to-verify>
    Pegue **um** dos arquivos gravados no checkpoint anterior (o da pasta
    descartável, nunca o acervo real) e abra na ferramenta que você de fato usa
    no dia a dia:

    1. Finder → selecione o arquivo → `Cmd+I` (Obter Informações) → seção
       `Mais informações`. A localização aparece?
    2. Se você usa o Fotos do macOS: importe o arquivo e veja se o mapa /
       o campo de local mostram o lugar gravado.
    3. Se você usa Lightroom: importe e veja o painel de metadados — os campos
       `Cidade` e `País` estão preenchidos? E o módulo Mapa mostra o pin?
    4. Anote **qual** ferramenta leu e qual não leu.

    Por que isto importa: D-075 escolheu escrita EXIF direta em vez de sidecar
    justamente porque "parte do fluxo real do dono não lê XMP". Se a ferramenta
    que você usa não mostrar o dado, a premissa da decisão não se sustenta para
    ela, e vale decidir se troca o grupo de tags, se acrescenta outro, ou se
    aquela ferramenta específica vira caso de sidecar. Isso é decisão sua, não do
    plano.
  </how-to-verify>
  <verify>
    <human-check>Ao menos uma ferramenta de terceiro foi aberta com um arquivo gravado e o resultado (leu / não leu, e qual campo) foi reportado.</human-check>
  </verify>
  <acceptance_criteria>
    - Ao menos uma ferramenta de terceiro foi testada e o resultado (leu / não leu, e qual campo) está registrado.
    - Se alguma ferramenta relevante não ler, o fato é registrado como decisão pendente em `docs/DECISOES.md` ou como item de backlog — não é silenciosamente ignorado.
  </acceptance_criteria>
  <done>A premissa de interoperabilidade de D-075 foi testada contra ao menos uma ferramenta real e o resultado está registrado no SUMMARY.</done>
  <resume-signal>Diga qual ferramenta testou e o que ela mostrou, ou "aprovado" se tudo leu como esperado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente de teste do dono → acervo real | O risco desta fase é o dono validar direto no acervo em vez de numa cópia |
| dado gravado → ferramenta de terceiro | A premissa de interoperabilidade de D-075 é uma suposição até aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-46 | Tampering | Verificação humana feita direto no acervo real, transformando o teste em mutação irreversível de 1.400 arquivos | mitigate | O passo 0 do checkpoint manda duplicar uma pasta pequena com `cp -R` e cadastrar a cópia como fonte; é a primeira instrução, antes de qualquer clique, e o executor está proibido de preparar a fonte por conta própria |
| T-06-47 | Repudiation | Premissa de interoperabilidade de D-075 nunca confirmada, e a fase encerrando como se estivesse | mitigate | Checkpoint dedicado, com resultado obrigatoriamente registrado; ferramenta que não lê vira decisão pendente, não silêncio |
| T-06-48 | Denial of Service | Resíduo `_original`/`.xmp` permanente na árvore de fotos | mitigate | Passos 4 e 8 do checkpoint fazem `find` explícito por resíduo |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo nesta fase; nenhum comando de instalação neste plano |
</threat_model>

<verification>
- `scripts/verificar.sh` verde (pytest + benchmark de agrupamento + vitest + build do webapp).
- `docs/ARQUITETURA.md` descreve `exif_write/`, o motivo do módulo próprio e a armadilha da FK.
- Os dois checkpoints foram respondidos pelo dono e as respostas estão no SUMMARY.
</verification>

<success_criteria>
1. A fatia inteira está verde pelo gate do próprio projeto.
2. A arquitetura documentada explica o módulo novo e as duas armadilhas que um leitor futuro tropeçaria (FK do AuditLog, modelo de segurança diferente do de cópia).
3. O dono percorreu o fluxo real numa cópia descartável e confirmou plano, dry-run sem escrita, opt-out por linha, resultado por campo e idempotência.
4. A premissa de interoperabilidade de D-075 foi testada contra ao menos uma ferramenta real e o resultado está registrado.
</success_criteria>

<output>
Create `.planning/phases/06-escrita-exif-de-localiza-o/06-09-SUMMARY.md` when done
</output>
