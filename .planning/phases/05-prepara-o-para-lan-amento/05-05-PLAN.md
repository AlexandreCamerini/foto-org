---
phase: 05-prepara-o-para-lan-amento
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - docs/AVALIACAO_UX.md
autonomous: false
requirements: [LANC-03]

must_haves:
  truths:
    - "Um usuário de primeira vez, sem instrução e sem documentação, chegou a uma grade populada"
    - "O que ele hesitou, tentou e errou está registrado com evidência, não com impressão"
    - "Cada achado está classificado como bloqueador ou não-bloqueador, sem trabalho de UI pré-aprovado"
  artifacts:
    - path: "docs/AVALIACAO_UX.md"
      provides: "Rodada datada com o resultado do teste de primeira execução"
      contains: "primeira execução"
  key_links:
    - from: "webapp/src/components/Panorama.tsx"
      to: "webapp/src/components/ModalCaminho.tsx"
      via: "botão 'Adicionar pasta…' do estado vazio abre o modal compartilhado"
      pattern: "Adicionar pasta"
    - from: "ModalCaminho"
      to: "POST /api/scan"
      via: "confirmar dispara a varredura e mostra progresso"
      pattern: "api/scan"
---

<objective>
Fechar LANC-03 validando, com um usuário genuinamente sem instrução, o caminho de
primeira execução que a Fase 4 (plano `04-06`, commit `d0c3839`) já entregou:
estado vazio → "Adicionar pasta…" → `ModalCaminho` → varredura com progresso →
grade populada.

Purpose: o critério é comportamental — "um usuário de primeira vez consegue
adicionar sua primeira fonte/pasta e chegar a uma grade populada sem ler
documentação" (D-06). Isso não se verifica lendo código; verifica-se observando
alguém que nunca viu o app.
Output: rodada datada em `docs/AVALIACAO_UX.md` com o que aconteceu e a
classificação de cada achado.

Escopo travado por D-04/D-05: esta fase **valida**, não redesenha. Não criar
wizard multi-etapa, não criar componente novo, não escrever mensagem específica de
"primeira vez" nem enriquecer o feedback de progresso — nada disso foi
pré-aprovado. Se o teste mostrar que algum desses realmente bloqueia um usuário
novo, o resultado é um achado a decidir, com evidência, não trabalho a executar
aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-06-SUMMARY.md
@.planning/phases/05-prepara-o-para-lan-amento/05-03-SUMMARY.md

<interfaces>
<!-- O caminho já construído que será validado. Não reconstruir. -->

Pontos de entrada do "Adicionar pasta…" (Fase 4, plano `04-06`):
- `webapp/src/components/Panorama.tsx:155` — estado vazio do Panorama
- `webapp/src/components/PhotoGrid.tsx` — estado vazio da Biblioteca
- `webapp/src/components/Trips.tsx` — estado vazio de Viagens
- barra lateral (`Sidebar`) — quarto ponto de entrada
Todos abrem o mesmo `webapp/src/components/ModalCaminho.tsx`, com título
"Caminho da pasta de fotos", que ao confirmar dispara `POST /api/scan` com o
caminho digitado, mostra progresso e mantém o modal aberto se o scan falhar.

Cobertura existente desse wiring: `webapp/src/App.test.tsx:373-510`, bloco
`describe("Adicionar pasta — um modal, quatro pontos de entrada (CONS-05/D-07)")`.

Como abrir o app com catálogo genuinamente vazio, sem tocar na produção:
exportar `FOTOORG_DATA_DIR=<dir descartável>` e lançar o binário do bundle
diretamente — `.../Foto Organizer.app/Contents/MacOS/foto-organizer`. Abrir por
`open -a` NÃO herda o env (LaunchServices) e cairia no catálogo real.

Padrão do documento de achados (`docs/AVALIACAO_UX.md`): seções `# ` datadas por
rodada, subseções por fonte do achado (crítica de fluxo, auditoria visual,
feedback do dono) e uma subseção final de priorização por valor/esforço. Rodadas
antigas não são reescritas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparar a sessão de teste e confirmar que o caminho está de pé</name>
  <files>(nenhum arquivo do repo é alterado nesta task — ambiente descartável e roteiro)</files>
  <read_first>
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-06-SUMMARY.md (o que exatamente a Fase 4 entregou neste caminho)
    - webapp/src/components/ModalCaminho.tsx (arquivo inteiro — o que o usuário vai ver, incluindo estados de erro e progresso)
    - webapp/src/components/Panorama.tsx (o estado vazio que é a primeira tela de um catálogo novo)
    - webapp/src/App.test.tsx linhas 373-510 (o wiring já coberto por teste)
  </read_first>
  <action>
    Rodar `cd webapp && npm test` e conferir que o bloco "Adicionar pasta — um modal,
    quatro pontos de entrada" passa. Não é substituto do teste com usuário (D-06); é para
    não queimar uma sessão de usuário real num app quebrado.

    Preparar o ambiente descartável: criar um diretório temporário para
    `FOTOORG_DATA_DIR` (catálogo genuinamente vazio, primeira execução de verdade) e
    separar uma pasta de fotos pequena e neutra que o usuário-teste possa apontar —
    fotos que não exponham o acervo pessoal do dono a quem estiver testando.

    Lançar o `.app` empacotado do plano 05-03 com esse env exportado, pelo binário direto,
    e deixar a janela aberta na tela inicial antes de entregar o teclado ao usuário. O
    launcher é operado por quem conduz o teste; a partir da janela aberta, o usuário fica
    sozinho.

    Escrever o roteiro de observação (vai para o SUMMARY, não para o repo): a única frase
    dita ao usuário é "este app organiza fotos; use como quiser" — nada além disso, nenhuma
    dica, nenhuma resposta a "onde eu clico?" enquanto ele não desistir explicitamente.
    Registrar em tempo real: (a) tempo até o primeiro clique com intenção; (b) qual dos
    quatro pontos de entrada ele achou primeiro; (c) se ele entendeu que precisava digitar
    um caminho e como reagiu ao campo de texto do `ModalCaminho`; (d) se ele soube o que
    fazer enquanto o scan rodava; (e) se ele reconheceu a grade populada como "deu certo";
    (f) toda frase de dúvida dita em voz alta. Marcar como bloqueio somente o ponto em que
    ele parou e não voltou sozinho.
  </action>
  <verify>
    <automated>cd webapp && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd webapp && npm test` sai com código 0.
    - O ambiente de teste tem `FOTOORG_DATA_DIR` apontando para diretório novo, sem `catalog.db` prévio (`test ! -f "$FOTOORG_DATA_DIR/catalog.db"` antes do lançamento).
    - O `.app` abriu com esse env e a tela inicial exibida é o estado vazio, não uma grade com dado do acervo real.
    - O roteiro de observação com os seis pontos (a)–(f) está escrito antes de o usuário começar.
  </acceptance_criteria>
  <done>Sessão pronta: app de primeira execução aberto sobre catálogo vazio, wiring verificado, roteiro de observação definido.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Teste com usuário de primeira vez, sem instrução</name>
  <files>(nenhum arquivo alterado — sessão de observação)</files>
  <action>Conduzir a sessão exatamente como descrito em &lt;how-to-verify&gt;: uma única frase dita ao usuário, silêncio depois, anotação dos seis pontos do roteiro da Task 1. Não intervir, não apontar botão e não consertar nada durante a sessão.</action>
  <what-built>
    O caminho de onboarding entregue na Fase 4 (plano `04-06`): quatro pontos de entrada
    "Adicionar pasta…" convergindo no modal compartilhado `ModalCaminho`, com progresso de
    varredura e erro exibido sem fechar o modal. O app empacotado do plano 05-03 está
    aberto sobre um catálogo vazio e descartável.
  </what-built>
  <how-to-verify>
    1. Sente alguém que nunca usou o Foto Organizer na frente da janela já aberta. Pode ser
       outra pessoa ou, se não houver ninguém disponível, alguém que não participou do
       desenvolvimento — o que não vale é você, que sabe onde tudo está.
    2. Diga exatamente uma frase: "este app organiza fotos; use como quiser". Não mostre
       documentação, não aponte botão, não responda "onde eu clico?".
    3. Observe em silêncio e anote os seis pontos do roteiro: tempo até o primeiro clique
       com intenção, qual ponto de entrada foi achado primeiro, reação ao campo de caminho,
       comportamento durante o scan, reconhecimento da grade populada, e cada frase de
       dúvida dita em voz alta.
    4. O teste termina quando ele chega a uma grade com fotos, ou quando ele desiste e pede
       ajuda. Anote qual dos dois aconteceu — é o resultado de LANC-03.
    5. Se ele travou, registre onde travou e o que tentou antes de travar; não conserte
       nada agora.
    Reporte de volta: chegou à grade (sim/não), tempo aproximado, ponto de entrada usado,
    e a lista de hesitações observadas.
  </how-to-verify>
  <resume-signal>Responda "chegou à grade" ou "travou em: &lt;ponto&gt;", com as observações do roteiro</resume-signal>
  <acceptance_criteria>
    - O relato distingue explicitamente "chegou a uma grade populada sozinho" de "precisou de ajuda", com o ponto exato onde a ajuda foi pedida.
    - As observações são comportamentais (o que a pessoa fez e disse), não opinião de design de quem observou.
    - O testador não foi alguém que participou da construção do app.
  </acceptance_criteria>
  <verify>
    <human-check>Relato do observador diz explicitamente se o usuário chegou sozinho a uma grade populada ou onde pediu ajuda, com as observações comportamentais do roteiro.</human-check>
  </verify>
  <done>Sessão realizada com alguém que não construiu o app e resultado de LANC-03 registrado sem suavização.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar a rodada e classificar os achados</name>
  <files>docs/AVALIACAO_UX.md</files>
  <read_first>
    - docs/AVALIACAO_UX.md (as ~60 primeiras linhas e todos os cabeçalhos `# ` — forma acumulativa e o estilo de priorização por valor/esforço já usado no arquivo)
    - .planning/phases/05-prepara-o-para-lan-amento/05-CONTEXT.md § Deferred Ideas (o que NÃO vira trabalho aqui)
  </read_first>
  <action>
    Acrescentar a `docs/AVALIACAO_UX.md` uma seção `# Rodada de <AAAA-MM-DD> — teste de
    primeira execução (fase 5, LANC-03)`, sem reescrever nenhuma rodada anterior. Conteúdo:

    - **Método:** quem testou (perfil, não nome), que o app era o `.app` empacotado do
      Marco 1 sobre catálogo vazio via `FOTOORG_DATA_DIR`, a única frase dita, e a ressalva
      honesta de que a janela foi aberta por quem conduziu — o teste cobre da primeira tela
      em diante, não o duplo-clique no ícone.
    - **Resultado de LANC-03:** chegou ou não chegou a uma grade populada sem documentação,
      com o tempo observado e o ponto de entrada usado. Esta linha é a resposta ao critério
      3 da fase; escrever sem suavizar.
    - **O que foi observado:** cada hesitação, tentativa e frase de dúvida, na ordem em que
      aconteceram.
    - **Achados classificados:** para cada um, se é **bloqueador** (o usuário parou e não
      voltou sozinho) ou **atrito** (hesitou e seguiu). Para bloqueadores, descrever o
      comportamento observado e a hipótese de causa — sem propor implementação. D-05 mantém
      fora de escopo, aqui, tanto a mensagem específica de "primeira vez" quanto o feedback
      de progresso mais rico: se algum deles aparecer como bloqueador, registrar como
      **decisão pendente do dono**, não como tarefa.
    - **Recomendação:** o que levar para a próxima decisão de escopo, ordenado por
      valor/esforço, no mesmo formato de priorização que o arquivo já usa.

    Se o resultado for "não chegou", dizer isso na primeira linha da seção. Um critério de
    fase que falhou é informação; maquiar seria pior do que não ter testado.
  </action>
  <verify>
    <automated>grep -c "primeira execução (fase 5, LANC-03)" docs/AVALIACAO_UX.md && git diff docs/AVALIACAO_UX.md | grep -c '^-' </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "# Rodada de .* teste de primeira execução (fase 5, LANC-03)" docs/AVALIACAO_UX.md` retorna 1.
    - `git diff docs/AVALIACAO_UX.md | grep -c '^-[^-]'` retorna 0 (nenhuma linha existente removida — o arquivo é acumulativo).
    - A seção contém uma linha que responde literalmente sim ou não ao critério "chegou a uma grade populada sem ler documentação".
    - Todo achado da seção está marcado como `bloqueador` ou `atrito`.
    - Nenhum arquivo em `webapp/src/` foi modificado por este plano (`git diff --stat webapp/` vazio) — a menos que um bloqueador tenha sido explicitamente aprovado pelo dono durante a Task 2, caso em que a aprovação está transcrita no SUMMARY.
  </acceptance_criteria>
  <done>Resultado de LANC-03 registrado com evidência comportamental e achados classificados, sem trabalho de UI não aprovado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terceiro (usuário-teste) → máquina do dono | Uma pessoa de fora opera o app e vê o que estiver na tela |
| App → catálogo | O teste cria uma fonte e varre uma pasta escolhida pelo usuário-teste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-40 | Information Disclosure | Acervo pessoal exposto ao usuário-teste | mitigate | Task 1 exige `FOTOORG_DATA_DIR` descartável e uma pasta de fotos neutra; a tela inicial deve ser o estado vazio, verificado antes de entregar o teclado |
| T-05-41 | Information Disclosure | Nome/identidade do testador em documento versionado | mitigate | Task 3 registra perfil ("alguém que nunca usou o app"), nunca nome |
| T-05-42 | Tampering | Usuário-teste apontando o scan para uma pasta sensível | accept | A varredura é somente leitura por invariante 1 e o catálogo é descartável; nenhum arquivo original é alterado |
| T-05-SC | Tampering | instalações npm/pip/cargo | mitigate | Nenhum pacote novo neste plano. RESEARCH.md § Package Legitimacy Audit: não aplicável |

Per RESEARCH.md § Security Domain, validar UX não introduz superfície ASVS nova:
nenhum código de produção muda neste plano por padrão, e o app segue local-only e
single-user.
</threat_model>

<verification>
1. `cd webapp && npm test` verde antes da sessão.
2. Sessão conduzida sobre catálogo vazio, com o `.app` empacotado.
3. Rodada registrada em `docs/AVALIACAO_UX.md`, achados classificados, nada de UI
   implementado sem aprovação explícita.
</verification>

<success_criteria>
- Critério 3 da Fase 5 respondido com teste real, não com inspeção de código (D-06).
- Escopo de D-04/D-05 respeitado: validação, não redesenho.
- Bloqueadores, se houver, virão como decisão do dono com evidência — não como trabalho silencioso.
</success_criteria>

<output>
Criar `.planning/phases/05-prepara-o-para-lan-amento/05-05-SUMMARY.md` ao terminar.
Commit convencional em português, ex.: `docs(05-05): registra teste de primeira
execução do onboarding`.
</output>
