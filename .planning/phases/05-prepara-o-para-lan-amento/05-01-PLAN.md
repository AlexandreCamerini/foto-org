---
phase: 05-prepara-o-para-lan-amento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_indices.py
  - fotoorganizer/models/catalog.py
  - fotoorganizer/models/inference.py
  - fotoorganizer/models/operations.py
  - fotoorganizer/models/duplicates.py
  - fotoorganizer/models/people.py
  - fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py
  - fotoorganizer/database/engine.py
autonomous: true
requirements: [LANC-02]

must_haves:
  truths:
    - "A consulta real da árvore de pastas (_sob_a_pasta) usa índice (SEARCH), não SCAN de media_files"
    - "A busca textual continua achando um nome gravado em caixa diferente da digitada"
    - "O filtro por prefixo de pasta continua devolvendo exatamente as mesmas linhas de antes"
    - "As 9 novas FKs indexadas e as 4 pré-existentes aparecem no schema de um catálogo migrado do zero (D-10: enumeração completa a partir do modelo, sem coluna sem consumidor real)"
  artifacts:
    - path: "tests/test_indices.py"
      provides: "Asserções de EXPLAIN QUERY PLAN, existência de índice e regressão de caixa"
      min_lines: 60
    - path: "fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py"
      provides: "DDL dos 9 índices novos, com downgrade simétrico"
      contains: "ix_media_files_pasta"
    - path: "fotoorganizer/database/engine.py"
      provides: "PRAGMA case_sensitive_like=ON na conexão"
      contains: "case_sensitive_like"
  key_links:
    - from: "fotoorganizer/models/catalog.py"
      to: "fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py"
      via: "cada Index() declarado no modelo tem create_index correspondente (ou já existe no DB)"
      pattern: "ix_media_files_pasta"
    - from: "fotoorganizer/database/engine.py"
      to: "fotoorganizer/repositories/media.py:_sob_a_pasta"
      via: "PRAGMA de conexão habilita o planner a usar ix_media_files_pasta no LIKE de prefixo"
      pattern: "case_sensitive_like"
---

<objective>
Fechar LANC-02: adicionar os índices de FK ausentes com consumidor real medido,
reconciliar as 4 declarações que existem no banco mas nunca foram espelhadas no
modelo, e — o achado que decide o critério de aceite — habilitar
`PRAGMA case_sensitive_like=ON` para que o índice de `MediaFile.pasta` seja de
fato usado pelo `LIKE 'prefixo/%'` de `_sob_a_pasta`.

Purpose: o critério 2 da fase é "consultas por prefixo de pasta (e demais FKs
hoje sem índice) usam índice, não table scan". Um `Index()` sozinho em `pasta`
cria o índice e mesmo assim mantém `SCAN media_files` — o teste desta fase
afirma o plano de consulta, não a existência do índice.
Output: migração 0018, 5 arquivos de modelo atualizados, PRAGMA na engine e
`tests/test_indices.py` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prepara-o-para-lan-amento/05-RESEARCH.md
@.planning/phases/05-prepara-o-para-lan-amento/05-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código; não explorar o repo. -->

`fotoorganizer/repositories/media.py:157-172` — a consulta que LANC-02 precisa
tornar indexada (note o `or_`: igualdade OU prefixo, ambos sobre `pasta`):

```python
def _sob_a_pasta(prefixo: str):
    limpo = prefixo.rstrip("/")
    seguro = limpo.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")
    return or_(
        MediaFile.pasta == limpo,
        MediaFile.pasta.like(f"{seguro}/%", escape="\\"),
    )
```
Consumidores: `repositories/media.py:288` (filtro da grade) e
`repositories/media.py:415` (árvore de `/api/pastas`, clicada a cada nível).

`fotoorganizer/database/engine.py:11-18` — hook de conexão a estender:

```python
def _set_sqlite_pragmas(dbapi_connection, _record) -> None:
    cursor = dbapi_connection.cursor()
    cursor.execute("PRAGMA journal_mode=WAL")
    cursor.execute("PRAGMA foreign_keys=ON")
    cursor.execute("PRAGMA synchronous=NORMAL")
    cursor.execute("PRAGMA busy_timeout=5000")
    cursor.close()
```

`tests/conftest.py:14-19` — fixture já existente, não criar outra:

```python
@pytest.fixture()
def migrated_engine(db_path):
    from fotoorganizer.database import create_db_engine, upgrade_to_head
    upgrade_to_head(db_path)
    engine = create_db_engine(db_path)
    yield engine
    engine.dispose()
```

`tests/test_repository.py:11-33` — padrão de seed (`Source` + `MediaFile` com
`pasta`, `nome`, `extensao`, `tamanho`, `data_capturada`) a copiar para o seed
do novo teste.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de plano de consulta, existência de índice e regressão de caixa</name>
  <files>tests/test_indices.py</files>
  <read_first>
    - tests/conftest.py (fixtures `db_path` e `migrated_engine` — usar, não recriar)
    - tests/test_database.py (padrão de `text()` cru + `migrated_engine`, ver `test_wal_e_foreign_keys_ativos`; padrão de seed em `test_roundtrip_media_file`)
    - tests/test_repository.py linhas 1-50 (padrão de fixture com `Source`/`MediaFile` e uso de `MediaFilters`)
    - fotoorganizer/repositories/media.py linhas 150-300 (`_sob_a_pasta`, `MediaFilters`, uso de `busca`/`pasta` em `listar`)
    - .planning/phases/05-prepara-o-para-lan-amento/05-PATTERNS.md § `tests/test_indices.py`
  </read_first>
  <behavior>
    - `test_prefixo_de_pasta_usa_indice_nao_scan`: `EXPLAIN QUERY PLAN` da consulta real
      compilada de `_sob_a_pasta("/fotos/a")` mostra `SEARCH` com
      `ix_media_files_pasta` e não contém `SCAN media_files`.
    - `test_indices_declarados_existem_no_schema`: as 13 índices nomeados abaixo
      existem em `sqlite_master` num catálogo migrado do zero.
    - `test_busca_textual_continua_insensivel_a_maiusculas`: mídia gravada como
      `PRAIA.JPG` é encontrada por `MediaFilters(busca="prai")` (guarda a mudança
      de PRAGMA da Task 3).
    - `test_prefixo_de_pasta_nao_vaza_pasta_irma`: com linhas em `/fotos/a`,
      `/fotos/a/sub` e `/fotos/ab`, o filtro por `/fotos/a` devolve as duas
      primeiras e nunca `/fotos/ab`.
  </behavior>
  <action>
    Criar `tests/test_indices.py` com uma fixture local `catalogo_com_pastas(migrated_engine)`
    que usa `create_session_factory(migrated_engine)` e semeia um `Source(caminho="/fotos")`
    mais quatro `MediaFile` com `pasta` igual a `/fotos/a`, `/fotos/a`, `/fotos/a/sub` e
    `/fotos/ab`, um deles com `nome="PRAIA.JPG"` e `extensao="jpg"` — mesmo shape de seed de
    `tests/test_repository.py`. Retornar `MediaRepository(factory)` e a `factory`.

    Para a asserção de plano de consulta, montar o statement real, não SQL escrito à mão:
    `select(MediaFile).where(_sob_a_pasta("/fotos/a"))` (importar `_sob_a_pasta` de
    `fotoorganizer.repositories.media`), compilar com
    `.compile(dialect=sqlite.dialect(), compile_kwargs={"literal_binds": True})`, prefixar a
    string resultante com `EXPLAIN QUERY PLAN ` e executar via `conn.execute(text(...))` sobre
    `migrated_engine.connect()`. Juntar a coluna `detail` (índice 3 de cada linha) num único
    texto e afirmar: contém `SEARCH`, contém `ix_media_files_pasta`, não contém
    `SCAN media_files`. Como `_sob_a_pasta` é um `or_` de igualdade e prefixo, o plano
    esperado é `MULTI-INDEX OR` com dois ramos `SEARCH` sobre o mesmo índice — a asserção deve
    tolerar isso (procurar substring, não igualdade exata da linha).

    Para a existência de índice, parametrizar com `@pytest.mark.parametrize` sobre exatamente
    estes 13 nomes, consultando
    `SELECT name FROM sqlite_master WHERE type='index'`:
    `ix_media_files_pasta`, `ix_media_files_location_id`, `ix_suggestions_media_id`,
    `ix_operation_items_plan_id`, `ix_operation_items_media_id`, `ix_audit_log_plan_id`,
    `ix_duplicate_members_media_id`, `ix_face_occurrences_media_id`,
    `ix_face_occurrences_person_id` (os 9 novos da migração 0018) mais
    `ix_media_files_gps_estimado_de_id`, `ix_media_files_tipo_imagem`,
    `ix_media_files_tipo_confirmado`, `ix_sources_volume_id` (os 4 que já existem no banco
    desde as migrações 0005/0006/0007/0011 e que a Task 2 apenas espelha no modelo).

    Para a regressão de caixa, chamar `repo.listar(MediaFilters(busca="prai"), limit=10, offset=0)`
    e afirmar que `PRAIA.JPG` está no resultado — `busca` compila para `lower(x) LIKE lower(y)`
    no SQLite e deve continuar imune ao PRAGMA da Task 3.

    Para o não-vazamento, chamar `repo.listar(MediaFilters(pasta="/fotos/a"), limit=10, offset=0)`
    e comparar o conjunto de `pasta` devolvido com `{"/fotos/a", "/fotos/a/sub"}`.

    Docstrings em português explicando POR QUE a asserção é sobre o plano de consulta e não
    sobre a existência do índice (citar RESEARCH.md Pitfall 3). Neste ponto o teste DEVE
    falhar — é o RED que prova que os índices ainda não existem.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indices.py -q --no-header; test $? -eq 1 && .venv/bin/python -m pytest tests/test_indices.py::test_busca_textual_continua_insensivel_a_maiusculas -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_indices.py -q --no-header` executa e FALHA (RED esperado), com falha vinda das asserções de índice/plano — não de `ImportError`, `AttributeError` ou erro de fixture.
    - `grep -c "def test_" tests/test_indices.py` retorna 4 ou mais.
    - `grep -c "EXPLAIN QUERY PLAN" tests/test_indices.py` retorna 1 ou mais.
    - `grep -c "_sob_a_pasta" tests/test_indices.py` retorna 1 ou mais (a asserção usa a consulta real, não SQL reescrito à mão).
    - `.venv/bin/python -m pytest tests/test_indices.py::test_busca_textual_continua_insensivel_a_maiusculas -q` PASSA já agora (é guarda, não RED).
  </acceptance_criteria>
  <done>Arquivo de teste existe, roda, e falha exatamente nas asserções de índice/plano de consulta.</done>
</task>

<task type="auto">
  <name>Task 2: Declarações de Index nos modelos + migração 0018</name>
  <files>fotoorganizer/models/catalog.py, fotoorganizer/models/inference.py, fotoorganizer/models/operations.py, fotoorganizer/models/duplicates.py, fotoorganizer/models/people.py, fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py</files>
  <read_first>
    - fotoorganizer/models/catalog.py (linhas 1-140: imports e `MediaFile.__table_args__` atual; linhas 70-95: classe `Source`, hoje sem `__table_args__`)
    - fotoorganizer/models/inference.py (linha 41 `Evidence.__table_args__` e a tupla de `Suggestion`)
    - fotoorganizer/models/operations.py (imports linha 12; classes `OperationItem` e `AuditLog`, hoje sem `__table_args__`)
    - fotoorganizer/models/duplicates.py (imports linha 6; `DuplicateMember.__table_args__`)
    - fotoorganizer/models/people.py (imports linha 12; classes `FaceOccurrence` e `FaceEmbedding`)
    - fotoorganizer/database/migrations/versions/0017_indice_trip_id_event_id.py (arquivo inteiro — template exato a copiar)
    - .planning/phases/05-prepara-o-para-lan-amento/05-PATTERNS.md § Pattern Assignments (código literal por arquivo)
  </read_first>
  <action>
    Adicionar 9 índices novos, cada um declarado no modelo E criado na migração 0018:
    `ix_media_files_pasta` (`media_files.pasta`), `ix_media_files_location_id`
    (`media_files.location_id`), `ix_suggestions_media_id` (`suggestions.media_id`),
    `ix_operation_items_plan_id` e `ix_operation_items_media_id` (`operation_items`),
    `ix_audit_log_plan_id` (`audit_log.plan_id`), `ix_duplicate_members_media_id`
    (`duplicate_members.media_id`), `ix_face_occurrences_media_id` e
    `ix_face_occurrences_person_id` (`face_occurrences`).

    Adicionar 4 declarações de reconciliação de drift — SOMENTE no modelo, SEM
    `create_index` na migração, porque o índice já existe no banco desde as migrações
    0005/0006/0007/0011 e recriá-lo aborta o `upgrade_to_head()`:
    `ix_media_files_gps_estimado_de_id`, `ix_media_files_tipo_imagem`,
    `ix_media_files_tipo_confirmado` (todos em `MediaFile.__table_args__`) e
    `ix_sources_volume_id` (primeiro `__table_args__` da classe `Source`).

    NÃO indexar `FaceEmbedding.person_id`, `MediaTag.tag_id` nem `Trip.location_id`:
    o grep de uso não achou nenhuma cláusula `WHERE`/join filtrando por essas colunas
    hoje, e o precedente escrito no próprio `catalog.py` é "índice quando o custo de
    escrita se justifica por um consumidor real e mensurável" (D-072). Registrar essa
    exclusão explicitamente no docstring da migração, com o motivo — não omitir em
    silêncio.

    Imports a acrescentar: `Index` em `fotoorganizer/models/operations.py`,
    `fotoorganizer/models/duplicates.py` e `fotoorganizer/models/people.py`
    (`catalog.py` e `inference.py` já importam). Manter a ordem alfabética existente
    da tupla de import do SQLAlchemy em cada arquivo.

    Cada `Index(...)` novo leva comentário inline citando o consumidor real com
    arquivo:linha, no mesmo estilo dos comentários já presentes em
    `MediaFile.__table_args__` para `trip_id`/`event_id`. Consumidores por coluna:
    `pasta` → `repositories/media.py:171` (`_sob_a_pasta`) e `repositories/media.py:415`
    (árvore de `/api/pastas`); `location_id` → `repositories/media.py:278,356`;
    `suggestions.media_id` → `classification/engine.py:1042,1083,1106`,
    `server/app.py:724`, `repositories/media.py:118,149`,
    `repositories/suggestions.py:119,296`, `operations/planner.py:60`,
    `operations/inventario.py:161`; `operation_items.plan_id` →
    `repositories/operations.py:71,141` e `operations/executor.py:272`;
    `operation_items.media_id` → `operations/planner.py:68`; `audit_log.plan_id` →
    `repositories/operations.py:95,128` e `operations/executor.py:131,163`;
    `duplicate_members.media_id` → `operations/planner.py:79` e
    `duplicates/detector.py:302` (registrar que o `UniqueConstraint(group_id, media_id)`
    NÃO cobre esse filtro, por `media_id` ser a segunda coluna); `face_occurrences` →
    `repositories/people.py:42,108,109`.

    A migração usa o nome de arquivo `0018_indices_de_fk_ausentes.py`, `revision = '0018'`,
    `down_revision = '0017'`, um bloco `op.batch_alter_table(<tabela>, schema=None)` por
    tabela com `batch_op.create_index(<nome>, [<coluna>], unique=False)`, e `downgrade()`
    simétrico com `drop_index` em ordem inversa — cópia estrutural exata de
    `0017_indice_trip_id_event_id.py`. Docstring em português citando, por índice, qual
    consulta faz SCAN hoje e qual tela/endpoint dispara.

    Não introduzir `Base.metadata.create_all()` em lugar nenhum: o schema vem só de
    `upgrade_to_head()`, e um atalho `create_all()` produziria um banco sem os 4 índices
    de drift.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_database.py tests/test_repository.py -q --no-header && .venv/bin/python -m pytest tests/test_indices.py::test_indices_declarados_existem_no_schema -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_indices.py::test_indices_declarados_existem_no_schema -q` PASSA (13 nomes presentes num catálogo migrado do zero).
    - `.venv/bin/python -m pytest tests/test_database.py tests/test_repository.py -q` PASSA — o `upgrade_to_head()` não quebrou em índice duplicado.
    - `grep -c "create_index" fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py` retorna 9.
    - `grep -c "drop_index" fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py` retorna 9.
    - `grep -E "ix_media_files_gps_estimado_de_id|ix_media_files_tipo_imagem|ix_media_files_tipo_confirmado|ix_sources_volume_id" fotoorganizer/database/migrations/versions/0018_indices_de_fk_ausentes.py | grep -v '^ *#'` não retorna nenhuma linha com `create_index` (os 4 de drift ficam fora da migração).
    - `grep -c "Index(" fotoorganizer/models/catalog.py` retorna 13 ou mais (7 existentes + 2 novos + 3 de drift + o de `Source`).
    - `grep -rn "FaceEmbedding" fotoorganizer/models/people.py | grep -c "Index("` retorna 0.
    - `.venv/bin/python -m pytest tests/test_indices.py::test_prefixo_de_pasta_usa_indice_nao_scan -q` AINDA FALHA — o índice existe mas o planner segue em SCAN; é exatamente o que a Task 3 fecha.
  </acceptance_criteria>
  <done>13 índices no schema migrado, 9 na migração 0018, 4 só no modelo, 3 colunas sem consumidor deliberadamente fora e justificadas no docstring.</done>
</task>

<task type="auto">
  <name>Task 3: PRAGMA case_sensitive_like=ON e fechamento do critério de aceite</name>
  <files>fotoorganizer/database/engine.py</files>
  <read_first>
    - fotoorganizer/database/engine.py (arquivo inteiro, 37 linhas)
    - fotoorganizer/repositories/media.py linhas 150-300 (as três formas de LIKE do código: `.like` em `pasta`, `.ilike` na busca/câmera/país/cidade/palavra-chave)
    - fotoorganizer/scanner/reconciliacao.py linha 86 e fotoorganizer/scanner/scanner.py linha 379 (`not_like` com `%://%`)
    - .planning/phases/05-prepara-o-para-lan-amento/05-RESEARCH.md § Pitfall 3
  </read_first>
  <action>
    Acrescentar `cursor.execute("PRAGMA case_sensitive_like=ON")` em `_set_sqlite_pragmas`,
    depois de `busy_timeout` e antes de `cursor.close()`, com comentário em português
    explicando que a linha é pré-requisito para o planner do SQLite reescrever
    `pasta LIKE 'prefixo/%'` em varredura de faixa sobre `ix_media_files_pasta` — sem ela o
    índice existe e a consulta continua em SCAN.

    O comentário deve registrar a varredura de segurança já feita: `.ilike()` compila para
    `lower(x) LIKE lower(y)` no SQLite e independe deste PRAGMA; `.not_like("%://%")` não tem
    caractere alfabético e já tinha curinga à esquerda (nunca foi elegível a índice); e o
    único `.like()` afetado é o de `pasta`, cujo valor vem de `/api/pastas` (que por sua vez
    lê de `MediaFile.pasta`), nunca de texto digitado livremente pelo usuário.

    Não mexer em `_sob_a_pasta`: a alternativa por `GLOB` foi avaliada e descartada por exigir
    reescrever o escape de `%`/`_`/`\` para o conjunto especial do GLOB, diff maior para o
    mesmo resultado.

    A verificação da task é o comando rápido (pytest dos três arquivos afetados), dentro do
    orçamento de 90s de latência de feedback do `05-VALIDATION.md`. A suíte completa
    (`scripts/verificar.sh --rapido`, que ainda roda pytest inteiro + vitest) é o portão de
    **fim de wave**, conforme § Sampling Rate — rodar uma vez ao fechar o plano, não a cada
    task. Qualquer teste de busca/filtro que regredir nesse portão é sinal de que a varredura
    de Pitfall 3 tinha um ponto cego — investigar antes de seguir, não relaxar a asserção.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indices.py tests/test_database.py tests/test_repository.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_indices.py -q` PASSA inteiro, incluindo `test_prefixo_de_pasta_usa_indice_nao_scan`.
    - Portão de fim de wave (não de task): `scripts/verificar.sh --rapido` sai com código 0.
    - `grep -v '^ *#' fotoorganizer/database/engine.py | grep -c "case_sensitive_like=ON"` retorna 1.
    - `grep -c "GLOB" fotoorganizer/repositories/media.py` retorna 0 (`_sob_a_pasta` intocado).
    - Comando de conferência independente do teste: `.venv/bin/python -c "import tempfile,pathlib;from fotoorganizer.database import create_db_engine,upgrade_to_head;from sqlalchemy import text;d=pathlib.Path(tempfile.mkdtemp())/'c.db';upgrade_to_head(d);e=create_db_engine(d);c=e.connect();print(' '.join(r[3] for r in c.execute(text(\"EXPLAIN QUERY PLAN SELECT * FROM media_files WHERE pasta LIKE '/x/%' ESCAPE '\\\\'\"))))"` imprime texto contendo `SEARCH` e `ix_media_files_pasta`, sem `SCAN media_files`.
  </acceptance_criteria>
  <done>Critério 2 da fase satisfeito: a consulta real da árvore de pastas usa índice, provado por plano de consulta, com suíte inteira verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webapp → API local (`/api/pastas`, `/api/midia`) | Valor de `pasta` e de `busca` atravessam para dentro da cláusula SQL |
| API → SQLite (`catalog.db`) | Mudança de PRAGMA de conexão afeta todas as consultas do processo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `_sob_a_pasta` (`repositories/media.py:157`) | mitigate | Manter intacto o escape de `\`, `%` e `_` já existente; a Task 3 proíbe explicitamente reescrever a função (nenhum `GLOB`), e o critério de aceite verifica por grep que ela não mudou |
| T-05-02 | Information Disclosure | Filtro de busca textual (`.ilike`) | mitigate | `PRAGMA case_sensitive_like=ON` poderia, se `.ilike` dependesse dele, esconder ou revelar linhas diferentes; a Task 1 cria `test_busca_textual_continua_insensivel_a_maiusculas` como guarda permanente |
| T-05-03 | Denial of Service | `upgrade_to_head()` sobre catálogo grande | accept | Criar 9 índices num catálogo de ~400 mil linhas leva minutos e é operação única de migração; não há caminho de usuário que dispare isso repetidamente |
| T-05-SC | Tampering | instalações npm/pip/cargo | mitigate | Nenhum pacote novo é instalado neste plano (RESEARCH.md § Package Legitimacy Audit: não aplicável); se um `pip install` aparecer durante a execução, parar e escalar |

Conforme RESEARCH.md § Security Domain, este plano não abre superfície ASVS
nova: é mudança de tier de armazenamento (schema) mais um PRAGMA de planner,
num app local single-user. Os 8 invariantes do CLAUDE.md seguem intactos —
nenhum arquivo original é tocado, nenhum dado sai da máquina.
</threat_model>

<verification>
1. `scripts/verificar.sh --rapido` verde — portão de fim de wave, rodado
   uma vez ao fechar o plano (§ Sampling Rate do `05-VALIDATION.md`), não por task.
2. `tests/test_indices.py` verde, incluindo a asserção de `SEARCH` sobre a consulta
   real compilada de `_sob_a_pasta`.
3. `alembic` chega a `0018` sem erro de índice duplicado num catálogo criado do zero
   (coberto pela fixture `migrated_engine`).
</verification>

<success_criteria>
- Critério 2 da Fase 5 satisfeito e provado por plano de consulta, não por existência de índice.
- 9 índices novos na migração 0018; 4 declarações de drift reconciliadas sem migração.
- 3 colunas sem consumidor observado ficaram de fora com justificativa escrita.
- Nenhuma regressão de busca, filtro ou agrupamento.
</success_criteria>

<output>
Criar `.planning/phases/05-prepara-o-para-lan-amento/05-01-SUMMARY.md` ao terminar.
Commits pequenos e convencionais em português, ex.:
`test(05-01): asserta plano de consulta da árvore de pastas`,
`perf(05-01): índices de FK ausentes e reconciliação de drift`,
`perf(05-01): case_sensitive_like para o índice de pasta valer`.
</output>
