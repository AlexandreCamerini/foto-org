---
phase: 04-consist-ncia-visual-secund-ria
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - webapp/src/App.tsx
  - webapp/src/App.test.tsx
autonomous: false
requirements: [CONS-06]

must_haves:
  truths:
    - "Abaixo de 1024px a barra da Biblioteca ocupa duas linhas e nenhum controle fica sob o Inspetor"
    - "A partir de 1024px a barra volta a ser uma linha só, idêntica à de hoje"
    - "O Inspetor continua sempre visível — nada colapsa, nada desaparece em nenhuma largura"
    - "Estreitar a janela além de 900px nunca produz uma terceira linha"
  artifacts:
    - path: "webapp/src/App.tsx"
      provides: "Barra da Biblioteca empilhando em 2 grupos abaixo de lg"
      contains: "lg:flex-row"
  key_links:
    - from: "webapp/src/App.tsx (barra da Biblioteca)"
      to: "breakpoint lg do Tailwind (1024px)"
      via: "flex-col por padrão, lg:flex-row a partir de 1024px"
      pattern: "lg:flex-row"
---

<objective>
Fechar CONS-06: abaixo de 1024px a barra superior da Biblioteca empilha em duas
linhas em vez de deixar dropdown, busca e Inspetor se sobrepondo — hoje isso
acontece a partir de ~900px, medido em `docs/AVALIACAO_UX.md` A.3.

Purpose: é o único achado desta fase que não é cosmético — em tela estreita
controles ficam literalmente por baixo do Inspetor e param de ser clicáveis. D-08
decidiu a forma (empilhar a barra, nunca colapsar o Inspetor) e D-09 travou o
breakpoint no token padrão `lg` (1024px), porque não existe nenhum breakpoint
customizado no `webapp/src/` para seguir como precedente — hoje não existe nenhuma
classe responsiva no arquivo, ponto.

Output: a barra passa de um `flex` único de N filhos para dois grupos com intenção
declarada — controles de escopo/identidade na linha 1, controles de visão/ordem na
linha 2 — que voltam a ficar lado a lado a partir de `lg`.

Este plano roda antes do 04-06 (CONS-05) de propósito: os dois tocam `App.tsx`, e
mexer no layout da barra é mais fácil antes de o arquivo ganhar o estado do modal
de "Adicionar pasta".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-CONTEXT.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barra da Biblioteca em dois grupos com retorno a uma linha em lg</name>
  <files>webapp/src/App.tsx, webapp/src/App.test.tsx</files>
  <read_first>
    - webapp/src/App.tsx (bloco `aba === "Biblioteca"`, linhas 294-400 — a barra
      atual: chip de recorte (297-305), toggle Lista/Mapa (309-333), e dentro do
      fragmento `{!noMapa && (<>...)}` o toggle Tudo/Organizáveis/Fora-de-alcance
      (339-364), a busca (365-370), o select de ordenação (371-380), o espaçador
      `flex-1` (381) e o slider de zoom (389-397); e as linhas 432-436, o Inspetor,
      que NÃO muda)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
      (Interaction Contract → "CONS-06 — responsive stacking at `lg` (1024px)",
      linhas 360-385: o agrupamento por intenção e as classes exatas)
    - webapp/src/App.test.tsx (linhas 24-45 e 149-170 — como os testes chegam na
      aba Biblioteca e localizam a busca por placeholder; base para a asserção
      estrutural nova)
    - webapp/src/index.css (para confirmar que nenhum `--breakpoint-*` customizado
      existe no `@theme` — o `lg` usado é o padrão do Tailwind, 1024px)
  </read_first>
  <action>
    Reestruturar SÓ o contêiner da barra da Biblioteca (o `div` que hoje é
    `className="flex items-center gap-2 border-b border-borda px-3 py-2"`) em dois
    grupos, sem alterar nenhum controle interno.

    Contêiner externo: `flex flex-col gap-2 border-b border-borda px-3 py-2
    lg:flex-row lg:items-center lg:gap-2`.

    Grupo 1 — escopo/identidade — `div` com `flex flex-wrap items-center gap-2`,
    contendo, nesta ordem: o chip de recorte (condicional a `recorte`), o toggle
    Lista/Mapa (condicional a `grupoAberto`) e o toggle
    Tudo/Organizáveis/Fora-de-alcance (condicional a `!noMapa`).

    Grupo 2 — visão/ordem — `div` com `flex flex-1 flex-wrap items-center gap-2`,
    contendo: a busca, o select de ordenação, o espaçador `<div className="flex-1" />`
    e o slider de zoom. O grupo 2 inteiro é condicional a `!noMapa` — no mapa não
    há grade e ele ficaria vazio; renderizar um `div` vazio só somaria `gap`.

    Atenção ao refatorar o fragmento `{!noMapa && (<>...)}` de hoje: ele hoje
    envolve o toggle de alcance JUNTO com busca/ordenação/zoom, e esses vão para
    grupos diferentes. O resultado tem duas condições `!noMapa` separadas (uma
    dentro do grupo 1, uma envolvendo o grupo 2), não uma só.

    Nenhuma classe, `title`, `onClick`, `value`, `placeholder` ou ordem interna de
    controle muda. Nenhum controle é removido, escondido ou colapsado em nenhuma
    largura. O Inspetor (linhas 432-436) não é tocado: D-08 é explícito — ele
    continua sempre visível, o custo aceito é altura vertical.

    Não introduzir breakpoint customizado, `container query`, `useMediaQuery` nem
    medição de largura em JS: D-09 trava o token padrão `lg`, e a solução é CSS
    puro via classes utilitárias.

    No teste, adicionar em `App.test.tsx` uma asserção estrutural: na aba
    Biblioteca, o contêiner da barra tem `flex-col` e `lg:flex-row` no className, e
    a busca e o botão "Tudo" NÃO compartilham o mesmo elemento pai (prova de que
    são grupos distintos, o que é o que garante as duas linhas). jsdom não calcula
    layout — a asserção é sobre estrutura e classes, e a prova visual de que nada
    se sobrepõe fica com a Task 2.
  </action>
  <verify>
    <automated>cd webapp &amp;&amp; npx vitest run src/App.test.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'lg:flex-row' webapp/src/App.tsx` retorna 1
    - `grep -c 'flex flex-col gap-2 border-b border-borda px-3 py-2' webapp/src/App.tsx` retorna 1
    - `grep -c 'useMediaQuery\|matchMedia\|ResizeObserver' webapp/src/App.tsx` retorna 0 (solução é CSS, não JS)
    - `grep -c 'w-72' webapp/src/App.tsx` inalterado em relação ao HEAD anterior (Inspetor não mexido)
    - Existe teste em `App.test.tsx` que assere `flex-col` + `lg:flex-row` no contêiner da barra e que busca e "Tudo" têm pais diferentes
    - Todos os testes pré-existentes de `App.test.tsx` continuam verdes (em especial os de limpeza de busca ao trocar de aba/pasta/degrau)
    - `cd webapp && npm run build` exit 0
  </acceptance_criteria>
  <done>A barra tem dois grupos declarados por intenção, empilha abaixo de 1024px e volta a uma linha a partir dali, sem JS de medição e sem tocar no Inspetor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Conferência visual em ~900px e em ≥1024px</name>
  <files>nenhum arquivo alterado — passo de verificação</files>
  <read_first>
    - webapp/src/App.tsx (o resultado da Task 1: o contêiner da barra e os dois
      grupos, para saber o que se espera ver em cada largura)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-CONTEXT.md
      (D-08 e D-09 — o Inspetor nunca colapsa e o breakpoint é `lg` 1024px; é contra
      essas duas decisões que a conferência julga)
  </read_first>
  <action>
    Pausar para verificação humana antes do commit. Subir a UI local com
    `scripts/executar.sh web`, exercitar a Biblioteca com um grupo aberto (a
    configuração mais cheia da barra, a que estourava) nas larguras ~900px, ~700px e
    1200px, e confirmar cada ponto de `<how-to-verify>`. Se o dono apontar
    sobreposição, corrigir na Task 1 e repetir a conferência inteira — não registrar
    aprovação parcial nem prosseguir com ressalva pendente.
  </action>
  <what-built>
    A barra da Biblioteca foi reagrupada em dois blocos (escopo/identidade e
    visão/ordem) que empilham verticalmente abaixo de 1024px e voltam lado a lado a
    partir dali. Nada foi colapsado nem escondido: o Inspetor continua sempre
    visível em qualquer largura, e o custo aceito (D-08) é a barra consumir mais
    altura em tela estreita. A asserção estrutural já está travada por teste
    automatizado; o que jsdom não consegue provar é o que este passo verifica —
    sobreposição real, que depende de layout de verdade.
  </what-built>
  <how-to-verify>
    1. Suba a UI: `scripts/executar.sh web` e abra o endereço local no navegador.
    2. Vá para a aba **Biblioteca** e abra um grupo (clique num card em Viagens, ou
       numa lacuna no Panorama) para que o chip de recorte e o toggle Lista/Mapa
       estejam presentes — é a configuração mais cheia da barra, a que estourava.
    3. Redimensione a janela para aproximadamente **900px** de largura. Confirme:
       - a barra ocupa **duas** linhas, não uma nem três;
       - o campo de busca, o select de ordenação e o slider de zoom estão inteiros e
         clicáveis, nenhum deles por baixo do painel Inspetor à direita;
       - o Inspetor continua visível à direita.
    4. Estreite mais, até ~700px. Confirme que continuam sendo no máximo duas
       linhas (os grupos usam `flex-wrap` internamente, então controles podem
       reposicionar dentro do próprio grupo — o que não pode é surgir uma terceira
       faixa de barra).
    5. Alargue para **1200px**. Confirme que a barra volta a ser uma linha só, com
       a mesma aparência de antes desta mudança (zoom encostado à direita pelo
       espaçador).
    6. Alterne para a vista **Mapa** dentro do grupo aberto em ~900px e confirme que
       a barra não deixou uma faixa vazia extra (o grupo 2 não deve renderizar ali).
  </how-to-verify>
  <acceptance_criteria>
    - Em ~900px: barra em exatamente 2 linhas; busca, ordenação e zoom clicáveis; nenhum controle sob o Inspetor; Inspetor visível
    - Em ~700px: ainda no máximo 2 linhas de barra
    - Em 1200px: barra em 1 linha, zoom à direita, aparência igual à anterior à mudança
    - Na vista Mapa em ~900px: nenhuma faixa vazia extra na barra
    - Veredito do dono registrado no SUMMARY, com as larguras conferidas e a resposta literal ("aprovado" ou o problema descrito)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação de sobreposição exige layout real; jsdom não calcula geometria e a alternativa automatizada (Playwright) exigiria instalar dependência, o que esta fase não autoriza. A cobertura automatizada possível (estrutura + classes) está na Task 1.</automated>
    <human-check>Conferência nas larguras ~900px, ~700px e 1200px conforme `<how-to-verify>`</human-check>
  </verify>
  <done>O dono confirmou que nada se sobrepõe em ~900px, que não há terceira linha em ~700px e que 1200px voltou ao layout de uma linha.</done>
  <resume-signal>Digite "aprovado" ou descreva o que ficou sobreposto/errado, com a largura em que aconteceu</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | O plano reagrupa elementos JSX já existentes e acrescenta classes utilitárias de layout. Nenhum endpoint, nenhuma serialização, nenhuma entrada de usuário nova, nenhum acesso a filesystem, nenhum subprocesso, nenhum dado saindo da máquina. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Denial of Service (auto-infligido: controle inalcançável em tela estreita) | barra da Biblioteca | mitigate | É exatamente a ameaça que o plano FECHA, não abre: hoje busca e ordenação ficam sob o Inspetor abaixo de ~1000px e param de ser clicáveis. Mitigação verificada pela Task 2 em 900px, 700px e 1200px |
| T-04-05-02 | Tampering (refator de JSX derrubar silenciosamente um controle da barra) | `App.tsx` | mitigate | A Task 1 proíbe remover/esconder qualquer controle; o critério de aceite exige que toda a suíte pré-existente de `App.test.tsx` (que exercita busca, troca de aba, pasta e degrau do funil) continue verde, e a Task 2 confere visualmente os seis controles |
| T-04-05-SC | Tampering | npm/pip install | mitigate | Nenhum pacote é instalado; `package.json`/`package-lock.json` fora do diff. Em particular, NÃO adicionar biblioteca de media query nem de layout — D-09 resolve com classe utilitária do Tailwind que já está no projeto |

## Nota sobre a forma deste plano

O guia de planejamento manda separar checkpoint de implementação em planos
distintos. Aqui os dois ficam juntos deliberadamente: a implementação é uma
reestruturação de um único contêiner JSX (custo de contexto baixo), e o critério
de sucesso de CONS-06 — "nada se sobrepõe" — é indemonstrável em jsdom, que não
calcula layout. Separar produziria um plano de uma linha e outro só de espera, sem
ganho de qualidade. A alternativa que dispensaria o humano (teste de browser real)
exigiria instalar Playwright, o que esta fase não autoriza.
</threat_model>

<verification>
1. `cd webapp && npm test` verde.
2. `cd webapp && npm run build` exit 0.
3. Aprovação humana da Task 2 registrada no SUMMARY, com as larguras conferidas.
</verification>

<success_criteria>
- Contêiner da barra com `flex flex-col ... lg:flex-row lg:items-center`
- Dois grupos por intenção; grupo 2 ausente quando `noMapa`
- Zero JS de medição de largura; zero breakpoint customizado
- Inspetor inalterado em qualquer largura
- Teste estrutural em `App.test.tsx` + aprovação visual em 900px/700px/1200px
</success_criteria>

<output>
Create `.planning/phases/04-consist-ncia-visual-secund-ria/04-05-SUMMARY.md` when done
</output>
