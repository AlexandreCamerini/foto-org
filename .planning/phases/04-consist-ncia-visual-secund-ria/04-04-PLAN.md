---
phase: 04-consist-ncia-visual-secund-ria
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - webapp/src/components/Trips.tsx
  - webapp/src/components/Trips.test.tsx
autonomous: true
requirements: [CONS-02]

must_haves:
  truths:
    - "Dois cards da seção Eventos com o mesmo nome mostram, cada um, um selo dizendo se vieram de álbum externo ou de detecção temporal"
    - "Card cujo nome não colide com nenhum outro da seção não ganha selo — o selo é sinal de ambiguidade, não decoração permanente"
    - "Cards da seção Viagens nunca ganham o selo"
  artifacts:
    - path: "webapp/src/components/Trips.tsx"
      provides: "Selo Álbum / Evento detectado no canto superior esquerdo do card"
      contains: "Evento detectado"
  key_links:
    - from: "webapp/src/components/Trips.tsx (Secao)"
      to: "webapp/src/components/Trips.tsx (Card)"
      via: "props secao + colideNome computadas sobre o array itens já buscado"
      pattern: "colideNome"
    - from: "webapp/src/components/Trips.tsx"
      to: "Agrupamento.metodo (webapp/src/api.ts:198)"
      via: "leitura do campo determinístico já serializado pela API"
      pattern: "album_externo"
---

<objective>
Fechar CONS-02: quando dois cards da seção Eventos colidem no nome, cada um mostra
um selo dizendo de onde veio — "Álbum" (importado de catálogo externo) ou "Evento
detectado" (agrupamento temporal do motor).

Purpose: o dono vê dois cards "Aniversário" lado a lado e não tem como saber se são
o mesmo evento duplicado ou dois recortes de origens diferentes. O dado que responde
isso já existe e já chega no cliente: `Agrupamento.metodo` é serializado pela API
(`server/app.py:869`), já está tipado em `webapp/src/api.ts:198`, e nunca foi lido
pelo `Trips.tsx`. Zero mudança de backend.

Output: dois props novos no `Card` (`secao` e `colideNome`), a computação de colisão
no `Secao` (que já tem o array inteiro), e o selo no canto superior esquerdo — mesma
família visual do badge "Mapa" que já mora no canto oposto do mesmo card.

Nota sobre a alternativa descartada: o dono levantou (segunda vez nesta fase) a
ideia de decidir "álbum vs. evento" por LLM em vez do campo determinístico. Ficou
adiada e registrada em `04-CONTEXT.md` § Deferred — D-03 trava o critério
determinístico para esta fase. Não implementar caminho de LLM aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-CONTEXT.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secao computa colisão de nome e discrimina a seção</name>
  <files>webapp/src/components/Trips.tsx</files>
  <read_first>
    - webapp/src/components/Trips.tsx (arquivo inteiro, 184 linhas — em especial
      `Trips` (22-69) que monta as duas `Secao`, `Secao` (71-95) que já tem o array
      `itens` completo, e `Card` (97-172) que recebe hoje só `grupo`/`onAbrir`/`onAbrirMapa`)
    - webapp/src/api.ts (interface `Agrupamento`, linhas 193-201 — `metodo: string`
      já existe e já chega preenchido; nada a mudar aqui)
    - fotoorganizer/grouping/classifier.py (linha ~95, constante `ORIGEM_ALBUM =
      "album_externo"`, e ~137-138 — a origem do valor que o selo interpreta)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
      (Interaction Contract → "CONS-02 — selo álbum/evento", linhas 334-358)
  </read_first>
  <action>
    Em `Trips.tsx`, adicionar à `Secao` a prop `secao: "viagens" | "eventos"` e
    passá-la nas duas chamadas do componente `Trips` (`"viagens"` para a seção
    Viagens, `"eventos"` para a de Eventos). Não derivar a seção do `titulo`: o
    título é texto de UI e um dia muda; o discriminador é explícito.

    Dentro de `Secao`, computar o conjunto de nomes que aparecem mais de uma vez no
    array `itens` (comparação case-insensitive, normalizando com
    `.trim().toLowerCase()`), antes do `.map()`. Passar ao `Card` a prop booleana
    `colideNome` = "o nome deste grupo está nesse conjunto", junto com `secao`.
    A computação é local ao array já buscado — nenhuma requisição nova, nenhum
    campo novo de API.

    O `Card` ganha as duas props na assinatura. Ainda não renderiza nada com elas
    nesta task (o selo é a Task 2) — mas o TypeScript já tem que compilar com as
    props declaradas e passadas.

    Não mexer nos estados `carregando`/`vazio` de `Trips` nesta task: o botão do
    estado vazio é CONS-05, tratado no plano 04-05, e tocar no mesmo trecho aqui
    criaria conflito de merge com ele.
  </action>
  <verify>
    <automated>cd webapp &amp;&amp; npx tsc -b &amp;&amp; npx vitest run src/components/Trips.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'secao: "viagens" | "eventos"' webapp/src/components/Trips.tsx` retorna ao menos 1 (a prop declarada)
    - `grep -c 'colideNome' webapp/src/components/Trips.tsx` retorna ao menos 3 (computação, passagem ao Card, declaração na assinatura do Card)
    - `cd webapp && npx tsc -b` termina com exit code 0
    - Os 8 testes pré-existentes de `Trips.test.tsx` continuam verdes
    - `git diff webapp/src/components/Trips.tsx` não mostra alteração nas linhas do bloco `{vazio && ...}`
  </acceptance_criteria>
  <done>`Secao` sabe qual seção é e quais nomes colidem; `Card` recebe as duas informações; tipos compilam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Selo Álbum / Evento detectado no card</name>
  <files>webapp/src/components/Trips.tsx, webapp/src/components/Trips.test.tsx</files>
  <read_first>
    - webapp/src/components/Trips.tsx (o badge "Mapa" nas linhas 163-169 — a
      família visual exata a reusar: `rounded-full border border-borda bg-janela/80
      px-2 py-0.5 text-texto-2 backdrop-blur-sm`, ancorado com `absolute` + `z-10`
      no canto oposto)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
      (Interaction Contract → CONS-02, linhas 348-358 para o markup travado; e a
      seção Color, linhas 173-179, para a exclusão explícita do acento)
    - webapp/src/components/Duplicates.tsx (linhas 135-142 — o badge "resolvido
      automaticamente" em `border-acento/40 text-acento`: é o precedente ERRADO,
      ler para não imitar)
    - webapp/src/components/Trips.test.tsx (fábrica `grupo({...})` no topo, que já
      define `metodo: "temporal"` por padrão — base dos casos novos)
  </read_first>
  <behavior>
    - Dois eventos com o mesmo nome, um com `metodo: "album_externo"` e outro com
      `metodo: "temporal"`: a tela mostra um selo "Álbum" e um selo "Evento detectado"
    - Dois eventos com nomes diferentes: nenhum selo aparece
    - Duas viagens com o mesmo nome: nenhum selo aparece (o selo é só de Eventos)
    - Nomes que diferem só por caixa/espaço ("Natal" e "natal ") contam como colisão
    - O selo não intercepta o clique do card nem o do badge "Mapa" (os testes
      existentes de navegação continuam passando)
  </behavior>
  <action>
    No `Card`, renderizar o selo condicionalmente a `secao === "eventos" &&
    colideNome`. Posição: canto superior ESQUERDO (`absolute left-2 top-2 z-10`) —
    o direito já é do badge "Mapa" e os dois precisam coexistir sem sobrepor.

    Texto: `grupo.metodo === "album_externo" ? "Álbum" : "Evento detectado"` — a
    regra determinística de D-03, sem lista de exceções e sem consultar mais nada.

    Classes: `rounded-full border border-borda bg-janela/80 px-2 py-0.5
    text-[11px] text-texto-2 backdrop-blur-sm`. É a mesma família do badge "Mapa" do
    próprio arquivo. Deliberadamente NÃO usar `border-acento`/`text-acento`: pela
    direção de arte (D-017) o acento é reservado a seleção, foco, progresso e CTA
    primário — um rótulo informativo não é nenhum dos quatro. O badge acentuado de
    `Duplicates.tsx` é dívida anterior, fora de escopo para corrigir e fora de
    escopo para imitar.

    Elemento: `<span>`, não `<button>` — o selo não é clicável. Posicioná-lo como
    IRMÃO do `div role="button"` do card (mesmo nível do badge "Mapa"), nunca
    dentro dele: conteúdo dentro de um `role="button"` vaza para o nome acessível
    do card, que é exatamente o anti-padrão ARIA que o comentário nas linhas
    113-118 documenta ter sido corrigido.

    Nos testes, cobrir os quatro casos de `<behavior>` acima usando a fábrica
    `grupo({ nome, metodo })` já existente, servindo `/api/eventos` e `/api/viagens`
    com as rotas simuladas do padrão do arquivo.
  </action>
  <verify>
    <automated>cd webapp &amp;&amp; npx vitest run src/components/Trips.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Evento detectado' webapp/src/components/Trips.tsx` retorna 1
    - `grep -c 'album_externo' webapp/src/components/Trips.tsx` retorna 1
    - `grep -c 'text-acento\|border-acento' webapp/src/components/Trips.tsx` retorna 0
    - `grep -c 'left-2 top-2' webapp/src/components/Trips.tsx` retorna 1 (selo à esquerda, badge Mapa à direita intocado)
    - `Trips.test.tsx` tem testes para: colisão em Eventos mostrando os dois rótulos; ausência de colisão sem selo; colisão em Viagens sem selo; colisão case-insensitive
    - Os testes pré-existentes de navegação por card e por badge Mapa (incluindo o de foco/Tab independentes) continuam verdes
    - `cd webapp && npm run build` exit 0
  </acceptance_criteria>
  <done>O selo aparece só onde há ambiguidade real, diz a origem certa pelo campo determinístico, e não rouba clique nem nome acessível do card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | O plano lê um campo (`Agrupamento.metodo`) que a API local já serializa e que o cliente já recebe e tipa. Nenhum endpoint novo, nenhum parâmetro novo, nenhuma requisição nova, nenhuma entrada de usuário nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing (rótulo afirma uma origem que o dado não sustenta) | selo Álbum/Evento detectado | mitigate | O selo é função pura e total de `metodo`: `album_externo` → "Álbum", qualquer outro valor → "Evento detectado". Não há caminho em que o selo invente origem, e um `metodo` desconhecido no futuro degrada para o rótulo genérico em vez de sumir ou quebrar. Coerente com o modelo de evidências do projeto: a afirmação da UI é rastreável a um campo do catálogo |
| T-04-04-02 | Information Disclosure | conteúdo do selo | accept | "Álbum" / "Evento detectado" não expõe caminho, fonte, id nem metadado sensível; é menos informação do que o card já mostra (nome, período, contagem) |
| T-04-04-SC | Tampering | npm/pip install | mitigate | Nenhum pacote é instalado; `package.json`/`package-lock.json` fora do diff |

Invariantes do `CLAUDE.md`: nada é escrito no catálogo nem no disco; nenhum
subprocesso; nada sai da máquina. Plano exclusivamente de leitura e apresentação.
</threat_model>

<verification>
1. `cd webapp && npm test` verde.
2. `cd webapp && npm run build` exit 0.
3. `git diff --name-only` mostra só `Trips.tsx` e `Trips.test.tsx` — em especial
   nenhuma alteração em `webapp/src/api.ts` nem em `fotoorganizer/server/app.py`,
   que é a prova de que CONS-02 não precisou de backend.
</verification>

<success_criteria>
- Selo aparece só na seção Eventos e só em cards com nome colidido
- Rótulo derivado de `metodo === "album_externo"`, sem lógica adicional
- Família visual do badge "Mapa", sem acento
- Quatro casos cobertos por teste
- `scripts/verificar.sh` verde
</success_criteria>

<output>
Create `.planning/phases/04-consist-ncia-visual-secund-ria/04-04-SUMMARY.md` when done
</output>
