---
phase: 04-consist-ncia-visual-secund-ria
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - webapp/src/components/Loupe.tsx
  - webapp/src/components/Duplicates.tsx
  - webapp/src/components/Loupe.test.tsx
  - webapp/src/components/Duplicates.test.tsx
autonomous: true
requirements: [CONS-04]

must_haves:
  truths:
    - "D-06: Prévia em alta resolução que falha no Loupe mostra estado de erro explícito, com o glifo ⊘ e duas linhas de explicação"
    - "Membro de grupo de duplicatas cuja prévia falha mostra 'imagem indisponível' com a explicação completa no title, nunca ícone quebrado do browser nem retângulo preto"
    - "Trocar de foto no Loupe reseta o estado de erro — a foto seguinte não herda a falha da anterior"
  artifacts:
    - path: "webapp/src/components/Loupe.tsx"
      provides: "Estado de erro de prévia em tela cheia"
      contains: "onError"
    - path: "webapp/src/components/Duplicates.tsx"
      provides: "Subcomponente por membro com estado de falha próprio"
      contains: "onError"
    - path: "webapp/src/components/Loupe.test.tsx"
      provides: "Teste do estado de erro e do reset ao navegar"
      min_lines: 40
  key_links:
    - from: "webapp/src/components/Loupe.tsx"
      to: "api.previewUrl"
      via: "onError do <img> principal alterna para o bloco de erro"
      pattern: "onError"
    - from: "webapp/src/components/Duplicates.tsx"
      to: "api.previewUrl"
      via: "onError por membro, estado local do subcomponente extraído"
      pattern: "onError"
---

<objective>
Fechar CONS-04: imagem em alta resolução que retorna 404 deixa de virar retângulo
preto (Duplicatas) ou ícone quebrado do browser (Loupe) e passa a ter estado de
erro explícito, no vocabulário visual que o app já estabeleceu.

Purpose: o app já sabe dizer "não alcanço esta imagem" em três lugares
(`Miniatura.tsx`, `Trips.tsx` card sem capa, e o `motivo_indisponivel` que o
backend já serializa) — sempre com o glifo `⊘` e uma frase. As duas telas de
prévia grande ficaram de fora e falham mudas. Um evento inteiro num volume
desmontado já fez o dono concluir que o sistema estava quebrado quando ele estava
apenas calado; este plano fecha os dois últimos pontos onde isso ainda acontece.

Output: estado de erro dedicado por contexto (D-06 autoriza tratamentos diferentes
entre Loupe e Duplicatas), com markup e cópia travados no `04-UI-SPEC.md`.

Escopo negativo explícito: a faixa de miniaturas do rodapé do Loupe
(`api.thumbUrl`) NÃO entra — CONS-04 fala de "imagem em alta resolução", e as
miniaturas já têm o tratamento do `Miniatura.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-CONTEXT.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
@.planning/phases/04-consist-ncia-visual-secund-ria/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estado de erro da prévia em tela cheia (Loupe)</name>
  <files>webapp/src/components/Loupe.tsx, webapp/src/components/Loupe.test.tsx</files>
  <read_first>
    - webapp/src/components/Loupe.tsx (arquivo inteiro, 98 linhas — o `useEffect`
      de troca de índice nas linhas 20-26, que já reseta `setZoom100(false)`, é
      onde o reset do novo estado entra; o `<img>` principal nas linhas 63-72; o
      wrapper com `onClick` de zoom nas linhas 57-62)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
      (Interaction Contract → "CONS-04 — broken-image state", linhas 235-271:
      markup, classes e as duas frases estão travados lá; e a Copywriting Contract,
      linha 225, com a cópia exata)
    - webapp/src/components/Trips.tsx (linhas 106-148 — o precedente de
      comportamento: `useState` + `onError` + bloco com `⊘`; D-06 diz para NÃO
      replicar literalmente, é referência de comportamento, não de layout)
    - webapp/src/components/Miniatura.tsx (a terceira ocorrência do vocabulário
      `⊘` + `bg-cartao` — confirma que o glifo é a convenção do app)
    - webapp/src/components/Trips.test.tsx (o teste "capa que falha ao carregar
      degrada para o mesmo aviso honesto" — o padrão de disparar `error` num
      `<img>` no vitest, a copiar para o teste novo)
  </read_first>
  <behavior>
    - Com a prévia carregando normalmente, o `<img>` de `api.previewUrl(media.id)` está no documento
    - Disparado o evento `error` nesse `<img>`, ele sai do documento e aparecem as
      duas frases: "Não foi possível carregar esta imagem em alta resolução." e
      "O arquivo pode ter sido movido, renomeado ou corrompido desde a catalogação."
    - No estado de erro, o glifo `⊘` está presente e marcado `aria-hidden`
    - No estado de erro, clicar na área central não alterna o zoom (nada a ampliar)
    - Navegar para outro índice (`onNavegar`) volta ao `<img>`: o estado de falha não persiste entre fotos
    - Cabeçalho (nome/data/contador) e rodapé (faixa de miniaturas) seguem renderizando nos dois estados
  </behavior>
  <action>
    Em `Loupe.tsx`, adicionar `const [falhouPreview, setFalhouPreview] =
    useState(false)` e resetá-lo para `false` dentro do `useEffect` que já roda a
    cada mudança de `index` (o mesmo que faz `setZoom100(false)`) — sem isso, uma
    foto quebrada contamina a próxima.

    Trocar o `<img>` principal por um condicional em `falhouPreview`:
    - ramo de erro: um `div` centralizado (`flex flex-1 flex-col items-center
      justify-center gap-3 text-center`) com três filhos — o glifo `⊘` em
      `span aria-hidden className="text-5xl text-texto-3"`, a frase primária em
      `text-texto-2` e a frase secundária em `text-texto-3`. As duas frases são as
      da Copywriting Contract, verbatim, sem reescrever.
    - ramo normal: o `<img>` de hoje, acrescido de `onError={() =>
      setFalhouPreview(true)}`. `src`, `alt`, `draggable` e o `className`
      condicional de zoom ficam exatamente como estão.

    Guardar o toggle de zoom do wrapper: `onClick={() => !falhouPreview &&
    setZoom100((v) => !v)}` — com a imagem falha não há o que ampliar, e alternar
    `overflow-auto`/`items-center` sob um bloco de texto só produz salto de layout.

    Não tocar no `<footer>` da faixa de miniaturas (linhas 75-95): elas usam
    `api.thumbUrl` e estão fora do escopo de CONS-04.

    Criar `webapp/src/components/Loupe.test.tsx` (não existe ainda) seguindo o
    padrão dos testes de componente do projeto: `montar()` de
    `webapp/src/test/servidor.tsx`, `screen`/`fireEvent` do testing-library.
    O Loupe recebe `itens`, `index`, `onNavegar`, `onFechar` por prop — não precisa
    de rota simulada para o `<img>`, basta disparar o evento `error` no elemento
    (o jsdom não busca a imagem de verdade). Para o teste de reset, remontar com
    `index` diferente via `rerender` ou montar com um wrapper que troque o índice.
  </action>
  <verify>
    <automated>cd webapp &amp;&amp; npx vitest run src/components/Loupe.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/Loupe.test.tsx` existe e cobre: (a) prévia ok, (b) prévia com erro mostrando as duas frases e o `⊘`, (c) reset ao mudar de índice
    - `grep -c 'Não foi possível carregar esta imagem em alta resolução.' webapp/src/components/Loupe.tsx` retorna 1
    - `grep -c 'O arquivo pode ter sido movido, renomeado ou corrompido desde a catalogação.' webapp/src/components/Loupe.tsx` retorna 1
    - `grep -c 'thumbUrl' webapp/src/components/Loupe.tsx` retorna 1 (a faixa de miniaturas segue intocada)
    - O teste falha se `onError` for removido do `<img>` de prévia (verificar removendo temporariamente e revertendo)
    - `cd webapp && npx vitest run src/components/Loupe.test.tsx` verde
  </acceptance_criteria>
  <done>O Loupe explica a falha em duas linhas com o glifo do app, não amplia o que não existe, e esquece a falha ao navegar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estado de erro por membro na comparação de Duplicatas</name>
  <files>webapp/src/components/Duplicates.tsx, webapp/src/components/Duplicates.test.tsx</files>
  <read_first>
    - webapp/src/components/Duplicates.tsx (linhas 165-220 — a grade de comparação
      e o `<figure>` inline dentro do `.map()`, que é o alvo da extração; note que o
      `.map()` não pode segurar `useState` por membro como está)
    - .planning/phases/04-consist-ncia-visual-secund-ria/04-UI-SPEC.md
      (Interaction Contract → "Duplicates (`Duplicates.tsx`)", linhas 272-305:
      markup, classes, o `title` e a troca de `bg-black` por `bg-cartao`)
    - webapp/src/components/Trips.tsx (linhas 97-148 — `Card` é o formato de
      referência: um `useState` por instância renderizada, sem mapa keyed, já que o
      React keya cada saída do `.map()`)
    - webapp/src/components/Duplicates.test.tsx (linhas 1-100 — fábricas `membro`,
      `grupo` e as rotas simuladas; o teste novo se encaixa nesse padrão)
  </read_first>
  <behavior>
    - Membro com prévia ok renderiza `<img>` com `className` contendo `bg-black object-contain`
    - Disparado `error` no `<img>` de um membro, aquele card mostra "imagem indisponível" e o glifo `⊘`
    - O bloco de erro carrega `title="O arquivo pode ter sido movido, renomeado ou corrompido desde a catalogação."`
    - O bloco de erro usa `bg-cartao`, não `bg-black`
    - A falha de um membro NÃO afeta os outros membros do mesmo grupo (estado é por membro)
    - A borda por papel do `<figure>` (`border-ok` para principal, `border-borda opacity-50` para ignorado) continua igual no estado de erro
    - O `<figcaption>` com nome, tamanho e botão de papel continua renderizando no estado de erro
  </behavior>
  <action>
    Extrair o corpo do `.map()` da grade de comparação para um subcomponente
    `MembroFigura` no mesmo arquivo (mesma convenção de `Trips.tsx`, que define
    `Secao`/`Card` abaixo do componente principal). O subcomponente recebe o membro,
    o `grupo.id`, o `grupo.nivel` e o callback de ação já existente, e mantém dentro
    de si `const [falhouPreview, setFalhouPreview] = useState(false)` — é a razão da
    extração: hook não roda dentro de `.map()`.

    Dentro de `MembroFigura`, condicionar só a área da imagem (o `<figure>`,
    `<figcaption>` e a borda por papel ficam fora do condicional):
    - ramo de erro: `div` com `title` (a frase completa, a mesma segunda linha do
      Loupe) e `className="flex aspect-[4/3] w-full flex-col items-center
      justify-center gap-1 bg-cartao text-texto-3"`, contendo `span aria-hidden
      className="text-2xl"` com `⊘` e `span className="text-texto-2"` com
      "imagem indisponível".
    - ramo normal: o `<img>` de hoje mais `onError={() => setFalhouPreview(true)}`.

    O fundo troca de `bg-black` para `bg-cartao` no erro de propósito: `bg-black` é
    letterbox de imagem carregada com `object-contain`; mantê-lo no erro leria como
    "ainda carregando". `bg-cartao` é a cor de superfície vazia/quebrada que
    `Miniatura.tsx` já estabeleceu.

    Não repetir o nome do arquivo dentro do bloco de erro — o `<figcaption>` logo
    abaixo já mostra `m.nome`. Não trocar a cópia por outra frase: "imagem
    indisponível" é a visível, e a explicação longa vive no `title` para o card
    continuar escaneável em 2-3 colunas.

    No teste, montar um grupo com dois membros, disparar `error` no `<img>` do
    primeiro e assertar que só ele degrada (o segundo `<img>` continua no documento)
    — é a asserção que prova que o estado é por membro e não global.
  </action>
  <verify>
    <automated>cd webapp &amp;&amp; npx vitest run src/components/Duplicates.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function MembroFigura' webapp/src/components/Duplicates.tsx` retorna 1
    - `grep -c 'imagem indisponível' webapp/src/components/Duplicates.tsx` retorna 1
    - `grep -c 'bg-cartao text-texto-3' webapp/src/components/Duplicates.tsx` retorna 1
    - Existe teste em `Duplicates.test.tsx` que dispara `error` num membro e assere que o outro membro do mesmo grupo continua com `<img>` no documento
    - Existe teste que assere o `title` com a explicação completa no bloco de erro
    - Os 5 testes pré-existentes de `Duplicates.test.tsx` continuam passando
    - `cd webapp && npm run build` exit 0 (a extração do subcomponente não quebra tipos)
  </acceptance_criteria>
  <done>Cada membro degrada sozinho, com o glifo e a cor de superfície vazia do app, e a explicação completa a um hover de distância.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor local → `<img>` do webapp (já existente) | As duas telas já consomem `GET /api/preview/{id}`. O plano trata a resposta de falha no cliente; não muda o endpoint, o que ele serve, nem quem pode chamá-lo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | frases de erro do Loupe/Duplicatas | mitigate | A cópia travada no `04-UI-SPEC.md` é genérica por design ("movido, renomeado ou corrompido") — não ecoa caminho absoluto, mensagem do sistema de arquivos, stack trace nem código HTTP. Não acrescentar detalhe técnico à mensagem: o `<figcaption>`/cabeçalho já mostram o nome do arquivo no nível de exposição que o app já tinha |
| T-04-03-02 | Spoofing (usuário confunde "não alcanço agora" com "arquivo perdido/apagado") | ambos os estados de erro | mitigate | A frase diz "pode ter sido movido, renomeado ou corrompido", não afirma perda. Alinhado ao invariante 8 do `CLAUDE.md`: nada que possa ser a referência real de uma foto é apagado, e a UI não pode sugerir que foi |
| T-04-03-SC | Tampering | npm/pip install | mitigate | Nenhum pacote é instalado; `package.json`/`package-lock.json` fora do diff |

Nenhum invariante de segurança do `CLAUDE.md` é tocado: nada é lido do filesystem
por este código, nenhum subprocesso, nenhum dado sai da máquina, nenhuma operação
física é criada ou alterada.
</threat_model>

<verification>
1. `cd webapp && npm test` verde.
2. `cd webapp && npm run build` exit 0.
3. `grep -rn '⊘' webapp/src/components --include='*.tsx' | grep -v '\.test\.'`
   mostra 4 arquivos (`Miniatura.tsx`, `Trips.tsx`, `Loupe.tsx`, `Duplicates.tsx`)
   — o vocabulário do app estendido aos dois novos call sites, não substituído.
</verification>

<success_criteria>
- Loupe: estado de erro em duas linhas, glifo `⊘`, zoom guardado, reset ao navegar
- Duplicatas: `MembroFigura` extraído, erro por membro, `bg-cartao`, `title` com a explicação
- Faixa de miniaturas do Loupe intocada
- Testes novos em `Loupe.test.tsx` (arquivo novo) e `Duplicates.test.tsx`
- `scripts/verificar.sh` verde
</success_criteria>

<output>
Create `.planning/phases/04-consist-ncia-visual-secund-ria/04-03-SUMMARY.md` when done
</output>
