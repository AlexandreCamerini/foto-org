---
phase: 05-prepara-o-para-lan-amento
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/medir_baseline_producao.py
  - docs/PERFORMANCE.md
autonomous: false
requirements: [LANC-04]

must_haves:
  truths:
    - "Existe um script reproduzível que mede as três métricas de LANC-04 e imprime os números"
    - "As três métricas foram medidas contra o acervo real, não contra fixture sintética"
    - "A geração de sugestões e a detecção de duplicatas foram medidas sem deixar lote não revisado no catálogo de produção"
    - "A metodologia está escrita junto dos números, de forma que a próxima medição seja comparável (D-07: acervo real, não fixture; D-08: as três métricas nomeadas; D-09: registrado em docs/PERFORMANCE.md)"
  artifacts:
    - path: "scripts/medir_baseline_producao.py"
      provides: "Medição cronometrada de varredura, geração de sugestões e detecção de duplicatas"
      min_lines: 80
    - path: "docs/PERFORMANCE.md"
      provides: "Baseline datado, com metodologia e composição do acervo medido"
      contains: "Metodologia"
  key_links:
    - from: "scripts/medir_baseline_producao.py"
      to: "fotoorganizer.scanner.CatalogScanner.scan_source"
      via: "chamada direta cronometrada com time.monotonic()"
      pattern: "scan_source"
    - from: "scripts/medir_baseline_producao.py"
      to: "SuggestionEngine.gerar / DuplicateDetector.detectar"
      via: "chamada direta sobre uma cópia descartável do catálogo"
      pattern: "gerar\\(\\)|detectar\\("
---

<objective>
Fechar LANC-04: medir taxa de indexação, tempo de geração de sugestões e tempo de
detecção de duplicatas contra o acervo real e registrar os números em
`docs/PERFORMANCE.md`, no padrão acumulativo de `docs/AVALIACAO_UX.md`.

Purpose: sem baseline documentado não existe medida de regressão. `catalog.db` foi
zerado em 2026-08-16 e ainda não rodou varredura completa nova — essa rescan é a
própria oportunidade de medição (D-07), não um passo extra.
Output: `scripts/medir_baseline_producao.py` reproduzível e a primeira seção datada
de `docs/PERFORMANCE.md`.

Decisões de execução tomadas neste plano (eram Claude's Discretion no CONTEXT.md;
ficam registradas em `docs/PERFORMANCE.md` para a próxima medição repetir):
- **P-1:** a varredura é medida **no catálogo real, in place** — é a rescan que D-07
  descreve.
- **P-2:** geração de sugestões e detecção de duplicatas são medidas numa **cópia
  descartável** do catálogo recém-varrido. Ambas são recomputação total com escrita
  como efeito colateral (`Suggestion`, `DuplicateGroup`, `DuplicateMember`); medir
  in place deixaria a produção com um lote de sugestões auto-geradas que ninguém
  pediu nem revisou.
- **P-3:** medição 100% local, `advisor=None`. O advisor LLM é opt-in
  (`privacidade.servicos_externos`, `server/jobs.py:280-290`); ligá-lo tornaria a
  medida dependente de latência de rede e de custo por token, e não reprodutível.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prepara-o-para-lan-amento/05-RESEARCH.md
@.planning/phases/05-prepara-o-para-lan-amento/05-PATTERNS.md

<interfaces>
<!-- Assinaturas e caminhos exatos. Não explorar o repo para descobri-los. -->

Padrão de cronometragem a copiar (`fotoorganizer/cli.py:600-611`, `cmd_bench`):
```python
inicio = time.monotonic()
_, m1 = scanner.scan_source(fotos)
frio = time.monotonic() - inicio
print(f"Indexação a frio : {m1.indexados} arquivos em {frio:.2f}s "
      f"({m1.indexados / frio:.0f} arq/s)")
```

Padrão de script solto (`scripts/avaliar_agrupamento.py:1-19`): docstring com
`Uso: .venv/bin/python scripts/<nome>.py`, `from __future__ import annotations`,
`sys.path.insert(0, str(Path(__file__).parent.parent))`, import direto de
`fotoorganizer.*`. Não é subcomando de `cli.py`, não entra no `_build_parser()`.

Construção do scanner (`fotoorganizer/cli.py:187-208`, `_build_scanner`):
`upgrade_to_head(db_path)` → `create_db_engine` → `create_session_factory` →
`CatalogScanner(factory, criar_extrator(), settings.scanner, thumb_cache=ThumbnailCache(settings.cache_dir))`.
`settings` é obrigatório de propósito.

Construção dos dois motores (`fotoorganizer/server/jobs.py:182-250`):
```python
engine = SuggestionEngine(factory, LocationResolver(OfflineGeocoder()),
                          template=template, advisor=..., lexico=lexico)
resultado = engine.gerar()          # -> dict
detector = DuplicateDetector(factory, ThumbnailCache(cache_dir))
stats = detector.detectar(progress=...)   # -> dict
```
`template` vem de `SettingsRepository(factory).obter_template(TEMPLATE_PADRAO)`;
`lexico` de `LexicoRepository(factory).conhecidos()`.

Config: `Settings.db_path` = `paths.default_db_path(data_dir)`; `data_dir` padrão
`~/Library/Application Support/FotoOrganizer`; override por `FOTOORG_DATA_DIR`
(que também isola `cache_dir`). Carregar com `fotoorganizer.config.load_settings()`.

Fontes do catálogo anterior ao reset (para saber o que rescanear), em
`~/Library/Application Support/FotoOrganizer/catalog-antes-do-reset-20260816-013503.db`,
tabela `sources` (`caminho`, `tipo`, `disponivel`). Tipos `APPLE_PHOTOS` e
`LIGHTROOM` são importadores, não varredura de pasta — só `PASTA` entra na medida
de taxa de indexação.

Estrutura acumulativa a espelhar (`docs/AVALIACAO_UX.md`): o arquivo cresce por
seções `# ` datadas, uma por rodada; rodadas antigas não são reescritas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de medição reproduzível</name>
  <files>scripts/medir_baseline_producao.py</files>
  <read_first>
    - fotoorganizer/cli.py linhas 155-260 (`_settings`, `_abrir_catalogo`, `_build_scanner`, `cmd_scan`) e 560-612 (`cmd_bench`)
    - scripts/avaliar_agrupamento.py linhas 1-30 (forma de script solto)
    - fotoorganizer/server/jobs.py linhas 182-250 e 280-290 (construção dos motores e o opt-in do advisor)
    - fotoorganizer/config/settings.py linhas 60-100 (`data_dir`, `cache_dir`, `db_path`, `ensure_dirs`)
    - .planning/phases/05-prepara-o-para-lan-amento/05-PATTERNS.md § `scripts/medir_baseline_producao.py`
  </read_first>
  <action>
    Criar `scripts/medir_baseline_producao.py` com docstring em português no formato de
    `scripts/avaliar_agrupamento.py`, explicando por que ele existe (baseline formal de
    LANC-04) e por que ele é o oposto de `fotoorganizer bench` (aponta para dado real, em
    vez de fixture sintética isolada).

    Interface de linha de comando, via `argparse`:
    - `--listar-fontes [CAMINHO_DO_BACKUP]`: lê a tabela `sources` do catálogo de backup
      (default `~/Library/Application Support/FotoOrganizer/catalog-antes-do-reset-20260816-013503.db`)
      e imprime, por fonte, `id`, `caminho`, `tipo`, e se o caminho existe agora
      (`Path(...).exists()`); marca `APPLE_PHOTOS`/`LIGHTROOM` como "importador, fora da
      medida de varredura". Modo somente leitura, não abre o catálogo ativo.
    - `--pasta P` (repetível): raízes a varrer, na ordem dada.
    - `--data-dir D`: sobrepõe o `data_dir` das settings; sem ele usa o real de produção.
    - `--pular-varredura`: mede só sugestões e duplicatas sobre o catálogo já existente.
    - `--saida CAMINHO`: onde gravar o bloco markdown (default: imprime no stdout).

    Fluxo do modo de medição, nesta ordem:
    1. Carregar settings com `load_settings()`, aplicar `--data-dir` se veio, chamar
       `ensure_dirs()`, e registrar `settings.db_path` no relatório.
    2. Para cada `--pasta`, montar o scanner igual a `_build_scanner` e cronometrar
       `scan_source(pasta, progress=<callback que imprime a cada N arquivos>)` com
       `time.monotonic()`. Acumular `indexados`, `pulados`, `erros`, `bytes_processados`
       e segundos por pasta e no total; taxa em arq/s.
    3. Fechar o engine da varredura e copiar `settings.db_path` para
       `<data_dir>/baseline-<AAAAMMDD-HHMMSS>.db` com `shutil.copy2`, junto dos arquivos
       `-wal` e `-shm` se existirem. É a cópia descartável da decisão P-2. Nunca apagar,
       nunca mover o catálogo real (invariante 8 do CLAUDE.md).
    4. Sobre a CÓPIA: `upgrade_to_head` + engine + factory; cronometrar
       `SuggestionEngine(factory, LocationResolver(OfflineGeocoder()),
       template=SettingsRepository(factory).obter_template(TEMPLATE_PADRAO),
       advisor=None, lexico=LexicoRepository(factory).conhecidos()).gerar()`. O `advisor=None`
       é literal e não configurável por flag — decisão P-3.
    5. Ainda sobre a cópia: cronometrar
       `DuplicateDetector(factory, ThumbnailCache(settings.cache_dir)).detectar(progress=<print de etapa>)`.
    6. Emitir um bloco markdown pronto para colar em `docs/PERFORMANCE.md`, com: caminho do
       catálogo medido, total de registros em `media_files`, pastas varridas e pastas
       indisponíveis puladas, as três métricas em tabela, e os dicionários de resultado de
       `gerar()`/`detectar()` resumidos.

    Robustez obrigatória: pasta inexistente vira aviso e continua (mesmo princípio do
    scanner — erro de leitura nunca derruba a varredura); `Ctrl-C` durante uma medição
    imprime o que já foi medido antes de sair; nenhum caminho do script apaga arquivo.

    Testar o script antes de rodá-lo contra o acervo: com `--data-dir` num diretório
    temporário e `--pasta` apontando para uma pasta de JPEGs sintéticos gerados por Pillow,
    o script inteiro deve completar em segundos e imprimir as três métricas. Esse é o
    verify automatizado da task.
  </action>
  <verify>
    <automated>TMP=$(mktemp -d) && .venv/bin/python -c "from PIL import Image; import pathlib; d=pathlib.Path('$TMP/fotos/a'); d.mkdir(parents=True); [Image.new('RGB',(64,48),(i*7%256,i*13%256,i*29%256)).save(d/f'f{i}.jpg') for i in range(6)]" && .venv/bin/python scripts/medir_baseline_producao.py --data-dir "$TMP/dados" --pasta "$TMP/fotos" | tee "$TMP/saida.md" && grep -qi "arq/s" "$TMP/saida.md" && grep -qi "duplicat" "$TMP/saida.md" && echo "script ok"</automated>
  </verify>
  <acceptance_criteria>
    - O comando de verify acima sai com código 0 e a saída contém as três métricas (taxa em `arq/s`, tempo de geração de sugestões, tempo de detecção de duplicatas).
    - `.venv/bin/python scripts/medir_baseline_producao.py --listar-fontes` imprime as 10 fontes do backup com `tipo` e disponibilidade, sem tocar no catálogo ativo.
    - `grep -v '^ *#' scripts/medir_baseline_producao.py | grep -c "advisor=None"` retorna 1.
    - `grep -cE "os\.remove|shutil\.rmtree|unlink|DROP TABLE|DELETE FROM" scripts/medir_baseline_producao.py` retorna 0.
    - `grep -c "shutil.copy2" scripts/medir_baseline_producao.py` retorna 1 ou mais (a cópia descartável de P-2).
    - Após rodar o verify, `ls $TMP/dados/baseline-*.db` existe e o `catalog.db` original em `$TMP/dados` continua presente.
  </acceptance_criteria>
  <done>Script mede as três métricas de ponta a ponta em ambiente descartável, sem nenhum caminho destrutivo.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Quais raízes entram na rescan medida</name>
  <files>(nenhum arquivo alterado — decisão do dono)</files>
  <action>Rodar `.venv/bin/python scripts/medir_baseline_producao.py --listar-fontes`, apresentar a lista com tipo e disponibilidade atual de cada fonte, e pedir a escolha entre as opções abaixo. Não iniciar a varredura da Task 3 sem a resposta.</action>
  <decision>
    Quais fontes do catálogo anterior ao reset serão varridas nesta medição de baseline.
  </decision>
  <context>
    O baseline precisa ser "contra um catálogo de tamanho representativo" (critério 4 da
    fase) e D-07 trava que a medida é contra o acervo real. As 10 fontes registradas no
    backup `catalog-antes-do-reset-20260816-013503.db` não são equivalentes entre si:
    duas são importadores (Apple Fotos, Lightroom) e não entram na taxa de varredura;
    duas dependem de volume externo montado (`/Volumes/Externo`, `/Volumes/photo`); e
    duas são raízes largas demais para varrer sem decisão explícita
    (`/Users/acamerini` e `/users/acamerini`, o diretório home inteiro).

    Rodar `.venv/bin/python scripts/medir_baseline_producao.py --listar-fontes` e
    apresentar a lista com disponibilidade atual antes de perguntar. A escolha muda o
    tempo de execução de minutos para horas e define o que "acervo representativo"
    significa nas comparações futuras — por isso é decisão do dono, não do executor.
  </context>
  <options>
    <option id="representativo">
      <name>Só as raízes de acervo, sem o home inteiro</name>
      <pros>Mede o que o produto realmente organiza; roda em tempo previsível; comparável entre rodadas</pros>
      <cons>Não exercita o caso extremo de varredura ampla que já produziu ~422 mil registros no histórico</cons>
    </option>
    <option id="completo">
      <name>Todas as fontes PASTA disponíveis, inclusive o home</name>
      <pros>Reproduz o volume histórico real e estressa o limite de escala já medido</pros>
      <cons>Varredura de horas; volumes externos precisam estar montados; a rodada vira difícil de repetir</cons>
    </option>
    <option id="lista-propria">
      <name>Lista específica ditada pelo dono</name>
      <pros>Reflete exatamente o acervo que importa hoje</pros>
      <cons>Precisa ser transcrita em docs/PERFORMANCE.md para a próxima rodada ser comparável</cons>
    </option>
  </options>
  <resume-signal>Responda: representativo, completo, ou liste as pastas (uma por linha)</resume-signal>
  <acceptance_criteria>
    - A lista final de raízes está escrita no SUMMARY, com o motivo de cada exclusão (importador, volume não montado, escolha do dono).
    - Volumes externos incluídos na lista foram confirmados montados com `test -d` antes de rodar a Task 3.
  </acceptance_criteria>
  <verify>
    <human-check>Dono escolheu representativo, completo ou uma lista própria; a lista final e os motivos de exclusão estão transcritos no SUMMARY.</human-check>
  </verify>
  <done>Conjunto de raízes da medição definido e registrado, com volumes externos confirmados montados.</done>
</task>

<task type="auto">
  <name>Task 3: Rodar a medição e escrever docs/PERFORMANCE.md</name>
  <files>docs/PERFORMANCE.md</files>
  <read_first>
    - docs/AVALIACAO_UX.md (as ~60 primeiras linhas e os cabeçalhos `# ` — padrão acumulativo de rodada datada a espelhar)
    - .planning/PROJECT.md § Context (composição do acervo: ~99 mil registros conhecidos, ~422.738 no histórico de auditoria, ~5% com pixel local alcançável)
    - scripts/medir_baseline_producao.py (o script escrito na Task 1)
  </read_first>
  <action>
    Rodar `.venv/bin/python scripts/medir_baseline_producao.py` com as raízes aprovadas na
    Task 2, sem `--data-dir` (catálogo real de produção, decisão P-1). A varredura pode
    levar de dezenas de minutos a horas; deixá-la terminar antes de escrever o documento —
    número parcial não é baseline.

    Criar `docs/PERFORMANCE.md` com uma primeira seção `# Baseline de <AAAA-MM-DD> — pós-reset
    do catálogo (fase 5, LANC-04)`, seguindo a forma acumulativa de `docs/AVALIACAO_UX.md`
    (rodadas futuras acrescentam nova seção `# `, nunca reescrevem esta). Conteúdo mínimo:

    - **Contexto:** `catalog.db` zerado em 2026-08-16, backup preservado, primeira varredura
      completa desde então; composição do acervo medida (total de `media_files`, quantos
      alcançáveis agora), confrontada com os ~99 mil registros conhecidos e os ~422.738 do
      histórico de auditoria citados em `PROJECT.md`.
    - **Metodologia:** as três decisões P-1 (varredura in place no catálogo real), P-2
      (sugestões e duplicatas medidas em cópia descartável, e por quê) e P-3 (`advisor=None`,
      100% local), mais o comando exato que reproduz a medição e as raízes varridas — com as
      excluídas e o motivo. Registrar também que a medida é posterior à migração 0018
      (plano 05-01), portanto reflete o schema com os índices de FK.
    - **Taxa de indexação (varredura):** tabela com arquivos indexados, pulados, erros, MB
      processados, tempo total e arq/s — total e por raiz.
    - **Tempo de geração de sugestões:** tempo total, quantidade de sugestões produzidas,
      tamanho do catálogo no momento.
    - **Tempo de detecção de duplicatas:** tempo total e o resumo do `dict` de `detectar()`
      (grupos por nível).
    - **Máquina:** modelo, chip, RAM e versão do macOS (`system_profiler SPHardwareDataType`
      resumido) — sem isso os números não são comparáveis entre rodadas.
    - **O que observar na próxima rodada:** o que este baseline não cobre (importadores
      Apple/Lightroom não cronometrados; volumes não montados; caminho incremental
      inexistente para sugestões e duplicatas, registrado como dívida em
      `.planning/codebase/CONCERNS.md`).

    Ao final, apagar a cópia descartável? Não: mantê-la (invariante 8 — nada que possa ser
    referência é apagado) e registrar o caminho dela no documento.
  </action>
  <verify>
    <automated>test -f docs/PERFORMANCE.md && grep -c "^# Baseline de" docs/PERFORMANCE.md && grep -qi "Metodologia" docs/PERFORMANCE.md && grep -qiE "arq/s" docs/PERFORMANCE.md && grep -qi "advisor" docs/PERFORMANCE.md && echo "baseline documentado"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PERFORMANCE.md` existe, com exatamente uma seção `# Baseline de <data>` e uma subseção `## Metodologia`.
    - As três métricas de D-08 aparecem com número real e unidade: `arq/s` para varredura, segundos para geração de sugestões, segundos para detecção de duplicatas.
    - O documento nomeia as decisões P-1, P-2 e P-3 e o comando exato que reproduz a medição.
    - O documento registra o total de linhas em `media_files` após a varredura e o modelo/chip/RAM/macOS da máquina.
    - `sqlite3 "$HOME/Library/Application Support/FotoOrganizer/catalog.db" "select count(*) from suggestions"` retorna 0 — a medição de sugestões não deixou lote não revisado na produção (prova de P-2).
    - `ls "$HOME/Library/Application Support/FotoOrganizer/"catalog-antes-do-reset-*.db` continua existindo (nenhum backup removido).
  </acceptance_criteria>
  <done>Critério 4 da fase satisfeito: baseline com número, metodologia e máquina, reproduzível pela próxima rodada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script de medição → catálogo de produção | Único componente desta fase que escreve no `catalog.db` real |
| Script de medição → filesystem do acervo | A varredura lê pastas pessoais reais |
| Motor de classificação → serviço externo (advisor LLM) | Opt-in; este plano o mantém desligado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-30 | Tampering | `catalog.db` de produção durante a medição | mitigate | Sugestões e duplicatas rodam sobre cópia (`shutil.copy2`) e não in place (P-2); critério de aceite verifica `count(*) from suggestions == 0` na produção; o script não tem nenhum caminho de `unlink`/`DELETE`/`DROP` (verificado por grep) |
| T-05-31 | Information Disclosure | Envio de nomes de pasta e metadados ao advisor LLM | mitigate | `advisor=None` literal e sem flag que o ligue (P-3); critério de aceite confere por grep. Invariante 4 do CLAUDE.md: nada sai da máquina sem opt-in explícito |
| T-05-32 | Tampering | Varredura tocando arquivos originais | accept | `CatalogScanner.scan_source` é somente leitura por invariante 1 e já tem cobertura de teste própria; este plano não altera o scanner |
| T-05-33 | Denial of Service | Varredura de horas segurando o WAL do catálogo | accept | Medição deliberada e supervisionada; o checkpoint da Task 2 existe justamente para o dono aprovar o custo de tempo antes |
| T-05-34 | Information Disclosure | `docs/PERFORMANCE.md` versionado com caminhos pessoais | mitigate | Registrar raízes por nome de pasta como já aparecem em `PROJECT.md`/`docs`; não incluir listagem de arquivos individuais nem nomes de pessoas |
| T-05-SC | Tampering | instalações npm/pip/cargo | mitigate | Nenhum pacote novo; o script usa só o que `pyproject.toml` já pina. RESEARCH.md § Package Legitimacy Audit: não aplicável |

Per RESEARCH.md § Security Domain, medir performance não abre superfície ASVS
nova. O risco real aqui não é de atacante externo, é de a própria medição alterar
o acervo — coberto por T-05-30 e T-05-31.
</threat_model>

<verification>
1. Script roda ponta a ponta em ambiente descartável em segundos (verify automatizado da Task 1).
2. As três métricas de D-08 medidas contra o acervo real e escritas em `docs/PERFORMANCE.md`.
3. Produção sem sugestões auto-geradas pela medição; nenhum backup removido.
</verification>

<success_criteria>
- Critério 4 da Fase 5 satisfeito com número real, não estimativa.
- Metodologia registrada de forma que a próxima rodada seja comparável (D-09).
- Nenhum efeito colateral não revisado no catálogo de produção.
</success_criteria>

<output>
Criar `.planning/phases/05-prepara-o-para-lan-amento/05-04-SUMMARY.md` ao terminar.
Commits convencionais em português, ex.: `feat(05-04): script de baseline de performance
contra o acervo real` e `docs(05-04): registra o baseline de performance pós-reset`.
</output>
