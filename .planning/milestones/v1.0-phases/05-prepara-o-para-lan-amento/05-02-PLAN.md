---
phase: 05-prepara-o-para-lan-amento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
autonomous: true
requirements: [LANC-01]

must_haves:
  truths:
    - "Existe um bundle `Foto Organizer.app` construído a partir do scaffold atual do repo"
    - "O runtime Python embarcado importa rawpy e pillow_heif de dentro do próprio bundle"
    - "O webapp de produção está embarcado onde o backend o procura"
    - "A identidade de assinatura efetivamente aplicada ao bundle é conhecida e registrada (D-01: escopo Marco 1 apenas; D-02: verificar o scaffold existente, não reconstruir)"
  artifacts:
    - path: "src-tauri/resources/runtime/python/bin/python3"
      provides: "CPython python-build-standalone com o projeto instalado"
    - path: "src-tauri/target/release/bundle/macos/Foto Organizer.app"
      provides: "Bundle do Marco 1"
    - path: "webapp/dist/index.html"
      provides: "Front de produção que o runtime embarca"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/resources/runtime/**"
      via: "campo bundle.resources"
      pattern: "resources/runtime"
    - from: "src-tauri/src/main.rs"
      to: "resources/runtime/python/bin/python3"
      via: "resource_dir().join(...) no setup()"
      pattern: "resources/runtime/python/bin/python3"
---

<objective>
Construir o bundle do Marco 1 de `docs/EMPACOTAMENTO.md` a partir do scaffold que
já existe no repo (commits `5a797e1` e `30ba735`) e descobrir empiricamente qual
assinatura o `cargo tauri build` aplica quando nenhuma `signingIdentity` está
configurada.

Purpose: o plano 05-03 só consegue exercer o critério de aceite do Marco 1 se
existir um `.app` construído. Este plano produz esse artefato e resolve a única
suposição não verificada da pesquisa (A1: assinatura ad-hoc automática ou não).
Output: `src-tauri/target/release/bundle/macos/Foto Organizer.app` e a saída de
`codesign -dv` registrada no SUMMARY.

Escopo: Marco 1 apenas, per D-01. Não pedir, não configurar e não usar certificado
Developer ID — `security find-identity` confirma que a máquina não tem um, e o
custo do Apple Developer Program é decisão do dono, não default desta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prepara-o-para-lan-amento/05-RESEARCH.md

<interfaces>
<!-- Contratos e caminhos exatos. Não explorar o repo para descobri-los. -->

Sequência canônica de build (`docs/EMPACOTAMENTO.md` § Build — passo a passo):
1. `(cd webapp && npm ci && npm run build)`
2. `scripts/empacotar_runtime.sh` → gera `src-tauri/resources/runtime/`
3. `(cd src-tauri && cargo tauri icon icons/icon.png)` — os ícones já existem
   (`src-tauri/icons/icon.icns` e `icon.png`), passo só se o build reclamar
4a. `(cd src-tauri && cargo tauri build)` — caminho sem assinatura (Marco 1)

`src-tauri/tauri.conf.json` (estado atual, campos relevantes):
```json
{ "productName": "Foto Organizer",
  "identifier": "app.acamerini.fotoorganizer",
  "build": { "frontendDist": "../webapp/dist" },
  "bundle": { "targets": ["app", "dmg"],
              "resources": ["resources/runtime/**/*"],
              "macOS": { "entitlements": "Entitlements.plist",
                         "minimumSystemVersion": "12.0" } } }
```

`src-tauri/src/main.rs` espera o Python embarcado exatamente em
`resource_dir()/resources/runtime/python/bin/python3` e o invoca com
`-m fotoorganizer web --porta 0 --encerrar-com-pai`.

Nome do binário dentro do bundle (`src-tauri/Cargo.toml` `name = "foto-organizer"`):
`Foto Organizer.app/Contents/MacOS/foto-organizer`.

Ambiente já verificado: rustc/cargo 1.97.1, tauri-cli 2.11.4, node v24.14.0,
npm 11.9.0, `.venv` Python 3.12.5, `webapp/node_modules` presente no worktree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Front de produção e runtime Python embarcado</name>
  <files>webapp/dist/ (artefato de build), src-tauri/resources/runtime/ (artefato de build)</files>
  <read_first>
    - docs/EMPACOTAMENTO.md (arquivo inteiro — decisão PBS, passo a passo, contingências)
    - scripts/empacotar_runtime.sh (arquivo inteiro — o que ele baixa, onde instala, o que ele já prova)
    - src-tauri/src/main.rs (caminho exato do python embarcado que o shell espera)
  </read_first>
  <action>
    Antes de qualquer download, conferir espaço em disco: `df -h /` mostrava ~12Gi livres
    na pesquisa, margem apertada para runtime PBS + `src-tauri/target` + DMG. Se estiver
    abaixo de 8Gi, limpar `src-tauri/target/` antes de seguir e registrar isso no SUMMARY.

    Construir o front de produção com `(cd webapp && npm run build)` — usar `npm run build`
    direto, sem `npm ci`, porque `webapp/node_modules` já existe neste worktree e um `npm ci`
    o apagaria e reinstalaria sem necessidade. Se `npm run build` falhar por dependência
    ausente, aí sim rodar `npm ci` e registrar o motivo.

    Gerar o runtime embarcado com `scripts/empacotar_runtime.sh` (sem argumentos: arm64,
    Python 3.12, extras `xmp,apple`). O script baixa o tarball `install_only` do
    python-build-standalone a partir da release mais recente de `astral-sh/python-build-standalone`,
    instala o projeto dentro dele e embarca `webapp/dist` em `site-packages/webapp/dist`.
    Não instalar o extra `llm`: ele habilita envio de dados a serviço externo e é opt-in de
    release, não de Marco 1 (invariante 4 do CLAUDE.md).

    Se o download do PBS falhar por rede ou por a release mais recente não ter asset 3.12
    arm64, não improvisar outro runtime nem cair para PyInstaller — parar, registrar o erro
    e escalar; PBS é decisão travada em `docs/EMPACOTAMENTO.md`.
  </action>
  <verify>
    <automated>test -f webapp/dist/index.html && src-tauri/resources/runtime/python/bin/python3 -c "import rawpy, pillow_heif, fotoorganizer; print('runtime ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/dist/index.html` existe e `ls webapp/dist/assets | wc -l` retorna 1 ou mais.
    - `src-tauri/resources/runtime/python/bin/python3 --version` imprime `Python 3.12.x`.
    - `src-tauri/resources/runtime/python/bin/python3 -c "import rawpy, pillow_heif, fotoorganizer"` sai com código 0.
    - `test -f src-tauri/resources/runtime/python/lib/python3.12/site-packages/webapp/dist/index.html` sai com código 0 (o front está onde `server/app.py` procura).
    - `src-tauri/resources/runtime/python/bin/python3 -c "import anthropic"` FALHA (extra `llm` deliberadamente fora).
  </acceptance_criteria>
  <done>Runtime autocontido importa as libs nativas de dentro dele mesmo e carrega o front embarcado.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle Tauri e identidade de assinatura efetiva</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    - src-tauri/tauri.conf.json (arquivo inteiro — estado atual dos campos de bundle)
    - src-tauri/Entitlements.plist (o que o bundle pede ao sistema)
    - .planning/phases/05-prepara-o-para-lan-amento/05-RESEARCH.md § Pitfall 1 e § Assumptions Log (A1)
  </read_first>
  <action>
    Rodar `(cd src-tauri && cargo tauri build)`. É a compilação release completa do Tauri:
    pode levar vários minutos na primeira vez. Não passar `--target` nem
    `signingIdentity` — Marco 1 é o caminho 4a de `docs/EMPACOTAMENTO.md`.

    Terminado o build, inspecionar a assinatura efetivamente aplicada com
    `codesign -dv --verbose=4 "src-tauri/target/release/bundle/macos/Foto Organizer.app"`
    (a saída vai para stderr — redirecionar com `2>&1`). Registrar no SUMMARY o valor de
    `Authority`, `Signature` e `TeamIdentifier`. Este é o teste da suposição A1 da pesquisa:
    se a saída indicar `Signature=adhoc`, o default do Tauri já basta para o Marco 1 e
    `tauri.conf.json` NÃO deve ser alterado. Se indicar objeto não assinado (`code object is
    not signed at all`), acrescentar `"signingIdentity": "-"` dentro de `bundle.macOS` em
    `src-tauri/tauri.conf.json` e reconstruir — assinatura ad-hoc, pseudo-identidade `-`,
    que não exige nem envolve certificado pago.

    Conferir também que o runtime foi de fato embarcado no bundle, em
    `Foto Organizer.app/Contents/Resources/resources/runtime/python/bin/python3`, e que o
    binário nativo está em `Foto Organizer.app/Contents/MacOS/foto-organizer`. Se o glob
    `resources/runtime/**/*` não tiver copiado a árvore inteira, é defeito de empacotamento
    a corrigir aqui (D-03) — não seguir para o plano 05-03 com um bundle incompleto.

    Não rodar `scripts/assinar_runtime.sh`, não chamar `notarytool`, não chamar `stapler`:
    tudo isso é Marco 2, fora de escopo por D-01.
  </action>
  <verify>
    <automated>test -d "src-tauri/target/release/bundle/macos/Foto Organizer.app" && test -x "src-tauri/target/release/bundle/macos/Foto Organizer.app/Contents/MacOS/foto-organizer" && test -x "src-tauri/target/release/bundle/macos/Foto Organizer.app/Contents/Resources/resources/runtime/python/bin/python3" && codesign -dv --verbose=4 "src-tauri/target/release/bundle/macos/Foto Organizer.app" 2>&1 | grep -E "Signature|Authority|not signed"</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/target/release/bundle/macos/Foto Organizer.app` existe como diretório.
    - `Foto Organizer.app/Contents/MacOS/foto-organizer` existe e é executável.
    - `Foto Organizer.app/Contents/Resources/resources/runtime/python/bin/python3 --version` imprime `Python 3.12.x` (runtime embarcado, não o do sistema).
    - `codesign -dv --verbose=4 "...Foto Organizer.app" 2>&1` NÃO contém `code object is not signed at all`; a linha de `Signature`/`Authority` observada está transcrita no SUMMARY.
    - Se e somente se o bundle tiver saído sem assinatura: `grep -c "signingIdentity" src-tauri/tauri.conf.json` retorna 1 e o build foi refeito; caso contrário `git diff --stat src-tauri/tauri.conf.json` sai vazio.
    - Nenhum comando de `notarytool`, `stapler` ou `assinar_runtime.sh` foi executado.
  </acceptance_criteria>
  <done>Bundle do Marco 1 existe, com runtime embarcado no lugar certo e identidade de assinatura conhecida e registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rede → máquina do dev | `scripts/empacotar_runtime.sh` baixa um tarball de release do GitHub e `pip install` resolve wheels |
| Bundle → sistema operacional | Entitlements e assinatura definem o que o `.app` pode fazer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | Tarball PBS baixado por `curl` da release `astral-sh/python-build-standalone` | accept | Origem oficial sobre HTTPS, mesma cadeia de confiança que o `pip` do projeto já usa; o script falha explicitamente se o asset não for encontrado, em vez de cair para outra origem |
| T-05-11 | Elevation of Privilege | `src-tauri/Entitlements.plist` | mitigate | Ler o arquivo antes do build e confirmar que ele não pede App Sandbox nem entitlement além do necessário para o runtime hardened; qualquer entitlement novo neste plano é proibido |
| T-05-12 | Information Disclosure | Extra `llm` do `pip install` | mitigate | `scripts/empacotar_runtime.sh` roda com extras `xmp,apple`; a Task 1 verifica por asserção que `anthropic` NÃO importa do runtime embarcado — nada que envie dados para fora entra no bundle (invariante 4) |
| T-05-SC | Tampering | instalações npm/pip/cargo | mitigate | Nenhum pacote novo é adicionado ao `pyproject.toml`, `package.json` ou `Cargo.toml`; o `pip install` do script instala apenas o próprio projeto e extras já pinados. RESEARCH.md § Package Legitimacy Audit: não aplicável. Pacote novo aparecendo aqui = parar e escalar |

Per RESEARCH.md § Security Domain, empacotar não introduz superfície ASVS nova:
o backend embarcado continua escutando só em 127.0.0.1, o subprocesso é spawnado
por lista de argumentos sem `shell=True`, e nada do que o `.app` faz muda os 8
invariantes do CLAUDE.md.
</threat_model>

<verification>
1. Runtime embarcado importa `rawpy`, `pillow_heif` e `fotoorganizer` de dentro do bundle.
2. Bundle existe com binário nativo e árvore `resources/runtime` completa.
3. Saída de `codesign -dv` transcrita no SUMMARY, resolvendo a suposição A1.
</verification>

<success_criteria>
- `Foto Organizer.app` construído a partir do scaffold existente, sem Marco 2.
- Suposição A1 respondida com evidência de comando, não com leitura de documentação.
- Nenhum entitlement, dependência ou extra novo entrou no bundle.
</success_criteria>

<output>
Criar `.planning/phases/05-prepara-o-para-lan-amento/05-02-SUMMARY.md` ao terminar,
com a saída literal de `codesign -dv --verbose=4`.
Commit só se houver mudança rastreada (ex.: `fix(05-02): assinatura ad-hoc explícita
no bundle do Marco 1`); artefatos de build não vão para o git.
</output>
