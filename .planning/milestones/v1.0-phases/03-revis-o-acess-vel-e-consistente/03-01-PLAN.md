---
phase: 03-revis-o-acess-vel-e-consistente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/App.tsx
  - webapp/src/App.test.tsx
autonomous: true
requirements: [REV-03]

must_haves:
  truths:
    - "(D-03) Os 3 pontos restantes usam o mesmo padrão setBusca(\"\") já aplicado em Panorama.aoRecortar e Trips.onAbrir: botão de troca de aba, Sidebar.onSelecionarPasta e StatusBar.aoIrPara"
    - "Digitar uma busca na Biblioteca, trocar de aba pelo botão de aba e voltar mostra o campo de busca vazio"
    - "Digitar uma busca na Biblioteca e escolher uma pasta na lateral (\"ver na grade\") limpa o campo de busca"
    - "Digitar uma busca na Biblioteca e clicar num degrau do funil na barra de status limpa o campo de busca"
    - "Clicar na aba já ativa NÃO apaga a busca que o usuário acabou de digitar"
    - "Um \"0 no filtro\" na Biblioteca só pode significar que o filtro atual devolve zero, nunca que uma busca de outra tela sobreviveu"
  artifacts:
    - path: "webapp/src/App.tsx"
      provides: "setBusca(\"\") nos 5 pontos de entrada de navegação (2 existentes + 3 de REV-03)"
      contains: "setBusca(\"\")"
    - path: "webapp/src/App.test.tsx"
      provides: "regressão dos 3 novos pontos + guarda da aba já ativa"
      contains: "toHaveValue(\"\")"
  key_links:
    - from: "webapp/src/App.tsx botão de aba (ABAS.map onClick)"
      to: "setBusca"
      via: "reset de estado antes de setAba(nome), condicionado a nome !== aba"
      pattern: "setBusca\\(\"\"\\)"
    - from: "webapp/src/App.tsx onSelecionarPasta (prop de Sidebar)"
      to: "setBusca"
      via: "reset de estado ao lado de setSelIndex(null)"
      pattern: "setBusca\\(\"\"\\)"
    - from: "webapp/src/App.tsx aoIrPara (prop de StatusBar)"
      to: "setBusca"
      via: "reset de estado ao lado de setRecorte(null)/setFonte(null)"
      pattern: "setBusca\\(\"\"\\)"
---

<objective>
REV-03 (parcial): fechar os 3 pontos de entrada de navegação que ainda deixam
uma busca de texto antiga sobreviver e fazer a Biblioteca mentir "0 no filtro".

Purpose: hoje 2 dos 5 pontos de entrada (`Panorama.aoRecortar`,
`Trips.onAbrir`) já chamam `setBusca("")`. Os outros 3 — botão de troca de
aba, `Sidebar.onSelecionarPasta`, `StatusBar.aoIrPara` — não. O resultado
medido (`docs/AVALIACAO_UX.md` A.2) é um recorte de milhares de fotos
aparecer como vazio, com a tela mostrando a mensagem genérica de biblioteca
vazia em vez de dizer que era a busca antiga filtrando tudo.

Output: 3 chamadas `setBusca("")` em `webapp/src/App.tsx` + 4 testes de
regressão em `webapp/src/App.test.tsx` (3 dos novos pontos + 1 guarda de que
clicar na aba já ativa não apaga a busca recém-digitada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-revis-o-acess-vel-e-consistente/03-CONTEXT.md
@.planning/phases/03-revis-o-acess-vel-e-consistente/03-PATTERNS.md
@CLAUDE.md

Fase 100% frontend, 100% estado local do React. Nenhum endpoint novo,
nenhuma mudança de backend, nenhuma dependência nova.
</context>

<interfaces>
<!-- Contratos que o executor precisa; extraídos do código atual. Não explorar o repo. -->

`webapp/src/App.tsx:73` — `const [busca, setBusca] = useState("");`
(já declarado; nenhum estado novo, nenhum import novo)

`webapp/src/App.tsx:358-361` — o input da busca, único ponto de leitura no teste:
`placeholder="Buscar por nome ou caminho…"`, `value={busca}`.

`webapp/src/components/Sidebar.tsx:19-20` —
`pastaAtual: string | null; onSelecionarPasta: (pasta: string | null) => void;`

`webapp/src/components/StatusBar.tsx:30` —
`aoIrPara?: (alcance: "tudo" | "organizaveis") => void;`

Gatilhos reais em teste (confirmados por leitura de
`ArvoreDePastas.test.tsx` e `Funil.tsx`):
- `onSelecionarPasta` só dispara pelo botão explícito **"ver na grade"** da
  `ArvoreDePastas` (navegar na árvore NÃO chama o callback — invariante já
  testada em `ArvoreDePastas.test.tsx`). O botão só existe quando `aberta`
  não é `null`, ou seja quando a fixture de `/api/pastas` devolve um
  `caminho` não vazio OU o usuário desceu um nível.
- `aoIrPara` dispara pelos degraus clicáveis do `Funil` dentro do
  `data-testid="funil"` da `StatusBar`; o degrau "conhecidas" chama
  `aoIrPara("tudo")` e é um `<button>` cujo nome acessível é o número
  formatado + o rótulo (ex.: `30 conhecidas`).

Fixture do dublê de API: `webapp/src/test/servidor.tsx` exporta
`servirApi(rotas)`, `montar(ui)` e `ROTAS_BASE`. `ROTAS_BASE` **não** inclui
`/api/pastas` — rota não declarada responde 404. `NivelDePastas`
(`webapp/src/api.ts:148-158`) tem o formato
`{ caminho, aqui, filhos: [{ nome, caminho, total, alcancaveis }] }`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de regressão dos 3 pontos de busca (RED)</name>

  <files>webapp/src/App.test.tsx</files>

  <read_first>
    - `webapp/src/App.test.tsx` — em especial o teste existente
      `it("abrir uma viagem limpa a busca deixada de outra visita à Biblioteca", ...)`
      (linhas 116-143): é o molde exato de shape (typar na busca → navegar →
      `expect(...).toHaveValue("")`).
    - `webapp/src/test/servidor.tsx` — `servirApi`, `montar`, `ROTAS_BASE`.
    - `webapp/src/components/ArvoreDePastas.test.tsx` — fixtures `RAIZ` e
      `DENTRO_DE_VOLUMES` e o teste
      `it("navegar não filtra a grade — só o botão explícito filtra", ...)`,
      que prova que o gatilho é o botão "ver na grade".
    - `webapp/src/App.tsx` — estado atual dos 3 alvos (linhas 207-217,
      226-241, 437-443) e dos 2 análogos já corretos (linhas 260-283).
    - `.planning/phases/03-revis-o-acess-vel-e-consistente/03-PATTERNS.md`
      seção "Test coverage for REV-03".
  </read_first>

  <behavior>
    Quatro novos blocos `it(...)` dentro do `describe("App", ...)` existente,
    todos em português como o resto do arquivo:

    - Teste 1 — **botão de troca de aba**: `servirApi(ROTAS_BASE)`; clicar
      "Biblioteca"; typar `IMG` no input `Buscar por nome ou caminho…`;
      clicar a aba "Duplicatas"; clicar a aba "Biblioteca"; esperar que o
      input volte com `toHaveValue("")`.
      (Sequência escolhida de propósito: nenhum outro handler de REV-03 é
      tocado, então o teste isola o botão de aba.)

    - Teste 2 — **`onSelecionarPasta`**: `servirApi({ ...ROTAS_BASE,
      "/api/pastas": <nível com caminho não vazio> })` para que o botão
      "ver na grade" exista sem precisar descer na árvore; clicar
      "Biblioteca"; typar `IMG`; clicar o botão `ver na grade`; esperar
      `toHaveValue("")`.
      Nenhum clique em aba entre typar e o gatilho — é isso que faz o teste
      provar `onSelecionarPasta` e não o botão de aba.

    - Teste 3 — **`aoIrPara`**: `servirApi(ROTAS_BASE)`; clicar "Biblioteca";
      typar `IMG`; dentro de `screen.getByTestId("funil")` clicar o botão do
      degrau `conhecidas`; esperar `toHaveValue("")`.
      `aoIrPara` chama `setAba("Biblioteca")` estando já na Biblioteca, então
      o botão de aba não participa — o teste isola `aoIrPara`.

    - Teste 4 — **guarda da aba já ativa** (trava a decisão de discretion do
      D-03/Claude's Discretion): clicar "Biblioteca"; typar `IMG`; clicar de
      novo a aba "Biblioteca"; esperar `toHaveValue("IMG")`. Este teste passa
      hoje e precisa continuar passando: clicar na aba em que já se está é
      no-op e não pode destruir o que o usuário acabou de digitar.

    Além disso: acrescentar um comentário curto ao teste existente de Viagens
    (linhas 116-143) registrando que, depois de REV-03, o clique na aba
    "Viagens" também limpa a busca — ou seja, aquele teste deixa de isolar
    `Trips.onAbrir` e passa a cobrir o contrato de saída, não o call site.
    Não alterar as asserções dele.
  </behavior>

  <action>
    Escrever os 4 testes acima em `webapp/src/App.test.tsx`, dentro do
    `describe("App", ...)` que já existe, seguindo o shape do teste de
    Viagens: `servirApi(...)`, `const usuario = userEvent.setup()`,
    `montar(<App />)`, `await usuario.click(await screen.findByText(...))`,
    `await usuario.type(busca, "IMG")`, asserção final com
    `await screen.findByPlaceholderText("Buscar por nome ou caminho…")` e
    `toHaveValue(...)`.

    Detalhes concretos que removem ambiguidade:
    - Selecionar abas por `screen.getByRole("button", { name: "Duplicatas" })`
      / `{ name: "Biblioteca" }` (as abas são `<button>` com o nome como
      único filho; o arquivo já usa esse seletor em outros testes).
    - Fixture de `/api/pastas` para o Teste 2: um `NivelDePastas` com
      `caminho: "/Volumes"`, `aqui: 3` e um filho
      `{ nome: "photo", caminho: "/Volumes/photo", total: 225914, alcancaveis: 0 }`
      — mesma forma do `DENTRO_DE_VOLUMES` de `ArvoreDePastas.test.tsx`. Com
      `caminho` não vazio, `ArvoreDePastas` renderiza o botão
      `ver na grade` de imediato (`pastaAtual !== aberta`).
    - No Teste 3, importar `within` de `@testing-library/react` (ou usar
      `screen.getByTestId("funil")` + `within`) e localizar o degrau por
      `getByRole("button", { name: /conhecidas/ })`; o valor numérico vem da
      fixture `/api/funil` de `ROTAS_BASE` (`conhecidas: 30`), então casar por
      regex do rótulo, não pelo número inteiro.
    - Não usar `waitFor` com asserção de ausência onde `findBy*` já resolve.

    NÃO tocar em `webapp/src/App.tsx` nesta task — a implementação é a Task 2.
    Os testes 1, 2 e 3 DEVEM falhar ao final desta task; o teste 4 e os 10
    testes existentes DEVEM continuar passando.
  </action>

  <verify>
    <automated>cd webapp && npx vitest run src/App.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cd webapp && npx vitest run src/App.test.tsx` reporta **14 testes**
      (10 pré-existentes + 4 novos) com exatamente **3 falhas** e 11 passes.
    - As 3 falhas são os testes de aba, de `ver na grade` e do degrau
      `conhecidas`, e cada mensagem de falha é da forma
      `expected element to have value "" ... received "IMG"` (falha pelo
      motivo certo — busca sobreviveu — e não por rota 404, elemento não
      encontrado ou timeout).
    - O teste 4 (aba já ativa mantém `IMG`) passa.
    - `git diff --name-only` lista apenas `webapp/src/App.test.tsx`.
    - `grep -c 'toHaveValue' webapp/src/App.test.tsx` retorna 5 ou mais
      (1 pré-existente + 4 novos).
  </acceptance_criteria>

  <done>
    App.test.tsx tem 4 testes novos; 3 falham por busca sobrevivente e 1
    (guarda da aba ativa) passa; App.tsx intocado.
  </done>
</task>

<task type="auto">
  <name>Task 2: setBusca("") nos 3 pontos de navegação (GREEN)</name>

  <files>webapp/src/App.tsx</files>

  <read_first>
    - `webapp/src/App.tsx` — os 2 call sites já corretos que são o molde
      exato: `Panorama.aoRecortar` (linhas 260-272, `setBusca("")` na 267,
      com o comentário que explica o bug) e `Trips.onAbrir` (linhas 273-283,
      `setBusca("")` na 277). E os 3 alvos: botão de aba (linhas 207-217),
      `onSelecionarPasta` (linhas 226-241), `aoIrPara` (linhas 437-443).
    - `webapp/src/App.test.tsx` — os 4 testes escritos na Task 1, que são o
      contrato que esta task precisa satisfazer.
    - `.planning/phases/03-revis-o-acess-vel-e-consistente/03-PATTERNS.md`
      seção `setBusca("") on navigation (REV-03)`.
  </read_first>

  <action>
    Aplicar o mesmo padrão dos 2 call sites existentes — uma linha
    `setBusca("");` inserida junto dos outros resets de estado do handler —
    nos 3 alvos de D-03. Sem estado novo, sem import novo, sem props novas,
    sem chips de filtro ativo (a alternativa de `docs/AVALIACAO_UX.md` A.2
    foi explicitamente rejeitada: o app já decidiu "limpar", não "mostrar
    como chip removível").

    1. **Botão de troca de aba** (`ABAS.map(...)`, `onClick={() => setAba(nome)}`,
       linha 211). Expandir a arrow inline para corpo de bloco e limpar a
       busca **apenas quando a aba de destino é diferente da atual**
       (`nome !== aba`). Essa é a resolução do item aberto em CONTEXT.md
       `Claude's Discretion`: clicar na aba em que já se está é um no-op hoje
       e continua sendo — apagar o texto que o usuário acabou de digitar
       porque ele reclicou a aba ativa seria destruir trabalho sem que nada
       tenha navegado. Trocar de aba de fato sempre limpa, inclusive entre
       abas sem grade, porque o problema é a busca sobreviver à navegação,
       não o destino específico.
    2. **`onSelecionarPasta`** (prop de `Sidebar`, linhas 231-237). Inserir
       `setBusca("");` no corpo do callback, ao lado de `setSelIndex(null)`,
       antes do `if (p) setAba("Biblioteca")`.
    3. **`aoIrPara`** (prop de `StatusBar`, linhas 437-443). Inserir
       `setBusca("");` no corpo do callback, junto de `setRecorte(null)` /
       `setFonte(null)`.

    Comentários: o codebase explica *por que* um reset existe quando o motivo
    não é óbvio (ver o comentário em `Panorama.aoRecortar` e em
    `onSelecionarPasta`). Adicionar um comentário curto em português apenas
    no botão de aba, registrando a condição `nome !== aba` e o motivo; os
    outros dois não precisam repetir a explicação já presente no arquivo.

    Nada mais em `App.tsx` muda: nenhum outro handler, nenhum
    `setBusca(e.target.value)` (linha 360) tocado, nenhuma classe Tailwind
    alterada (REV-02 é o plano 03-02 e não toca este arquivo).
  </action>

  <verify>
    <automated>cd webapp && npx vitest run 2>&1 | tail -10 && npx tsc -b 2>&1 | tail -5 && grep -c 'setBusca("")' src/App.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `cd webapp && npx vitest run` termina com 0 falhas em toda a suíte
      (todos os arquivos de teste, não só `App.test.tsx`); `src/App.test.tsx`
      reporta 14/14 passando.
    - `cd webapp && npx tsc -b` sai com código 0 e sem diagnóstico.
    - `grep -c 'setBusca("")' webapp/src/App.tsx` retorna exatamente **5**
      (os 2 pré-existentes + os 3 de REV-03). O token `setBusca("")` não pode
      aparecer em comentário — se precisar citá-lo em prosa, escrever sem os
      parênteses/aspas para não inflar a contagem.
    - `grep -n 'nome !== aba' webapp/src/App.tsx` casa dentro do `onClick` do
      botão de aba.
    - `git diff --stat webapp/src/App.tsx` mostra apenas inserções nas três
      regiões nomeadas; `git diff webapp/package.json webapp/package-lock.json`
      é vazio (nenhuma dependência nova nesta fase).
    - Comportamento: com `IMG` digitado na Biblioteca, clicar "Duplicatas" e
      voltar mostra o campo vazio; reclicar "Biblioteca" estando na
      Biblioteca mantém `IMG`.
  </acceptance_criteria>

  <done>
    Os 5 pontos de entrada de navegação da Biblioteca limpam a busca; a aba
    já ativa não limpa; suíte inteira verde e build de tipos limpo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (webapp) → API local 127.0.0.1 | Único limite tocado indiretamente: `busca` vira query string em `/api/midia`. Esta fase só **remove** valor desse parâmetro, nunca adiciona campo novo. |
| usuário → estado React local | `setBusca("")` é setter de `useState`; sem persistência, sem I/O, sem subprocesso. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `webapp/src/App.tsx` handlers de navegação | accept | Só resets de estado local síncronos; nenhum dado do catálogo é escrito. Invariante 1 do CLAUDE.md (catalogação somente leitura) não é tocada. |
| T-03-02 | Information Disclosure | parâmetro `busca` em `GET /api/midia` (127.0.0.1) | accept | A mudança estreita o que é enviado (string vazia em vez do termo antigo); nenhum campo novo trafega e nada sai da máquina. |
| T-03-03 | Denial of Service | re-render/refetch extra ao limpar a busca na troca de aba | accept | `busca` já é dependência de query existente; limpar dispara no máximo o mesmo refetch que trocar de aba já dispara. Sem laço, sem polling novo. |
| T-03-SC | Tampering | instalação de pacotes npm/pip/cargo | mitigate | Esta fase instala **zero** pacotes. Gate: `git diff webapp/package.json webapp/package-lock.json` deve ser vazio no fim do plano (já em `<acceptance_criteria>` da Task 2). Nenhum Package Legitimacy Audit é necessário porque nenhum install ocorre. |

Perfil de risco: baixo. Frontend puro, sem superfície de rede nova, sem
mudança de fluxo de dado, backend intocado.
</threat_model>

<verification>
1. `cd webapp && npx vitest run` — suíte inteira verde (todos os arquivos).
2. `cd webapp && npx tsc -b` — sem erro de tipo.
3. `grep -c 'setBusca("")' webapp/src/App.tsx` → 5.
4. `git diff --name-only` lista apenas `webapp/src/App.tsx` e
   `webapp/src/App.test.tsx`.
5. `scripts/verificar.sh --rapido` (se `.venv` disponível no worktree) — não
   pode regredir; esta fase não toca Python, então pytest deve ficar no mesmo
   estado do baseline.
</verification>

<success_criteria>
- REV-03 fechado: os 3 pontos nomeados (`botão de aba`,
  `Sidebar.onSelecionarPasta`, `StatusBar.aoIrPara`) limpam a busca, somando
  5/5 pontos de entrada.
- 4 testes de regressão novos em `App.test.tsx`, todos verdes.
- Clicar na aba já ativa preserva a busca (decisão de UX travada por teste).
- Zero mudança em backend, dependências ou classes Tailwind.
</success_criteria>

<output>
Create `.planning/phases/03-revis-o-acess-vel-e-consistente/03-01-SUMMARY.md` when done
</output>
